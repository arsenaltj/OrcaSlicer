# 发现与决策

## 当前基线

- 当前开发分支：`codex/model-generation-v2`
- 创建基线：`codex/orca-integration-v2`，提交 `808efe4401`
- 冻结旧分支：`codex/model-generation`，提交 `16f8ac0fb0`
- 集成提交 `616a063192` 明确记录 `Accepted-feature: 16f8ac0fb0`。
- 旧功能线和集成线没有共同祖先，因此不应直接双向 merge。

## 文档审计

- 旧功能线有大量未被集成线收录的逐阶段设计、实施和复核文档；全部恢复会让当前产品线混入过期要求。
- 三份根目录上下文文件包含最完整的长期决策、进度和路线图，因此以迁移时工作树状态冻结归档。
- 精选补充资料覆盖四色图片管线、Beta 1 路线、盲测、多视图校准、模块边界和供应商边界。
- 两份大型质量报告已经存在于集成线的小写 `docs/` 路径，且 Git blob 与旧分支版本完全一致，不需要复制。

## 分支工作流

- 功能开发从最新集成线创建新分支。
- 验收后把固定 SHA 集成到 `codex/orca-integration-v2`。
- 已完成的功能分支冻结或归档；下一轮从更新后的集成线重新创建功能分支。
- 不把集成线反向 merge 或快照移植到旧功能线。

## 风险提示

- 当前本地集成线 `808efe4401` 比远端 `origin/codex/orca-integration-v2` 的 `1fbcd10416` 多一个提交；多电脑协作前应先统一远端主线。
- 工作区仍有大量未跟踪的临时产物，提交时必须使用精确路径，不能直接 `git add -A`。

## 历史资料

- [模型生成 v1 历史上下文](Docs/history/model-generation-v1/README.md)

## 2026-09-03：六通道模型生成复核

- v2 基线已经包含 Orca 原生混色能力：混合耗材配置、CMYW/RYBW/材料列表配方推荐、子层或整层执行，以及 3MF 往返保存。
- 当前模型生成链路仍把可打印能力表示为槽位编号和 RGB 字符串，并把 compatible slots 截断到前四个；这无法区分物理通道和虚拟混色槽。
- C++ 面板与 Python sidecar 存在大量固定四元素结构；简单把常量 4 改为 6 只能得到六个离散色，不能表达过程混色。
- 当前 Orca 的产品级配方搜索和 UI 以每个目标色 2～3 个分量为主。六通道应作为物理颜色池；不同目标色可选不同稀疏配方，整个模型可共同使用全部六通道。
- `ModelImportRequest::auto_slice_after_import`、`ModelImportResult::slice_after_import`、面板开关、适配器打印配置写入和布尔导航回调共同构成模型生成触发切片的完整链路，需要一起移除。
- 目标边界：模型生成发布经校验的几何制品与颜色意图；Orca 工作区负责导入和持久化；智能切片分支负责后续切片决策。

## 2026-09-03：方案比较

| 方案 | 结论 | 原因 |
|------|------|------|
| 将所有四色常量直接改成六色 | 拒绝 | 仍会混淆物理通道、目标色和混色配方，后续继续返工 |
| 在 Python sidecar 复制一套 CMYK/六色求解器 | 拒绝 | 与 Orca 原生能力产生双实现和颜色结果漂移 |
| 类型化颜色契约 + OBJ/颜色意图清单 + Orca 适配层复用原生配方引擎 | 采用 | 保持模块边界、兼容旧制品，并能逐阶段交付 |

## 2026-09-03：自动切片删除边界

- `ModelGenerationPanel` 的导入设置区同时包含颜色模式和自动切片复选框；删除复选框后保留颜色模式，并把区标题从“导入与切片”改为“导入设置”。
- `ModelImportRequest` 和 `ModelImportResult` 中的两个切片布尔值可以直接删除；它们尚未形成持久化格式，不涉及 3MF/profile 迁移。
- `OrcaWorkspaceAdapter` 当前会在自动切片前关闭独立支撑层高和擦料塔；删除自动切片后，这段打印预设写入也应整体删除，避免模型导入改变打印参数。
- `MainFrame` 的导入完成回调目前用 `bool slice` 同时负责准备页导航和发起切片；应收窄成无参回调，只更新工作区并进入准备页。
- Sidebar 仍可显示导入、网格检查、颜色处理和摆放结果；切片/G-code 步骤统一保持等待，不从模型生成域启动。
- 原生契约回归位于 `tests/slic3rutils/test_ai_contracts.cpp`，可先用编译失败/测试变化驱动字段删除；架构预算由 `tools/ai/test_integration_guardrails.py` 和 `scripts/verify_ai_integration.py` 守护。
- 当前仓库已有可复用的 Visual Studio 构建树 `build/` 和 `build-ai-tests/`；实现阶段优先构建 `slic3rutils_tests`，再执行架构守卫和完整 Release 验证。

## 2026-09-03：自动切片移除结果

- 生产调用链已不再包含 `auto_slice_after_import`、`slice_after_import` 或“导入后自动切片”控件；导入完成回调只有更新工作区和进入 Prepare 两项职责。
- 模型生成导入不再修改 `independent_support_layer_height`、`enable_prime_tower` 等打印预设，也不再发出切片事件或写入 `slice_requested` 旅程事件。
- 旧模型库 JSON 中的自动切片键不会阻断加载；新代码忽略它们，并在模型再次导入时删除这些过期键。这保持了读取兼容，同时停止继续发布错误状态。
- 架构守卫新增仓库正例和包含旧字段、预设写入、切片事件的反例 fixture；全套 41 项 Python 守卫通过。
- 本机现有构建树缺少完整的捆绑 Python 3.12.13 开发运行时，无法可靠重新配置或链接完整原生测试；独立 MSVC 编译契约测试源成功，GUI 全量验证需在恢复标准构建环境后补做。

## 2026-09-03：六通道契约基础

- 通道上限属于模型生成能力边界，不等于 Orca 项目槽位编号上限；物理通道用零基 `slot` 保留与 Orca 配置的稳定映射。
- 单个目标色配方限制为 1～3 个唯一物理槽，权重必须为有限正数且归一化；六个物理通道可以在模型的不同目标色配方中共同参与。
- 新字段追加在 `PrintablePaletteSnapshot` 的四个旧字段之后，因此旧四字段聚合初始化仍可编译；迁移期由 `rebuild_legacy_projection()` 从类型化通道生成旧视图。
- 颜色意图引用是可选值，包含本地路径、schema 和 SHA-256；这为后续 `color-intent.v1.json` 留出加法式升级点，不改变现有 OBJ 制品默认行为。

## 2026-09-03：Orca 色板能力映射

- Orca 用 `filament_is_mixed` 区分虚拟槽，`filament_mixed_components` 保存一基配置槽编号，`filament_mixed_sublayer_ratios` 保存可归一化的比例；适配器转换后对外统一为零基物理槽。
- 六通道限制只施加在模型生成类型化能力视图：按稳定项目顺序选择前六个有效物理槽，虚拟混色槽不占名额，项目本身的其他槽和配置不被删除或改写。
- 物理耗材的材料、喷嘴温度和允许范围完整时，沿用现有最大兼容子集算法；元数据不完整时仍保留旧离散色投影，但不宣称可以创建新的过程混色配方。
- 已有虚拟配方只有在颜色、分量数、比例以及全部物理引用均有效且兼容时才进入 `mixed_recipes`；引用第七个被能力边界排除的物理槽会被明确过滤。
- 旧手动匹配依赖包含全部槽位的 `project_colors`，因此适配器保留该原始投影；自动/类型化消费者改用物理 `compatible_slots`，避免把虚拟槽误算为六个物理通道之一。

## 2026-09-03：Python 1～6 色动态化

- Python 通用颜色角色按数量取稳定前缀：`primary`、`structure`、`light`、`accent`、`secondary`、`detail`；1～4 色保留原角色语义，5/6 色只做加法扩展。
- 推荐接口新增可选 `palette_color_count`，范围为 1～6；旧客户端省略该字段时仍按四色处理，旧任务状态缺字段时按已有色板长度推断，否则回退四色。
- 文本/图片推荐、预处理提示、精确色板图像管线和视觉质检都使用实际请求数量；sidecar 健康能力同时公布最小值 1、最大值 6 和默认值 4。
- 用户确认推荐色后，实际 `palette` 可以少于最初推荐数量；任务保留原 `palette_color_count`，以便推荐结果持久化和重载校验不发生语义漂移。
- 人像四视图优化仍明确限定 `len(job.palette) == 4`，因此其中的四色提示属于专用能力，不影响 1、2、3、5、6 色通用生成；历史基准和已批准候选同样保持固定四色以保证结果可比。
- `four-color-preview` 等既有内部文件名保留，避免破坏旧任务目录与下载契约；文件内容和通用元数据已支持 1～6 色。

## 2026-09-03：C++ / wxWidgets 1～6 色动态化

- C++ 与 Python 现在共用同一目标色角色前缀语义：`primary`、`structure`、`light`、`accent`、`secondary`、`detail`；旧四色映射和默认数量 4 保持不变。
- 目标色数量与物理进料通道数量拥有独立常量和校验函数；两者当前都允许 1～6，但不会因当前数值相同而在后续叠色清单中混为一个概念。
- `AIModelGenerationClient` 在文本 JSON 与图片 multipart 请求中都发送 `palette_color_count`，并只接受 1～6 的响应值；旧 sidecar 或旧任务缺字段时仍回退四色。
- 模型生成页为 AI 推荐增加 1～6 色数量选择，推荐任务快照和恢复状态包含该数量；用户在等待确认时改变数量会发起新推荐，不会误用旧结果。
- AI 推荐后的手动补色以上述目标色数量为上限；用户可先删除再补色或直接替换，避免“请求 3 色、确认 5 色”造成任务元数据和角色数不一致。
- 推荐卡、角色选择、模型材料选择和局部改色控件都按六通道容量构造；局部改色改为三列网格，避免 5/6 色在高 DPI 下横向挤压。
- 纯色彩角色分配和进度映射已从 wx 依赖文件拆到独立 presentation core，使 Catch2 可以直接覆盖 1～6 色，同时让 `ModelGenerationPanel.cpp` 保持在 5200 行架构预算内。
- 本批没有修改 SmartSlicing、切片触发、打印配置或 3MF/profile；模型导入边界守卫继续通过。

## 2026-09-03：形体参考与打印配色解耦复核

- 用户确认采用“结构先约束、精确颜色后收敛”的模型生成路线，并要求继续覆盖 1～6 色；智能切片仍由独立分支负责。
- 当前 sidecar 已区分 `style-preview-raw.png`、`geometry-reference.png`、`preview.png`，OBJ 后处理也已具备纹理烘焙、色板量化、肖像材质投影、微小色岛合并和边界规整基础。
- 当前分离仍不彻底：`preprocess_image()` 把目标色板传入 Image2 风格提示，写实路径又把同一张结果直接复制成 `geometry-reference.png`。因此几何输入依然受到大色块、禁渐变和精确耗材色方向影响。
- `_portrait_monochrome_geometry_prompt()` 已定义但没有调用点；界面关于“单色雕塑建模参考”的说明与一次 Image2 请求复用同图的实际行为不一致。
- 写实身份优先几何选择仍由 `len(job.palette) == 4` 门控；解除时不能只删除条件，还要把四色专用多视图、角色和旧任务恢复假设逐项隔离。
- 推荐的一次调用方案是：Image2 生成连续色调、低纹理、平光、透明背景的打印友好造型稿；确定性的图片管线再从同一原始结果生成精确 1～6 色材质预览。这样不增加图片调用次数。
- 模型生成应同时发布精确 1～6 色 OBJ 回退和连续目标色/语义区域意图。前者保证当前 Orca 可用，后者避免提前丢失未来 CMYK/六通道叠色求解所需的信息。
- 艺术风格按“雕塑写实 → 肖像速写 → 版画式水墨/浮雕水墨”推进；传统透明晕染和微小点彩不适合作为普通 FDM 默认表达。
- `.planning/.active_plan` 仍指向旧的 500 图质量任务；会话恢复脚本返回更早的 PPT 上下文。当前工作以根目录计划、当前 Git 分支和已提交 v2 文档为准，不执行陈旧恢复内容。
- `GeneratedModelArtifact` 已有可选 `ColorIntentManifestRef`，但 sidecar `Job`、持久化状态、公开状态和下载路由尚无实际清单路径；C++ 客户端也只下载模型制品，因此当前契约还没有运行时生产者或消费者。
- sidecar 当前只在模型通过几何与视觉处理后设置 `artifact_path` 并进入 `ready`。清单应在同一原子完成边界之前生成并校验，避免公开“模型已就绪但颜色意图缺失”的半完成状态。
- 客户端现有通用下载函数可以复用，但应增加受限的 `color-intent` 下载入口、大小限制、schema/hash 状态解析，并在导入前校验 SHA-256；旧 sidecar 缺少清单时继续走当前兼容路径。
- 形体/配色解耦的最小红测入口是 `tools/ai/test_openai_preprocessor.py` 和 `tools/ai/test_printable_sidecar_pipeline.py`；身份几何选择与旧任务恢复覆盖集中在 `tools/ai/test_obj_generation.py`。
- C++ 契约测试已验证 `ColorIntentManifestRef` 值类型，但没有覆盖 sidecar 状态解析和下载挂接；运行时交接需要在客户端/面板生产链路补测试或抽出无 wx 的纯解析/校验助手。
- `preprocess_text()`、文本生图提示和最终 `generation_prompt()` 也会注入精确色板；只改图片编辑提示仍会让文字生图/图生 3D 请求受到早期限色，三处需要统一采用“连续色调造型参考、色板仅供后处理”的策略。
- `_portrait_face_lock_mask()` 在所有非空色板的写实图片任务中都会运行，并留下稳定的遮罩文件；它可以作为与色板数量无关的真人肖像证据。人像材质清理仍可继续依赖肤色/衣料角色，不必为 1～2 色强行启用四色专用清理。
- 身份几何路径和肖像材质清理应拆开门控：前者依据写实质量模式、1～6 色和人脸证据；后者依据可用的皮肤/衣料语义证据。这样能推广几何保真而不误用四色材质算法。
- `model_job_support.generation_prompt()` 是提交给 3D provider 的最后一层提示，目前无条件在非空色板时追加精确 RGB。它需要显式的 `constrain_palette` 参数；有造型图片时传 `False`，无图片的文本直出兼容路径仍保留原行为。
- `ModelGenerationPanel.cpp` 已处于 5200 行架构预算，清单下载不能靠大段内联状态机堆入面板。优先复用客户端的并行下载集合并将校验放入客户端/纯助手；面板只保存状态、路径并在导入制品中挂接引用。
- v1 清单可用精确预览像素作为区域标签，从同位置的连续色调 raw 图计算每个回退色的代表 `desired_color`；最终 OBJ 顶点色继续作为区域 ID。这样不复制网格拓扑，也能保留叠色求解所需的连续目标色。
- 运行时现已按上述双轨实现：付费 3D 输入只使用连续色调造型参考，精确色板只用于本地材质草图与 OBJ 顶点区域；文本和图片链路都不会再把所选耗材 RGB 写回几何提示。
- `color-intent.v1.json` 以 OBJ、连续外观源、材质草图和可选几何参考的 SHA-256 固定身份；每个 1～6 色目标同时保存语义角色、连续目标色和直接耗材回退色，不包含 CMYK/六通道层叠配方。
- 原生客户端只接受 64 KiB 以内、schema 精确匹配且 SHA 为小写 64 位的清单；下载落盘前再次校验完整 JSON、清单响应哈希及其绑定的本地 OBJ 哈希。旧任务无清单时仍走原 OBJ 兼容路径。
- 模型库元数据升级为加法式 schema 6，保存清单相对路径/schema/hash；历史模型导入前会重新校验，局部改色生成新 OBJ 时主动清除旧清单引用，避免哈希错配。
- 为避免继续堆大面板，模型/清单下载与完成状态机已迁到 `ModelGenerationArtifactFlow.cpp`；主面板降到 5172 行，仍低于 5200 行预算。

## 2026-09-03：打印原生艺术预设

- “肖像速写”不是先把照片压成少数平面色再建模，而是在连续色调形体参考中保留身份、脸型、五官间距和不对称，再把材质表达收敛为 2～5 个有意义的大体块与少量打印宽度线条。
- 肖像速写复用写实质量模式的身份优先几何门控和人脸锁定证据，兼容 1～6 色；四色专用多视图和材质清理没有被错误推广。
- “水墨版画浮雕”采用实体背板、浅层级凹凸和正负形构图，把笔触转成相连的压印/雕刻线；明确排除透明水洗、灰雾、微网点、细密排线和漂浮墨点。
- 两种预设的“少色”是打印结构约束，不要求把用户选择的每个颜色都塞进模型；允许使用 1～6 色中的语义真实子集，避免微小色岛和错误区域。
- 本地推荐器现在优先将人像推荐为 `portrait_sketch`，将 Logo、平面图形及透明/光效题材推荐为 `ink_relief`；推荐结果仍提供可回退的传统风格。
- 样式 ID 已贯通 Python 提示词、sidecar 健康契约、C++ 客户端、原生 UI 映射和质量基准；本批没有增加 CMYK 配方、切片触发、3MF/profile 或硬件逻辑。
