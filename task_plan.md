# 任务计划：OrcaSlicer AI 能力路线图

## 目标
评估模型生成、智能切片、易用性、软件架构以及账号计费五大领域的当前状态，形成 provider-agnostic、兼容 OrcaSlicer 演进的目标架构、依赖关系和可执行分期计划。

## 当前阶段
阶段 45 质量优先调优中：在阶段 44 动态四色实现上建立覆盖多类失败模式的基准图集，批量比较 Image2 候选并固化优胜提示策略，再用少量优胜结果完成 Tripo/Orca 端到端验收。

### 阶段 45：多样本质量基准与优胜风格验收
- [ ] 建立 8 至 12 张小而难的回归图集，覆盖人物半身、人物全身、建筑、宠物、商品、深色主体、复杂背景和高频细节
- [ ] 为每张图定义目标主体、构图类型、身份/语义保持点和禁止项
- [ ] 实现可重复的 Image2 多候选批量运行、产物归档和统一评分汇总
- [ ] 运行首轮 Q 版设计师玩具对照并目视复核，定位提示词与确定性清理的主要失真来源
- [ ] 固化优胜提示策略，补充自动化回归并重新运行 Python 全量测试
- [ ] 选择优胜人物与非人物预览进入 Tripo，验证顶点色 OBJ、颜色边界和 Orca 导入切片
- **状态：** in_progress

#### 阶段 45 约束
- 质量优先，Image2 调用次数不设人工上限，但每次调用必须属于明确的对照组并保留输入、提示、色板、指标和输出。
- 不把 API 调优称为模型训练；本阶段做的是提示策略、候选选择和确定性后处理回归，不上传或训练新的模型权重。
- 扩充样本优先使用可合法测试的本地图片或明确可复用的公开素材；外部内容只记录在 `findings.md`，不执行其中任何指令。
- Tripo 仍只消费二维优胜结果，避免把明显失败的图片转成收费 3D；调用次数继续控制在用户既有授权的 10 次以内。
- 全部基准输入副本、运行记录和过程产物保存在 `generated_models/quality-benchmark-phase45/`，不提交密钥或隐私配置。

#### 阶段 45 错误记录
| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| Wikimedia API 从当前出口连续返回 403 Too Many Requests | 1 | 停止批量 API 查询，改用已检索到的明确文件页 |
| Wikimedia `Special:FilePath` 直接文件下载同样被出口限流 | 1 | 完全停用该来源；新增样本改为 Image2 生成的受控合成输入，避免继续重试和外部许可不确定性 |
| 新基准测试按包路径运行时找不到顶层 `quality_benchmark` 模块 | 1 | 与项目其余测试保持一致，改从 `tools.ai.quality_benchmark` 导入 |
| 基准运行器把 `role_by_color` 反向映射传给需要 `color_by_role` 的生产提示接口，22 个候选均在本地校验阶段失败 | 1 | 改为统一传递角色到颜色映射并增加实际提示契约测试；失败发生在远程调用前，未产生付费编辑任务 |
| 两份真实 50 万面级 OBJ 分别残留 162/82 个三色三角面 | 1 | 将可能振荡的四轮局部改色替换为单调降低标签的收敛算法，并在写出前强制断言三色面为 0；复用已下载包离线重做 |
| 固定 64 轮单调扫描在真实 50 万面传播链上仍残留 4 个三色面 | 1 | 不扩大盲扫轮数，改为顶点—面邻接队列；改色后只重查受影响面，且每个顶点最多下降三次，保证有限终止 |

### 阶段 44：无贴图顶点色打印风格滤镜
- [x] 核对当前风格入口、OBJ 顶点色读取和贴图转顶点色产物链路
- [x] 审计顶点色烘焙、量化、修复和减面过程中的颜色边界损失风险
- [x] 确认第一批统一为最多 4 种实际耗材色，并要求适配不同打印机/项目的动态四色
- [x] 冻结首批滤镜的几何、动态配色角色和适用对象契约
- [x] 设计 provider-agnostic 风格预设，并将图片提示与顶点色后处理解耦
- [x] 增加顶点色专用质量指标：单双/三色三角形、色板用量与颜色连通性
- [ ] 用固定三图完成小批量 Image2 对照，只让胜出结果进入最多 3 次 Tripo 验证
- [ ] 在正式 Orca 中验证无 MTL/贴图依赖的 OBJ 显示、颜色映射、修复与切片保持
- **状态：** in_progress

#### 阶段 44 当前验证结果
- 正式 GUI 已显示 5 种中文风格和 4 个动态感知角色；角色交换保持一一对应，当前项目自动色板最多取 4 个兼容槽位。
- 真实 `方飞总.png` 完成 2 次 Image2 调用、0 次 Tripo：赛璐璐结果验证动态角色传输，Q 版结果验证默认半身玩具、稳定底座与四色材料语义。
- 对真实 Image2 产物增加“原始边界背景先分割、主体后量化”，解决服务返回不透明棋盘格时白色脸部/服装被当背景吞掉的问题；同一缓存原图重跑后主体面积 31.24%、最大连通占比 97.85%、3 种有效主体耗材色，质量门通过。
- Python AI 全量回归 118/118、`git diff --check`、GUI 静态库和 Windows Release DLL 链接均通过；运行目录 DLL SHA256 为 `834A10936E7C0E8A2641F27AFC4F0155055769CFE8FA09EE992BCD94604A176E`。

#### 阶段 44 错误记录
| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 全量 CMake 构建误触发大范围 `libslic3r` 重编并因 Windows 页面文件不足失败 | 1 | 使用 `/m:1` 且关闭项目依赖重编，定向构建 `libslic3r_gui` 后链接正式 DLL |
| 正好 4 色的预览被统一显示为“配色不足” | 1 | 区分颜色数量、主体面积和主体连通性原因，并把合法小型独立细节容差调整为主连通占比至少 90% |
| Image2 以 RGB 棋盘格模拟透明背景，白色耗材主体被后置量化背景吞掉 | 2 | 在固定色板量化前从原始图边界识别并洪泛背景；清理预览与 3D 参考图保留真实透明通道，GUI 用棋盘格展示透明区域 |

#### 阶段 44 初步候选
| 风格 | 视觉/几何策略 | 顶点色适配度 | 第一批建议 |
|---|---|---:|---|
| 纯色搪胶玩具 | 圆润大体块，按头发/皮肤/服装/底座分大色区 | 高 | 保留并强化现有 `q_cartoon` |
| 低多边形拼色 | 大平面切面，每个面或面簇使用单一耗材色 | 高，但必须处理共享顶点边界 | 新增 MVP |
| 赛璐璐色块 | 每个语义部件使用主色与一档阴影，不使用细黑描边 | 中高 | 新增 MVP |
| 釉彩嵌色摆件 | 光滑主体、少量大色块；颜色边界由浅浮雕沟槽或结构分区承载 | 高 | 新增 MVP |
| 双色雕塑 | 主体色 + 强调色，牺牲颜色丰富度换取最高稳定性 | 很高 | 作为稳健兜底 |

#### 阶段 44 约束
- 最终验收对象是无贴图依赖的 `model-vertex-color.obj`，图片预览好看但丢弃贴图后失真的方案不算通过。
- 禁用细线、微小文字、噪点、材质纹理、透明/金属高光和依赖渐变的视觉效果；色彩必须依靠足够大的实体色区成立。
- 优先采用“风格预设 + 确定性顶点色后处理”，不把稳定性完全寄托在 Image2/Tripo 提示词上。
- 第一轮只做方案和离线审计；真实验证允许付费 Image2，Tripo 总调用保持在用户授权的 10 次以内，阶段 44 目标不超过 3 次。
- 继续使用阶段 43 的半身人物、全身人物和建筑三图固定回归集；全部过程产物写入 `generated_models/`。
- 最多 4 色是数量契约，不是固定红绿蓝白；默认色板必须来自当前项目中温度兼容的真实耗材槽，手动色板也必须限制为 1 至 4 个唯一颜色。
- 任务创建时冻结生成色板与槽位快照；当前打印机、材料或颜色变化后，已有二维预览不得直接继续生成 3D，必须重新配色或重新生成预览。
- 历史模型保留其生成时色板，不静默重写顶点色；导入到另一组四耗材时使用 Orca 的手动匹配或显式“按当前耗材重新配色”。

#### 阶段 44 动态四色契约草案
1. **真实来源：** `IPrintablePaletteProvider` 返回当前项目槽位、HEX、材料类型和温度兼容结果；自动模式最多选择 4 个兼容槽位。
2. **感知角色：** 不再使用固定“红/绿/蓝/白”角色，按实际色板计算 `light/background`、`dark/structure`、`primary`、`accent`；颜色不足四种时自动降级而不是虚构颜色。
3. **透明背景：** 3D 参考图优先使用透明或独立的中性背景，背景不占用四种模型耗材色，避免没有白色耗材时拿主体色充当背景。
4. **任务冻结：** Sidecar 继续只消费 provider-agnostic 的规范化颜色列表，同时任务元数据保存槽位快照和角色分配，便于恢复、审计和跨打印机重新映射。
5. **历史兼容：** 旧任务只有 HEX 色板时仍可正常读取；新字段为可选扩展，不修改既有 OBJ/3MF 格式。
6. **导入策略：** 同一色板可直接/自动匹配；目标打印机颜色不同则默认弹出原生手动匹配，不把生成色板覆盖到项目耗材配置。

#### 阶段 44 验收指标草案
- OBJ 的每个有效顶点色必须命中目标色板；最终产物不依赖 `.mtl`、PNG/JPG 或 UV 贴图。
- 保持共享顶点和流体拓扑；Orca 用双色三角形编码真实的面内多耗材边界，因此双色面允许存在。三色三角形目标为 `0`，双色边界面比例和小色岛受控并写入质量报告。
- 清除低于可打印最小特征面积的颜色小岛，且关键五官、服装、建筑轮廓不得被清理掉。
- 修复、减面和 Orca 导入前后，主连通体、面数级别、颜色数量及主要色区面积保持在可解释容差内。
- 正式 Orca 中能看到预期多色，打印机耗材映射不退化为单色，颜色修复后不丢失。

### 阶段 43：设计师玩具效果调优
- [x] 精确关闭项目当前 Orca 验证实例，不影响 Sidecar 和其他服务
- [x] 盘点并目视检查 `Camera Roll` 三张固定样图，划分人物半身、人物全身、建筑多主体三类难点
- [x] 审视现有 Image2 提示、四色确定性管线与阶段 41 质量门禁
- [x] 形成“基线—构图/风格—身份/语义—四色打印”分层调优方案和验收记分卡
- [x] 用户确认半身参考图默认生成带稳定底座的半身设计师玩具，不补全未知下肢
- [x] 按确认方案创建真实 Image2 基线与候选结果，逐轮对照并固化回归用例
- [x] 将胜出策略接入正式 Sidecar，完成 111 项 Python 回归并重启正式服务；本轮无 C++ 变化，不需要重编 DLL
- **状态：** complete

#### 阶段 43 约束
- 三张样图固定作为回归集，不在迭代中途更换，避免凭主观印象判断提示改动。
- 先评价 AI 原图的主体、构图和风格，再评价严格色板与清理图；不让量化算法掩盖上游生成问题。
- 本轮规划阶段不调用收费 API；实施后 Image2 采用小批量受控迭代，胜出前不创建 Tripo 任务。
- 所有生成结果、评分表和截图写入项目 `generated_models/`，不提交真实生成资产或密钥。
- Windows 正式流程优先；保留工作树中其他未提交修改，不重置或覆盖用户文件。

#### 阶段 43 固定回归集
| 样图 | 核心难点 | 主要验收点 |
|---|---|---|
| `方飞总.png` | 半身肖像、双臂交叠、原图无完整下肢 | 身份与职业气质保持；形成自洽单体玩具构图；手臂不得粘连或重复 |
| `刘亦菲.jpg` | 全身人物、长发、细密服装纹理、细手脚 | 人脸可辨；比例适度 Q 化；发丝和纹理简化成可建模实体块 |
| `大雁塔.jpg` | 佛像、塔、树木和水印色卡并存 | 明确单一主角；剥离背景与底部水印；保留建筑/佛像关键轮廓且不混成一团 |

#### 阶段 43 调优轮次
1. **基线冻结：** 每张图用当前正式参数生成一次，保存 AI 原图、严格色板、清理图、热力图和元数据。
2. **构图与玩具风：** 把“原图原地换材质”改成“提取主角并重构为完整商品级设计师玩具参考图”，统一三分之四视角、完整轮廓、简洁背景、稳定底座和大块实体材质。
3. **身份与语义：** 人物按可辨识五官、发型、服装轮廓和姿态控制夸张度；建筑图显式选择单一主角，禁止复制佛像、树木、水印和摄影 UI。
4. **四色打印：** 在 AI 原图合格后，再按语义区域分配 3 至 4 种耗材色，收敛小区域、边界复杂度和背景误映射。
5. **正式回归：** 将胜出策略接入 Sidecar，跑自动测试、正式 Orca 多图 GUI 回归，并只对最终 1 至 2 个胜出预览进入 3D 验证。

#### 阶段 43 错误记录
| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| PowerShell 默认编码读取 UTF-8 中文规划文件时显示乱码 | 1 | 确认磁盘文件未损坏，后续显式使用 `Get-Content -Encoding UTF8`，不做编码重写 |
| 新提示契约用例仍使用默认雕塑风格，却断言 Q 版 70/30 比例 | 1 | 将该用例显式传入正式 GUI 默认的 `q_cartoon`，保留各风格独立契约测试 |
| PowerShell here-string 向 Python 传递中文样图路径后，上传前无法读取输入图 | 1 | 未发起远端请求；先复制为 `generated_models/` 内 ASCII 回归文件名，再使用稳定路径调用 |

### 阶段 42：3D 生成页面渐进式交互
- [x] 审视当前正式界面与状态流，确认首屏信息过载和主操作被滚动隐藏的问题
- [x] 确认“渐进式单页旅程”方案并形成交互设计文档
- [x] 实现固定四步进度、可滚动设置区和固定主操作区
- [x] 按阶段显示模型精度、导入颜色和自动切片，并折叠高级打印约束
- [x] 完成 Windows Release 复编、链接与正式 GUI 多状态验收
- [x] 更新发现、进度与最终证据
- **状态：** complete

#### 阶段 42 约束
- 不改变 Sidecar、付费确认、任务恢复、模型库和 Orca 导入契约。
- 不创建新的 Image2 或 Tripo 任务；复用现有任务和历史模型验证页面状态。
- Windows 正式版优先，本轮不验证 macOS/Linux。
- 保留工作树中其他未提交改动，不重置或覆盖用户文件。

#### 阶段 42 错误记录
| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 本地历史模型没有远程任务 ID，`refresh_controls()` 将已完成状态误重置为第 1 步 | 1 | 仅在“无任务且未就绪”或输入过期时回到第 1 步；历史模型保持第 4 步和 95% |
| 历史模型进入最终状态后仍显示禁用的“准备 3D 提示词” | 1 | 最终状态隐藏预处理按钮，只保留“导入”主操作和“重新开始”次操作 |
| 验收滚动手势落在色板下拉框上，意外把色板来源改为手动 | 1 | 使用下拉框键盘导航恢复“当前项目”，后续以无滚动的阶段首屏作为验收重点 |
| 一次无截图的可访问性观察缺少点击几何信息 | 1 | 重新获取包含截图与文本的同一时点状态后再执行点击，不复用失效索引 |

### 阶段 41：四色预览质量与即时刷新修复
- [x] 在用户当前 Orca 界面复现预览质量和下拉切换延迟
- [x] 对照 AI 原图、严格色板、清理图、热力图和任务参数定位根因
- [x] 确认四色视觉目标与色彩分配策略
- [x] 修复阶段切换后的画布即时重绘
- [x] 改进四色生图提示与低质量/缺色门禁
- [x] 完成 Python/C++ 回归、Release 构建和真实 GUI 验收
- **状态：** complete

#### 阶段 41 约束
- 不重复使用当前不匹配的“机械麒麟”提示词生成肖像。
- 优先在 Image2 构图阶段形成大块实体色，确定性量化只负责兜底，不把灰度雕塑硬量化成四色成品。
- GUI 选择预览阶段后必须在同一事件内立即更新标签、位图和画布，不依赖缩放或其他按钮。
- 新的真实 Image2 调用用于最终效果验收；不创建 Tripo 任务。

#### 阶段 41 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 选择预览阶段后只有标签更新，位图仍停留在旧阶段 | 1 | 已确认点击缩放后位图才更新；修复方向为阶段事件内主动刷新画布与布局 |
| 灰白雕塑原图量化后只剩蓝白两色 | 1 | 当前风格、输入提示和四色目标冲突；改为四色构图提示并增加色板使用率质量门禁 |
| 提示词定向测试仍断言“可只使用色板子集”和旧阴影措辞 | 1 | 新策略明确要求至少三种有意义颜色；更新两条历史断言，保留其余构图与安全约束检查 |
| `/m:4` 完整 Release 因历史增量记录误重编 libslic3r，并发 PCH 耗尽页面文件 | 1 | 不重复并行全构建；改为 `BuildProjectReferences=false` 的 GUI 单工程串行编译，再单独链接 Orca DLL |
| 32 色自适应聚类把较小绿衣区域和蓝色暗部合并 | 1 | 将自适应聚类下限提高到 64，并增加“红色主体占优时仍保留绿/蓝材料”的回归测试 |
| Image2 第三轮保留灰色影棚背景，最近色映射产生蓝色背景岛 | 1 | 亮中性灰统一归到最亮耗材色；复用已付费原图离线重处理，不增加 3D 调用 |
| GUI 已完成任务不会自动重新下载被离线替换的同名产物 | 1 | 不把运行时热替换当正式流程结论；通过新任务、独立管线产物和阶段切换分别验收，后续任务自动使用最终 Sidecar 代码 |

### 阶段 40：正式 Orca 四色生成流程 GUI 验收
- [x] 核对正式 Sidecar、运行目录 DLL 与最新构建产物，安全部署本轮版本
- [x] 启动正式 Orca 并确认 3D 生成页面、中文打印参数和默认值
- [x] 使用真实文字输入生成 Image2 预览并验证付费确认门禁
- [x] 验证 AI 原图、严格色板、可打印清理、问题热力图四阶段切换和指标
- [x] 验证确认预览后才可生成 3D，并检查跨页面返回后的预览保持
- [x] 保存 GUI、Sidecar 状态与产物证据，记录问题和结论
- **状态：** complete

#### 阶段 40 约束
- 只验证 Windows 正式服务和正式 Orca，不使用 mock。
- 允许真实 Image2 调用；仅在流程验证确有必要时创建一次 Tripo 3D 任务。
- 不覆盖正在被 Orca 使用的 DLL，不强制关闭可能含未保存内容的窗口。
- GUI 操作只使用 computer-use，生成和过程证据写入 `generated_models/gui-validation-phase40/`。

#### 阶段 40 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| PowerShell 再次将 `foreach` 代码块直接接到管道，触发空管道元素解析错误 | 2 | 第二条文件/进程检查命令也误用了同一写法；随后固定先保存为 `$rows` 再单独输出，不再使用代码块直连管道 |
| 一条同时停止和重启 Sidecar 的复合命令被执行策略拒绝 | 1 | 将精确停止旧 PID、启动正式服务和健康检查拆成三个独立动作，最新 Sidecar 在 18764 正常就绪 |
| 隔离 Orca 首次启动在 `Plater.cpp:5211` 发生 `c0000005` | 1 | 崩溃栈和时间戳确认 `Plater.obj` 早于已改动的 `MainFrame.hpp`，属于定向编译遗漏依赖导致的 C++ ABI 混用；改为串行增量编译所有受影响 GUI 翻译单元后重新归档/链接 |
| 只补编 `Plater.cpp` 后，隔离实例越过原崩溃点但在最近项目/打印配置初始化阶段再次访问冲突 | 1 | 构造检查点证明适配器、模型生成页面和预设页已完成；根因仍是更多 GUI 翻译单元沿用旧 `MainFrame.hpp` 布局，改为完整重编 362 个 GUI 对象 |
| 完整 GUI `ClCompile` 两次达到工具 30 分钟等待上限 | 2 | 第一次并发 2 精确停止并确认无损坏对象；第二次并发 4 的 MSBuild/编译器在工具返回后仍健康后台运行，按对象时间戳持续推进，保持该批次自然完成且不再重启 |
| Image2 任务创建成功后首个状态轮询返回 404，GUI 停在 10% | 1 | `/model-jobs/{id}` 被解析为 `status`，但 GET 白名单漏掉该动作；补入 `status` 并新增任务状态契约回归测试，复用原任务完成验证，没有重复付费 |
| 默认 Python 和网站虚拟环境分别缺 pytest、Pillow | 2 | 不修改项目环境；用工作区 Python 提供 Pillow，并通过临时 `PYTHONPATH` 复用网站环境的 pytest，完整 AI 测试 104/104 通过 |

### 阶段 39：四色可打印图像链路正式实现
- [x] 阅读 `Docs/FOUR_COLOR_IMAGE_PIPELINE.md` 并审计现有提示词、量化、Sidecar、GUI 与测试边界
- [x] 抽离固定耗材色板量化、最小特征清理、蒙版、热力图、评分和元数据模块
- [x] 将打印参数、原始 AI 图、严格色板图、清理图和导出产物纳入任务持久化与 API
- [x] 让文字、图片和文字+图片在开启可打印颜色时统一先生成并确认可打印图片
- [x] 在中文 GUI 中提供打印宽度、喷嘴/线宽、最小特征和多阶段预览
- [x] 完成离线回归、真实 Image2 验证、Windows Release 构建和结果审查
- **状态：** complete

#### 阶段 39 约束
- 默认实现实体色块模式；不在本轮引入 HueForge TD、随机抖动、STL/3MF 图像浮雕导出。
- 最终清理图只能包含当前耗材色板中的颜色，禁用可打印颜色时保持既有自然色链路。
- 新算法放在独立模型生成模块中，Orca GUI 只消费稳定契约，不把图像算法写入 `ModelGenerationPanel`。
- 真实 Image2 可用于验证；不创建新的 Tripo 3D 付费任务。
- 所有生成和过程产物继续写入项目 `generated_models/`，不提交真实生成图或密钥。

#### 阶段 39 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 单像素小岛测试在量化前被 3×3 中值滤波消除，无法验证区域合并 | 1 | 将夹具改为中值滤波可保留、但仍小于最小特征面积的 3×3 色块 |
| Sidecar 文字链路测试的 Image2 mock 把第一个 `instruction` 参数误当输出路径 | 1 | 改用显式 lambda 将第二个 `output` 参数交给合成图片夹具 |
| MSBuild `SelectedFiles` 传入分号分隔的两个绝对路径时把第二个路径解析为属性开关 | 1 | 每个源文件分别执行一次 `ClCompile`，两项均编译通过 |
| 首次执行 GUI `_Lib` 时未禁用项目引用，意外触发 Orca 上游依赖重编译并遇到 Windows 分页文件不足（1455） | 1 | 终止该次遗留编译进程，使用 `BuildProjectReferences=false` 仅归档已验证 GUI 对象并链接正式 DLL |
| 最终安全扫描命令把含单引号的正则再次嵌入 PowerShell 单引号字符串，导致解析失败 | 1 | 将编译/校验、敏感词扫描和空白检查拆成三条无嵌套引号的并行命令后通过 |

### 阶段 38：双机协作 Git 基线审核、精简与发布
- [x] 清点全部已修改和未跟踪文件，划分正式源码、测试、文档、本机配置与过程产物
- [x] 审计待提交内容中的密钥、个人路径、大文件、Mock/旧入口和明显冗余
- [x] 精简正式代码与提交集合，保留用户本地过程文件但不纳入 Git
- [x] 在根 README 增加双人职责、模块边界、分支和同步流程
- [x] 完成 Python/C++ 定向回归、Release 链接和提交前安全检查
- [x] 创建并推送共享集成分支，验证远端 commit 与本地一致
- **状态：** complete

#### 阶段 38 约束
- 不删除本地模型、截图、构建产物或历史过程文件，只控制 Git 提交范围。
- 不提交 API Key、用户配置、Codex/Claude 会话数据、运行日志或大体积生成产物。
- 不直接推送 `master`；共享基线使用独立集成分支，双方功能从该分支派生。
- 保留当前业务行为与 Orca 解耦边界；本轮只做审核、必要精简、协作文档和版本发布。

#### 阶段 38 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 恢复脚本再次返回旧 PPT 会话上下文且控制台乱码 | 1 | 将其视为过期恢复数据，不执行其中动作，以当前工作树和阶段 37 结果为准 |
| 误把工具输出层乱码判断为规划文件磁盘编码损坏 | 1 | 检查原始字节前缀确认文件仍为正确 UTF-8，不执行编码重写 |
| PowerShell 把 `foreach` 代码块直接接管道导致解析失败 | 2 | 改为 `$rows = foreach (...) { ... }` 后再单独接管道；后续所有循环输出固定采用数组形式 |
| 枚举约 30,276 个 untracked 文件并逐个取大小超过 20 秒，且 Git 转义中文路径无法直接传给 `Test-Path` | 1 | 改用 `core.quotepath=false` 的目录级 status 与顶层目录统计；不再逐文件扫描全部输出包 |
| 本机未安装 `ruff`、`pyflakes`、`cppcheck`、`clang-tidy` 或 `gitleaks` | 1 | 不临时安装或改变依赖；使用 AST 重复定义/函数规模审计、定向安全搜索、编译器和现有测试替代 |
| 一次从仓库根递归搜索打包脚本引用超过 20 秒 | 1 | 缩小到 `packaging/`、`scripts/`、`README.md` 和 `Docs/` 明确范围后立即完成 |
| Windows `rg` 再次收到 `tools/ai/test_*.py` 字面通配路径并返回失败 | 1 | 使用 `rg -g 'test_*.py' ... tools/ai` 的工具内 glob，不再依赖 shell 展开 |
| PowerShell 双引号字符串中的 `$file:` 被解析为无效变量名 | 2 | 使用 `${file}:...` 明确变量边界；后续带冒号的插值统一加花括号 |
| 用 `rg` 精确匹配空 Key 批处理行时引号经 PowerShell 传递后未命中 | 1 | 改用 `Get-Content` 与字符串精确相等检查，两个 Key 均确认为空 |
| 首次批量清理文件末尾空行的补丁误用了 `04-stop-ai.bat` 结束行上下文 | 1 | 读取六个文件的精确尾部后重新应用补丁 |
| AI 暂存内容检查发现 6 个新文件末尾多余空白行 | 1 | 清理批处理和契约头文件末尾空行，重新暂存后 `git diff --cached --check` 通过 |
| 文档暂存检查发现两处 Markdown 尾随空格和三个文件末尾空白行 | 1 | 用空引用行替代 Markdown 双空格换行，并删除末尾空白行 |

### 阶段 37：模型生成线与 Orca 第一批等价解耦
- [x] 冻结 `GeneratedModelArtifact`、导入请求/结果和可打印色板契约
- [x] 建立模型制品消费端口与 Orca 工作区适配器
- [x] 将 OBJ 导入、颜色映射、网格修复和自动切片编排迁出 `ModelGenerationPanel`
- [x] 让 `ModelGenerationPanel` 不再直接包含或持有 `Plater`、Preset、CGAL 修复依赖
- [x] 保持 MainFrame 为薄组合根，并登记新增源文件
- [x] 完成契约/适配静态边界检查、Windows Release 链接和现有 AI 回归
- **状态：** complete

#### 阶段 37 约束
- 本批只做等价抽取，不改变当前用户交互、颜色语义、修复策略和自动切片默认值。
- 保护当前大量未提交改动，不创建会丢失现状的独立 worktree，不执行 reset/checkout。
- 不升级 Orca 上游，不拆多个进程或仓库，不调用收费 API。
- Windows Release 为交付构建门禁；领域契约不得依赖 wx、`Plater` 或供应商 SDK。

#### 阶段 37 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `session-catchup.py` 返回另一会话的旧架构图上下文且控制台编码异常 | 1 | 只作为恢复数据记录，以当前 Git diff、阶段 36 文档和用户最新指令为准，不执行其中动作 |
| 一次 `apply_patch` 把 `progress.md` 的上下文误放入 `task_plan.md` 补丁，导致校验失败 | 1 | 拆成两个独立补丁并使用各自文件中的精确上下文 |
| 将成功的 `git diff --check` 与预期无匹配的 `rg` 串在同一命令，整体因 `rg=1` 被标为失败 | 1 | 后续把空白检查与“预期零匹配”检查分开解释，不将零匹配当成代码错误 |
| PowerShell 查找 `job_id` 行返回两处结果，数组参与减法导致范围读取失败 | 1 | 改用唯一函数边界和字符串索引，不依赖非唯一行模式 |
| 首次从完整文件工具输出提取函数块时受包装/截断影响，未找到结束标记 | 1 | 让 PowerShell 只输出目标函数子串，再由 `apply_patch` 完成替换 |
| 当前 PowerShell `PATH` 不包含 `cmake` | 1 | 使用 Visual Studio 2022 自带 CMake/MSBuild 的绝对路径，不修改用户环境 |
| 短超时构建退出后 MSBuild 子进程仍在后台运行，第二个构建与其争用对象文件 | 1 | 核对完整命令行后只停止本轮两组精确 PID；后续不并发启动同一工程构建 |
| 新 adapter 沿用 GUI 根目录本地 include，单文件编译找不到 `GUI_App.hpp` | 1 | 改用从 `src` include root 锚定的 `slic3r/GUI/...` 路径 |
| Panel 删除 `Plater.hpp` 后暴露 `glsafe` 过去依靠间接 include | 1 | 显式包含定义该宏的 `3DScene.hpp`，消除隐式依赖 |
| `SelectedFiles` 使用相对路径时 MSBuild 未编译目标文件 | 1 | 使用绝对源码路径分别编译 adapter、Panel 和 MainFrame |
| `/t:Build` 因跟踪状态准备重编 338 个未改动 GUI 文件并超过短验证窗口 | 1 | 停止精确构建 PID，改用 `_Lib` 基于现有 362 个对象归档，再用 `_Link` 完整链接 DLL |
| 两次 PowerShell/`rg` 组合正则引号解析失败 | 2 | 放弃复杂引号表达式，改用简单固定模式查找 MSBuild `_Lib`/`_Link` 目标 |

### 阶段 36：模型生成与智能切片双主线解耦架构
- [x] 盘点当前 AI 代码、Orca 核心改动和跨模块依赖
- [x] 比较可选解耦方案并确定推荐边界
- [x] 设计模型生成、智能切片、Orca 适配层和 sidecar 契约
- [x] 定义两名开发者的目录所有权、接口先行和合并规则
- [x] 制定上游 Orca 版本同步、兼容测试和迁移路线
- [x] 输出架构设计文档与关键 ADR
- **状态：** completed

#### 阶段 36 约束
- 当前只规划“模型生成”和“智能切片”两条主线；交互重构、账号、计费暂不进入实现范围。
- 保持 `.3mf`、打印机配置和 Orca 原生切片行为向后兼容。
- 优先减少对 Orca 高频变更文件的直接修改，确保后续可持续同步上游版本。
- 本阶段只做代码审计和架构文档，不改业务行为。

#### 阶段 36 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| PowerShell 将 `foreach` 代码块直接接管道时报空管道元素 | 1 | 先把结果收集到数组，再统一 `Format-Table` 输出 |

### 阶段 35：正式模型生成任务持久化与重启恢复
- [x] 审计正式 sidecar、GUI、模型库和 Windows 打包入口的恢复断点
- [x] 原子保存文字、参考图、风格预览、色板、质量档、任务状态和 Tripo task ID
- [x] sidecar 启动时恢复等待确认、生成中与已完成任务
- [x] 恢复生成中任务时复用已保存 generation/conversion ID，禁止自动重复付费
- [x] 新增最近任务接口，生成页自动恢复输入、预览和最终 OBJ
- [x] 删除任务时移除任务清单但保留用户生成产物
- [x] 完成 AI 全量回归、`git diff --check` 和 Windows Release 构建
- [x] 使用隔离正式 sidecar 执行一次收费 3D 生成，并验证两次重启均复用原 generation/conversion ID
- [x] 验收恢复后的真实 OBJ：95,338 面、2 个连通部件、0 个异常边、顶点色有效
- [x] 在当前未保存 Orca 正常关闭后部署正式 sidecar/DLL，并完成正式 GUI 恢复验收
- **3D 付费预算：** 1/10（本阶段新增；无需逐次确认）
- **状态：** complete

#### 阶段 35 约束
- Windows 正式版优先，macOS/Linux 本轮不处理。
- 不使用 mock 作为功能或 GUI 验收结论。
- 模糊状态（远端 task ID 尚未可靠落盘）不得自动创建第二个付费任务，必须由用户重新发起。
- 任务清单和所有过程/最终产物继续保存在项目 `generated_models/`。

#### 阶段 35 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 大范围并行状态扫描超过工具等待时间 | 1 | 缩小到正式链路入口、sidecar 状态机与定向测试分段审计 |
| 当前 PowerShell `PATH` 不含 `python.exe` | 1 | 使用 Codex 工作区依赖中的 Python 绝对路径运行回归，不安装新依赖 |
| `_download_conversion` 新增默认参数导致两个旧 mock 断言失败 | 2 | 非恢复路径保持原四参数调用；只有恢复路径显式传入 `resume=true`，80 项回归全过 |
| 恢复图片时原图和 AI 预览争用单一 HTTP 请求句柄 | 1 | 串行恢复原图，再下载风格预览；原图失败时直接继续预览恢复 |
| Windows Release 构建超过工具短等待窗口 | 3 | 不重复构建，持续跟踪同一 `cmake/MSBuild/cl/link` 进程直到链接完成 |
| 本机代理把 Tripo 官方 CDN 解析为保留地址，安全校验误判产物 URL | 1 | 仅精确允许 HTTPS 官方主机 `openapi.cdn.tripo3d.com`，继续拒绝相似域名、凭据 URL 和 HTTP，并增加安全回归 |
| 10 万面档实际返回 95,338 面，被旧的精确下限误判失败 | 1 | 按每个所选档位独立执行 90%–125% 容差，不设置跨档位统一最低面数 |
| 恢复下载向已有 `package` 目录重复解压，返回无效 OBJ 包 | 1 | 优先复用并重新校验本地完整 OBJ；损坏时改用新的 `recovery-XX` 工作目录，保留原过程文件 |
| 停止旧正式 sidecar 后，另一遗留启动器立即接管 18764 | 1 | 枚举并核对端口子进程与项目批处理父进程，只精确停止确认属于本项目的 PID 对 |
| Orca 在 sidecar 重建索引前缓存了旧 job ID | 1 | 确认新窗口无未保存内容后正常重启 Orca，使生成面板在初始化时读取新的最新任务 |

### 阶段 34：Windows AI 同事测试包

### 阶段 34：Windows AI 同事测试包
- [x] 核对正式 Orca 运行时、AI sidecar、Python/Pillow 与外部 API 环境变量边界
- [x] 生成不含真实密钥的交互式 AI 配置、环境检查、启动和停止脚本
- [x] 排除旧 DLL、Mock、测试脚本、付费验证脚本、历史模型和开发目录
- [x] 组装并压缩 Windows x64 测试包，生成 SHA256 校验文件
- [x] 验证 DLL 哈希、包内容、批处理 CRLF、Python/Pillow 和隔离 sidecar `/health`
- [x] 确认验证期间未创建图片或 Tripo 付费任务
- **状态：** complete

#### 阶段 34 交付
- `output/packages/OrcaSlicer-AI-Windows-x64-20260813-demo3-simple-config.zip`
- SHA256：`3E9DFCDAF875D43524D5DD99F1DEF24093B1EB63063DDEBB49D72B784E655EAB`
- 可复用打包入口：`scripts/package_windows_ai_test.ps1`
- 同事只需编辑 `setup/ai-config.bat` 中的两个 API Key；ZIP 解压后无额外同名内层目录。

#### 阶段 34 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| Windows `Compress-Archive` 读取资源 SVG 时遇到短暂文件占用 | 1 | 不重复同一后端，改用系统 `tar.exe -a` 创建 ZIP，并将稳定路径固化到打包脚本 |
| Windows PowerShell 5 将无 BOM 中文 `.ps1` 按系统代码页解析 | 1 | PowerShell 交互提示改为 ASCII 英文，中文使用说明保留在 UTF-8 文本中 |
| 首版环境检查把启动器已有默认值误判为必填 | 1 | 检查脚本与正式启动器共用相同的 Base URL 和模型默认值，只要求两个 API Key |

阶段 33 代码、构建和部署已完成；真实耗材颜色最终端到端验收仍应在下一轮演示彩排中复核。

### 阶段 33：导入颜色与真实耗材匹配修复
- [x] 根据截图定位“显示已映射但模型/打印机颜色不一致”的根因
- [x] 将导入颜色策略改为手动匹配、自动匹配和单色三个显式选项
- [x] 正常导入恢复 Orca 原生颜色匹配窗口并校验用户确认结果
- [x] 禁止自然色导入覆盖打印机当前耗材色块
- [x] 取消或映射退化时停止自动切片并显示准确状态
- [x] 完成 Windows Release 构建
- [ ] 当前未保存的正式 Orca 关闭后部署，并用真实 OBJ 完成 GUI 验证
- **状态：** in_progress（等待安全部署与 GUI 验证）

#### 阶段 33 约束
- “使用可打印颜色”只决定 AI 图片/模型生成约束，不再隐式决定 OBJ 导入策略。
- 打印机耗材色块始终代表当前真实配置，不得被模型自然色静默覆盖。
- 不创建新的图片或 Tripo 付费任务，复用项目内已有真实彩色 OBJ 验证。

#### 阶段 33 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 首次构建调用超过工具短等待窗口，无法取得最终退出码 | 1 | 确认残留构建进程退出后重新运行同一单并发目标，取得明确成功输出 |
| 当前 Windows 构建树没有 `libslic3r_tests` 项目且 `ctest -N` 为 0 项 | 1 | 不重复错误目标；以完整 Release 编译、链接和静态分支检查作为本轮验证，记录测试配置缺口 |
| 隔离运行副本被 Orca 实例检查拒绝启动；首次还误用了不存在的 `--single-instance-off` | 2 | 核对源码后改用官方 `--no-single-instance` 仍在初始化前退出；不继续改共享配置，不操作未保存的正式窗口，待用户正常关闭后部署 |

### 阶段 32：可选导入后自动切片
- [x] 明确开关位置、默认行为与手动处理门禁
- [x] 在 3D 生成页增加默认开启的“导入后自动切片”开关
- [x] 关闭时保留导入、颜色映射、网格检查/修复与自动摆放，并停在准备页
- [x] 让生成结果与模型库导入共同遵循该开关
- [x] 完成 Windows Release 构建与两条分支验证
- **状态：** complete

### 阶段 31：正常导入颜色保真修复
- [x] 复现并区分单对象结构与单耗材颜色问题
- [x] 定位正常导入错误复用温度兼容耗材槽映射
- [x] 将正常导入改为全部有效项目颜色映射，严格可打印模式继续使用兼容槽位
- [x] 检测多色源退化为单色并进入手动上色，不再误报保色成功
- [x] 同步 OBJ 主色到项目耗材色块并完成 Windows Release、真实 OBJ GUI 复验
- **状态：** complete

### 阶段 29：高精度生成、OBJ 结构分组与颜色保真
- [x] 核对低模参数、20,000 面门禁和现有顶点色 OBJ
- [x] 确认 10 万/30 万/50 万/100 万四档，默认 30 万面
- [x] 撤销单对象/单连通体要求，确认保留对象与材质分组
- [x] 输出高精度 OBJ 颜色与分组设计
- [x] 实现 GUI 质量选择与请求契约
- [x] 实现 Tripo 高质量生成参数和 100 万面处理上限
- [x] 保留对象/组/材质边界并提高自然色烘焙精度
- [x] 补充 Python/C++ 回归并完成 Windows Release 构建
- [x] 将付费 Tripo 端到端验收转入阶段 30 的完整用户流程
- **状态：** complete

### 阶段 30：质量优先的付费黄金路径验收
- [x] 关闭打印耗材颜色约束，使用同一参考图生成 Q 版卡通、低多边形、雕塑三种真实风格预览
- [x] 确保三种预览不新增人物、物件、文字或背景装饰，并通过完整主体与可成模门禁
- [x] 选择最佳预览生成 30 万面彩色 OBJ，必要时升级 50 万或 100 万面，总计不超过 20 次 Tripo 生成
- [x] 验证 OBJ 颜色、对象/材质结构、流体性、非流体边和 Orca 导入结果
- [x] 在准备/预览页面实现长期可见的自动流程状态组件
- [x] 按用户流程完成自动导入、摆盘、切片和 G-code 预览
- [x] 对比风格预览、3D 渲染和最终切片结果，记录差异并针对主要偏差迭代
- [x] 完成 Python/C++ 回归、Windows Release 构建和 GUI 截图验收
- **付费 3D 预算：** 1/20（硬上限 20）
- **状态：** complete

#### 阶段 30 当前验证
- Q 版 30 万面 OBJ 离线检查通过：296,642 面、单连通封闭流体、0 边界边、0 非流体边、0 退化面，并携带自然色顶点 RGB。
- 长期侧栏已接入 AI 导入、网格检查/修复、颜色处理、自动摆放、切片和 G-code 六步状态；普通切片不会覆盖已完成状态。
- Windows Release 单编译器构建与 AI Python 75/75 回归已通过；新 DLL 已部署并由正式 Orca 加载，production sidecar v4 健康。
- 全部 296,642 个三角面的正面顶点色渲染证明实际 OBJ 与 Q 版风格图在人物、姿态和主色块上高度一致。
- 正式 GUI 已从模型库双击加载 `d960d74e...`，显示 296,642 面、22 个颜色组和 40.0 x 43.2 x 100.0 mm；正常导入保留自然颜色且不再弹出 OBJ 颜色确认框。
- 空项目单模型干净复跑六步最终全绿，G-code 导出为 38,127,361 字节；日志确认当前打印仅 1 个对象、`0 -> 1`、`on_process_completed:finished`、完成后 `invalidated=0`，且没有刀路冲突、`toolpath_outside`、新的 `1 -> 0` 或崩溃日志。
- 最终 Windows Release DLL 已部署，构建与运行目录 SHA256 均为 `9D0582BB678161FAB3210DBF803FE8FA2E89264161EB938036C8910D62729BD3`；AI Python 75/75、C++ 顶点色 5 个断言、`py_compile` 和 `git diff --check` 通过。

#### 阶段 30 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 当前 Codex PowerShell 的 `PATH` 未包含 `cmake.exe` | 1 | 使用 Visual Studio 2022 Community 自带 CMake 的绝对路径继续构建 |
| 并行构建曾触发 `C1060` 编译器堆空间不足 | 1 | 使用 `/m:1`、`CL_MPCount=1`、`UseMultiToolTask=false` 和 `BuildInParallel=false` 完成两个 Release 目标 |
| 日常运行 DLL 被响应中的 Orca PID 61376 占用 | 1 | 不强制关闭可能含未保存内容的窗口；保留新构建产物，待进程正常关闭后同步并验收 |
| 组合 `Start-Process` sidecar/Orca 命令被本机策略拦截 | 1 | 未启动任何进程；改用固定 sidecar 批处理 `/b` 隐藏启动、健康检查和单独 Orca 启动 |
| GUI 验收时前台仍为 Windows 锁屏 | 3 | 停止坐标操作；真实 OBJ 离线视觉审计已完成，模型库、长期侧栏和 G-code 用户路径必须待解锁后继续，CLI 不能替代 GUI 验收 |
| 完成确认使用临时 `wxTimer` 并在自身回调中删除，触发 `Plater.cpp:10286` 访问冲突 | 1 | 改为由 `Plater::priv` 生命周期托管的单次定时器，通过统一 `wxEVT_TIMER` 分发确认结果；真实复跑后进程持续响应且无新增 crash 日志 |
| 多色自动流程沿用打印预设擦料塔，当前机型将塔放到热床外 | 1 | AI 自动切片前关闭擦料塔，并用 `is_slice_result_ready_for_export()` 作为完成门禁；保留颜色/换料映射，越界或 G-code 错误不再被标记为成功 |
| 首次短超时构建遗留编译器进程，占用 `Plater.obj` 与输出 DLL | 2 | 终止本轮遗留的 `mspdbsrv`，确认文件锁释放并关闭验收 Orca 后，以单并发完成 Release 构建 |

## 各阶段

### 阶段 1：恢复历史上下文
- [x] 检查已有规划文件
- [x] 运行会话恢复脚本
- [x] 定位源码仓库与版本控制根目录
- **状态：** complete

### 阶段 2：检查当前开发状态
- [x] 检查分支、远端与工作区（根目录无 `.git`，无法恢复当前分支/提交状态）
- [x] 分析本地开发改动与历史会话
- [x] 检查构建与测试痕迹
- **状态：** complete

### 阶段 3：总结与续作建议
- [x] 记录关键发现
- [x] 给出当前状态与下一步建议
- **状态：** complete

### 阶段 4：补充架构与打包审计
- [x] 核对 sidecar 打包与跨平台启动
- [x] 核对 feature gate、本地化目录和自动测试覆盖
- **状态：** complete

### 阶段 5：四项能力现状映射
- [x] 评估模型生成当前纵向流程与缺口
- [x] 识别 OrcaSlicer 现有模型检查/修复能力
- [x] 评估切片参数 AI 化当前能力和数据闭环
- [x] 梳理端到端交互可复用的执行入口
- **状态：** complete

### 阶段 6：目标架构与边界
- [x] 定义 provider-agnostic 服务、工作流与数据契约
- [x] 定义确定性内核与 LLM/Agent 的职责边界
- [x] 定义审批点、失败恢复、任务持久化和安全边界
- **状态：** complete

### 阶段 7：分期路线图
- [x] 给出 MVP 到完整能力的里程碑和依赖关系
- [x] 为每阶段定义验收标准、测试和发布要求
- [x] 输出建议优先级和下一步实施起点
- **状态：** complete

### 阶段 8：M0 AI 功能门控与能力发现
- [x] 读取计划、发现和进度日志
- [x] 重新确认当前 Git 工作区、AI GUI 接入点与 sidecar 契约差异
- [x] 增加默认关闭的 `enable_ai_features` 应用设置及 Preferences 实验开关
- [x] 统一 production/mock 的 `/health` v1 capability schema 并添加 Python 契约测试
- [x] 新增异步、loopback-only 的 `AIServiceManager`，仅在 capability 可用时延迟注册 AI UI
- [x] 恢复标准标签固定索引；3D Generate 改为 discovery 成功后追加且不抢占默认页
- [x] 在已提供 Windows CMake 路径后编译 `libslic3r_gui` 与 `OrcaSlicer` Release 目标
- [x] 使用隔离 `--datadir`、mock sidecar 和安装目录可执行文件完成默认关闭与已启用模型生成页的 GUI E2E
- [x] 在独立 `build-tests` 目录启用测试并运行新增 AppConfig Catch2 覆盖（3 assertions 通过）
- [ ] 在支持 OrcaSlicer 自定义菜单的驱动中验证 “Show AI Assistant” 菜单项与停靠 pane 显示/隐藏；当前 Windows DPI 坐标与 UI Automation 未能可靠驱动该菜单
- **状态：** complete — M0 核心门控、discovery、生成页、Release 编译与 AppConfig 单测均已验证；AI Assistant 的菜单交互为唯一后续补充

> 历史状态说明：当前 `master@a1ef7204fe` 已改为始终创建 3D Generate 页面并立即执行 capability discovery，不再保留本阶段记录的默认 `enable_ai_features` 门控；现状与后续决策以阶段 9 为准。

### 阶段 9：五大产品域状态与实施计划
- [x] 读取 `Docs/` 架构文档和 `Docs/开发进展.xlsx`
- [x] 以当前 `master@a1ef7204fe` 源码核对 27 项子能力
- [x] 输出统一成熟度口径、逐项状态矩阵和关键缺口
- [x] 输出核心 AI 与商业平台双轨路线图、依赖和验收门槛
- [x] 更新 `findings.md`、`progress.md` 并校验最终文档
- **状态：** complete

### 阶段 10：当前软件基线验证
- [x] 核对 `master@a1ef7204fe`、Windows 工具链、构建配置和工作区
- [x] 完成 Windows Release 全量构建、安装产物一致性检查
- [x] 完成 129 项 `libslic3r` Catch2/CTest 回归测试
- [x] 完成 production/mock sidecar health、Python 契约和语法检查
- [x] 使用隔离 `--datadir` 与 mock sidecar 完成 GUI 启动、首页、3D Generate、文生 3D、彩色 OBJ 导入和退出检查
- [x] 记录当前测试缺口、平台边界和下一步开发门槛
- **状态：** complete

### 阶段 11：2026-08-10 核心演示版冲刺
- [x] 将 macOS/Linux、账号计费和扩展架构降为最低优先级
- [x] 以当前 Windows Release 与模型生成纵向链路作为冲刺基线
- [x] 确认演示必须调用真实 AI provider；mock 仅作回归和灾备
- [x] 冻结单条黄金路径、非目标和失败降级策略
- [x] 修复黄金路径上的阻断问题并补最小回归覆盖
- [x] 完成真实/演示 provider 文生与图生 smoke
- [x] 完成生成结果导入、摆放、切片预览的端到端彩排
- [x] 产出可重复启动脚本、演示数据和现场操作清单
- **状态：** complete

### 阶段 12：3D Generate 页面交互优化
- [x] 复核用户标注图和 Windows 演示窗口布局
- [x] 图片选择后立即完整预览，并提供 50%–400% 缩放、Fit 和滚动查看
- [x] 将 provider 状态映射为五阶段用户进度，并显示步骤名和百分比
- [x] 保留失败发生时的最后阶段，修复 Windows 中文代码页下的符号乱码
- [x] 完成 Release 构建、锁屏窗口句柄验收和回归检查
- [x] 输出交互设计文档与验收记录
- **状态：** complete

### 阶段 13：完整参考图与两段式图生 3D 流程
- [x] 核对 Windows 启动入口、原始参考图和当前预处理契约
- [x] 将参考图预览改为稳定的 letterbox 完整绘制，保留缩放与滚动
- [x] 明确区分参考图与 AI 风格化预览图
- [x] 仅在风格化预览下载并显示后允许确认生成 3D
- [x] 图片风格化失败时禁止用原图冒充成功结果
- [x] 完成 Windows Release 构建、GUI 验收和 AI 回归
- [x] 更新设计、发现与进度记录
- **状态：** complete

## 关键问题
1. 四项 AI 能力各自已经达到什么程度，哪些只是 UI/规划？
2. 哪些能力应复用 OrcaSlicer 的确定性算法，哪些适合交给 LLM/Agent？
3. 端到端交互需要哪些可审计工具、状态机和用户确认点？
4. 如何分期交付，避免同时铺开四条线导致无法验收？

## 已做决策
| 决策 | 理由 |
|------|------|
| 以文件系统和 Git 现状为准恢复 | 会话恢复脚本没有返回未同步上下文 |
| 续作时先恢复可靠 Git 工作区 | 当前根目录没有 `.git`，继续修改前应先保护并可追踪现有工作 |
| Git 恢复后先修两个 UI 回归，再做真实 Tripo smoke | UI 问题已明确且改动小；真实服务验证是当前最大功能缺口 |
| 模型库与架构增强后置 | 当前首个纵向流程已可操作，应先完成收尾和真实链路验收 |
| 在真实服务验收后优先补 feature gate、sidecar 打包和本地化目录 | 这些缺口会影响未配置用户、跨平台交付和可发布性 |
| C++ 核心保持 provider-agnostic，供应商适配留在 sidecar | OrcaSlicer 不应被 Tripo、AGNES、OpenAI、Claude 等具体服务绑定 |
| 端到端交互采用受控工作流/工具编排，不让 LLM 直接任意修改模型或配置 | 几何、切片、导入和配置变更需要确定性校验、审计和用户确认 |
| 实施顺序采用 M0 基础治理 → M1 生成产品化 → M2 Preflight → M3 Repair → M4 切片闭环 → M5 Guided Workflow → M6 受控 Agent | 各能力先独立验收，避免编排层掩盖底层不稳定；最大化复用当前纵向生成流程和 OrcaSlicer 内核 |
| 演示版允许通过 `ORCASLICER_AI_ALLOW_PREPROCESS_FALLBACK=1` 显式启用文字预处理降级 | OpenAI-compatible 代理当前返回 HTTP 502；文字可保留原始 prompt 继续调用真实 Tripo，但图片必须先得到真实 AI 风格预览，不能再复制原图冒充成功结果 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 未找到既有规划文件 | 1 | 创建新的恢复状态规划文件 |
| 更新问题记录时文本不匹配 | 1 | 重新读取计划并使用准确文本更新 |
| 补充 GUI 追踪代理上下文超限（502） | 1 | 不重试；采用已完成的代码审计、截图和历史运行记录交叉验证 |
| 组合嵌套 Git 对象库与根目录后出现全量删除/未跟踪 | 1 | 放弃该全树 diff；改用版本、时间戳、代码引用和构建产物交叉恢复 |
| 重复提交无变化的计划编辑 | 1 | 不再重复该编辑，继续记录新的实际发现 |
| 等待构建审计代理最终摘要超时 | 1 | 不轮询；等待后台完成通知自动回传 |
| 新阶段恢复流程中的 `git diff --stat` 再次失败：根目录无 `.git` | 1 | 不再尝试根目录 Git 命令；继续以已恢复的文件与历史证据为准 |
| 模型检查/修复盘点代理因上游连接失败返回 502 | 1 | 不重复原任务；缩小范围，在同一研究上下文中只汇总关键入口与缺口 |
| 真实文生 smoke 在预处理阶段失败：OpenAI-compatible `/models` 返回 HTTP 502 | 1 | 未创建 Tripo 任务且未产生生成费用；实现显式、默认关闭的原始输入降级后再执行真实双 smoke |
| 后台重启后探针仍命中旧 `cmd /K` 控制台派生的 sidecar | 2 | 停止两个已确认命令行的生产 sidecar 子进程；改在独立端口以前台受控实例验证环境开关，避免重复付费调用 |
| OrcaSlicer CLI 重开已保存的 140 万面项目并切片时 `0xc0000005` | 1 | 保留 crash 日志与 WER 证据；先用 PDB/转储定位，再区分 CLI、3MF round-trip 与高面数因素，不重复相同切片命令 |
| 锁屏 GUI 验收中同步发送按钮消息超时并叠加多个确认框 | 1 | 改用异步 `PostMessage`，逐个确认现有弹窗；后续每次点击后先检查顶层窗口状态 |
| Windows Release 并行构建出现 `C3859/C1076`，系统分页文件不足 | 1 | 将本轮构建降为 `/m:1` 单并发；代码编译和链接随后通过 |
| 当前 PowerShell 未将 `cmake.exe` 加入 PATH | 1 | 使用 `CMakeCache.txt` 与既有配置定位 Visual Studio Community 自带 CMake，并以绝对路径构建 |
| 首次窗口截图只显示对照区左半边 | 1 | 确认画布无横向滚动；截图进程受 200% DPI 虚拟化影响，启用 Per-Monitor DPI awareness 后以 3144×2008 完整截图验收 |
| 真实 Tripo 低模任务返回 26,147 个三角面且存在 26 条边界边、14 条非流体边 | 1 | 保留唯一付费任务及完整产物，不自动创建第二个任务；门禁拒绝导入并输出结构化诊断，后续需收紧生成目标或增加保色本地修复/简化 |

### 阶段 14：真实 AI 风格预览通路修复
- [x] 确认机器级 `OPENAI_BASE_URL=https://laotie.dev`，并免费探测根路径与 `/v1/models`
- [x] 修复旧 sidecar 复用旧代理地址的问题，重启当前 production sidecar
- [x] 为域名根 Base URL 自动补全标准 `/v1` 前缀，并保留已有/自定义 API 路径
- [x] 为短图片指令补充风格预览约束，要求真实重绘、保留身份与完整构图、禁止原图冒充
- [x] 完成真实 `gpt-image-2` 图片编辑，校验输出与原图哈希不同并人工复核画面
- [x] 完成 Python 回归、语法与空白检查，确认当前 OrcaSlicer 与 sidecar 运行正常
- **状态：** complete

### 阶段 15：原图与 AI 结果对照预览
- [x] 复核用户截图并定位单一预览位图覆盖原图的根因
- [x] 将参考图与 AI 结果拆分为独立状态并并排绘制
- [x] 补齐生成中、等待结果、失败和成功状态文案
- [x] 完成 Windows Release 构建与回归检查
- [x] 使用 mock/已有预览完成 GUI 对照验收
- [x] 更新设计、发现与进度记录
- **状态：** complete

### 阶段 16：OBJ-only 生成与导入
- [x] 将 production/mock sidecar 的模型产物能力收敛为仅 `obj`
- [x] 移除真实生成中的 3MF/STL 回退，只请求并校验 OBJ
- [x] 将启动探针、smoke 客户端和 GUI capability discovery 同步为 OBJ 契约
- [x] 在 GUI 下载前拒绝非 OBJ 产物，并保留顶点色校验
- [x] 补充 OBJ 成功和转换失败不回退测试
- [x] 完成 Python 回归、Windows Release 构建、安装同步与运行态验证
- **状态：** complete

### 阶段 17：OBJ 资源包、颜色与本地产物闭环
- [x] 复核真实 Tripo OBJ 返回结构与 OrcaSlicer 颜色导入能力
- [x] 将 sidecar 任务、原始资源包和中间文件固定到项目根目录
- [x] 安全解压 OBJ ZIP 并保留 OBJ、MTL 与全部贴图
- [x] 将 UV 底色贴图烘焙为 OrcaSlicer 可识别的顶点色 OBJ
- [x] 将 GUI 下载文件固定到项目根目录并取消自动删除
- [x] 补充 ZIP 安全、纹理烘焙、持久化和回归测试
- [x] 离线恢复本次真实失败产物并验证带颜色导入
- [x] 完成 Windows Release 构建、部署和运行态复验
- **状态：** complete

### 阶段 18：彩色低模与打印色板约束
- [x] 核对 Tripo 官方生成、文件与图像编辑参数
- [x] 确认低模生成参数与失败回退策略
- [x] 定位打印色板来源为当前项目 `filament_colour`
- [x] 确认默认色板交互：自动采用当前耗材槽颜色，并在生成区完整展示
- [x] 设计生成提示约束、预览量化和 OBJ 二次量化闭环
- [x] 实现 Tripo 低模参数、色板传递与确定性量化
- [x] 补充 Python/C++ 回归、Windows Release 构建和离线产物验收
- [x] 补充实现计划与验收标准
- [x] 使用真实付费图片编辑验证 Q 版风格、纯色背景和严格打印色板
- [x] 经用户确认后执行一次付费 Tripo 低模 3D 端到端验收
- [x] 保存生成/转换 task ID、原始 ZIP、解包资源、顶点色 OBJ、渲染预览与结构化诊断
- [x] 使真实产物同时通过不超过 20,000 面、单连通、封闭流体和严格色板门禁
- **状态：** complete（阶段 20 新真实任务以 15,148 面、单连通、封闭流体和严格色板通过全部门禁）

### 阶段 19：3D Generate 导航、统一输入与色板交互
- [x] 复核用户截图、当前标签注册顺序、输入状态机和 `filament_colour` 读取入口
- [x] 确定统一文字+图片输入、按内容自动路由和本页自定义色板方案
- [x] 将 3D Generate 标签移到 Prepare 前并保持标签枚举一致
- [x] 移除文字/图片模式选择，支持文字、图片或两者任意组合继续
- [x] 压缩步骤区，只显示当前阶段主操作和必要的复核内容
- [x] 支持当前项目耗材色动态刷新与不回写项目的手动色板
- [x] 完成回归、Windows Release 构建和 200% DPI GUI 验收
- **状态：** complete

#### 阶段 19 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| Windows Release 构建因项目 `/MP` 并发触发 `C3859/C1076`、系统错误 1455 | 1 | 使用 `CL_MPCount=1`、`UseMultiToolTask=false` 和 `/m:1` 真正串行重试 |

### 阶段 20：三风格预览、色板保真与一键 G-code
- [x] 恢复当前实现、运行态、测试和规划上下文
- [x] 定位严格色板预览退化为少量主色的根因并设计保真映射策略
- [x] 增加 Q 版卡通、赛博朋克、古典风格选择并优化各自生成约束
- [x] 从本机图片目录选取合适输入完成三风格真实图片验证
- [x] 将模型生成后的微小脱离件清理、保色拓扑预修复、CGAL 失败保护、尺寸规范化、摆盘和切片收敛为默认自动流程
- [x] 为自动流程补充修复诊断、可恢复产物和 G-code 结果入口
- [x] 使用自然色板真实失败 OBJ 完成自动修复、切片与 Preview 验收
- [x] 完成 Python/C++ 回归、Windows Release 构建、GUI 与 G-code 验收
- **状态：** complete

#### 阶段 20 约束
- 图片生成可按需调用；3D 生成累计最多 20 次，必须记录每次 task ID、用途、结果和剩余额度。
- 默认黄金路径在风格预览确认后尽可能一键到 G-code，不要求用户手动修复、简化、缩放或摆盘。
- 自动修复或简化不得静默丢失打印颜色；失败时保留原始产物和结构化诊断。

#### 阶段 20 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 新色簇匹配算法定向测试报 `NameError: _rgb_to_lab` | 1 | 核对现有实现后改用实际函数名 `_srgb_to_lab`，再运行同组测试 |
| OBJ 自动重试测试仍断言旧三参数调用，且成功夹具触发同路径 `copyfile` | 1 | 更新断言包含 attempt 编号，并允许恢复场景直接复用最终目标路径 |
| 新增肤色区域测试时误用不存在的 `PaletteTests` 类名 | 1 | 通过源码确认实际类名为 `PrintablePaletteTests`，改用精确测试标识重跑 |
| 全仓搜索量化调用点因生成产物目录过大超时 | 1 | 将搜索范围收敛到 `tools/ai`，确认唯一生产调用点并继续修改 |
| 合并停止、环境设置、日志和后台启动的 PowerShell 命令被本机策略拒绝 | 2 | 已分步停止精确 PID；改用现有 Windows 批处理启动链的后台模式传入非敏感配置，不再重复复杂 `Start-Process` 命令 |
| Win32 `GetDlgCtrlID` 将 wxWidgets 高位控件 ID 返回为负数，文本变更通知被临时驱动跳过 | 1 | 除 `-1` 外统一按低 16 位发送 `EN_CHANGE`，确认文字-only 输入可启用下一步 |
| 同步按钮消息导致自动化超时并叠加两个生成确认框 | 1 | 改用异步点击；只确认最上层 mock 流程并取消残留确认框，未产生真实 3D 调用 |
| mock 四色切片完成后提示 G-code 路径超出热床边界 | 1 | 日志确认 `model_fits=1` 且 G-code 成功导出；该提示来自当前 WonderMaker 多色打印配置的路径检查，不影响 OBJ 导入、切片或 Preview 验收 |
| `planning-with-files-zh` 自带 `check-complete.ps1` 因安装文件乱码产生 PowerShell 解析错误 | 1 | 不修改用户技能目录；改用 UTF-8 读取和状态文本扫描确认阶段 20 已完成 |
| 自然色板真实 OBJ 含 1 个微小脱离件、45 条边界边和 4 条非流形边 | 1 | generation `92c11c8e-e949-4902-9bc6-1566b4536846`、conversion `a873c5f1-95a5-4ecb-bf56-0a4a79c2e3fc` 已持久化；不重复付费生成，改为本地删除微小组件并在 Orca 内自动 CGAL 保色修复 |
| 当前 Codex PowerShell 的 `PATH` 未包含 `cmake` | 1 | 使用 Visual Studio 2022 自带 CMake 的绝对路径完成单并发 Release 构建 |
| 复合 PowerShell 后台启动命令被本机策略拦截 | 1 | 未启动任何进程；拆成 mock 与 Orca 两个短批处理入口，并分步执行健康检查 |
| CGAL 自动修复真实 OBJ 后仍返回 `mesh still open after hole filling` | 2 | GUI 已正确撤销导入且未切片；不再重复同一 CGAL 方案，改为 sidecar 对约 1 mm 非流形局部做保色预修复，再由 Orca 执行最终门禁 |
| `py_compile` 与 `unittest` 并行写同一个 `__pycache__` 时 Windows 返回拒绝访问 | 1 | 验证改为串行；单测设置 `PYTHONDONTWRITEBYTECODE=1`，随后语法检查和 3 项定向测试通过 |

### 阶段 21：最终需求审计与生产链路复验
- [x] 对照用户标注截图核对 3D Generate 标签顺序、统一输入、紧凑布局和耗材色板交互
- [x] 确认页面显示时重新读取当前项目 `filament_colour`，并保留本页自定义色板
- [x] 复核三种最终风格预览及其实际用色统计
- [x] 验证生产 sidecar 与真实 AI 的一次连续风格预览到 G-code 黄金路径
- [x] 重跑 Python 回归、Windows Release 构建和最终 GUI 验收
- **状态：** complete

#### 阶段 21 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 真实 GUI 黄金路径导入成功但切片被混合温度门禁拒绝 | 1 | 当前项目槽 1 为 PLA、槽 2–8 为 ABS；不关闭安全门禁，改为自动选择最大的温度兼容槽组并保留原始槽号映射 |
| 初版温度筛选从合并配置读取单值/短数组，真实项目仍显示 8 色 | 1 | 改为从 `PresetBundle::filament_presets` 逐槽读取实际耗材预设，与切片安全检查使用同一温度数据来源 |
| 兼容色板状态单行文案在 200% DPI 下被截断 | 1 | 将兼容颜色数与排除槽位数拆为两行，最终截图无裁切 |
| 首页嵌入式视图吞掉物理导航点击，旧截图未进入 3D Generate | 2 | 枚举导航栏子窗口并向 `3D Generate` 实际句柄发送鼠标消息，废弃无效截图 |
| 首次增量构建调用因工具短超时返回 124，但编译仍在后台运行 | 1 | 不启动重复构建，跟踪原 `cmake/MSBuild` 进程直到链接完成并校验 DLL 哈希 |

### 阶段 22：3D 生成页模型预览与中文化
- [x] 恢复规划、运行态、工作区和既有 GUI 证据
- [x] 明确 3D 预览后的流程停顿点与确认操作
- [x] 设计并实现生成页内的只读交互式 3D 预览
- [x] 将 3D 生成页、状态、操作按钮和相关确认框统一为中文
- [x] 使用现有真实 OBJ 完成 Windows Release、GUI 和 G-code 回归
- [x] 更新设计、发现和最终运行记录
- **状态：** complete

### 阶段 23：可打印颜色开关
- [x] 在生成页增加“使用可打印颜色”开关，默认保持开启
- [x] 关闭时让图片预览、提示词和 OBJ 后处理保留自然颜色
- [x] 关闭时将模型作为普通单耗材对象导入，避免错误映射为有限耗材色
- [x] 补充 sidecar 契约、自然色 OBJ 与状态一致性回归
- [x] 完成 Windows Release 构建和 GUI 验收，不新增付费 AI 调用
- **状态：** complete

#### 阶段 23 约束
- 开启时完全保留现有严格打印色板与多耗材流程。
- 关闭时自然颜色用于图片和 3D 预览；G-code 使用当前单一耗材，不承诺还原自然色。
- 切换开关后，已生成的预览必须失效并要求重新生成，不能混用两种颜色策略。

#### 阶段 23 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 自然色 OBJ 测试假定烘焙说明位于首行，但尺寸规范化会前置自己的注释 | 1 | 移除脆弱的注释位置断言，继续以顶点色、拓扑和产物验证作为验收标准 |
| Windows 下 `python -m py_compile tools/ai/*.py` 不展开通配符 | 1 | 由 PowerShell 枚举明确的 `.py` 文件列表后传给 `py_compile` |
| `cmake --install` 长时间同步资源后未更新运行目录 DLL | 1 | 确认 Orca 未运行后，仅部署已通过构建的 DLL，并以 SHA256 校验构建/运行副本一致 |
| 部署检查只查询 `OrcaSlicer`，漏掉实际进程名 `orca-slicer`，DLL 仍被 PID 49440 占用 | 1 | 使用 `tasklist /m OrcaSlicer.dll` 精确定位本项目实例，停止该 PID 后再部署 |
| 停止 PID 49440 后立即复查仍短暂返回进程存在 | 1 | 单独复查模块占用确认进程随后正常退出，再执行部署，不强制终止或重复发送停止命令 |
| 关闭颜色约束后的单行说明在 Windows 200% DPI 窄栏中被裁切 | 1 | 将自然色预览与单耗材打印说明拆成固定两行后重新构建验收 |

### 阶段 24：正式 AI 链路稳定性与准备页首开性能
- [x] 定位截图中图片预处理服务连接失败的真实根因并恢复正式链路
- [x] 移除日常开发和验收入口对 mock sidecar 的依赖，统一使用 `18764` 正式服务
- [x] 将图片预处理连接错误改为中文、可诊断且不泄露凭据的提示
- [x] 定位“准备”页首次进入卡顿的同步初始化热点并优化
- [x] 完成 Windows Release 构建、正式服务检查和真实 GUI 验收
- **状态：** complete

#### 阶段 24 约束
- 后续不再使用 `18765` mock 版进行功能或 GUI 验收。
- 允许使用不产生费用的正式服务健康检查；新的图片生成调用可直接验证，3D 生成仍需用户单独确认。
- 保留正式失败产物与错误分类，不用本地假结果冒充成功。

#### 阶段 24 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 父进程保留旧 `OPENAI_BASE_URL`，重启子进程仍访问旧 IP | 2 | 启动前从 Windows User/Machine 环境刷新配置；健康接口报告非敏感 provider URL，启动器检测变化后只重启确认归属的正式 sidecar |
| 旧 `/K` sidecar 宿主在仅停止 Python 后继续占用启动链 | 1 | 停止脚本同时验证并关闭专属 `start_orca_ai_sidecar.bat` 父进程，避免旧宿主重新派生子进程 |
| `.bat` 经补丁后使用 LF，`cmd.exe` 无法稳定识别 `goto` 标签 | 1 | 三个 Windows 批处理入口统一恢复 CRLF，并重跑 `--check` |
| 安装目录 DLL 被正在运行的 Orca 锁定 | 1 | 不强制关闭未保存窗口；保留旧 DLL 备份并并排部署已构建 DLL，随后用隔离实例验证 |

### 阶段 25：彩色预览保持与持久模型库
- [x] 定位颜色、跨页面预览和模型库的当前状态根因
- [x] 明确颜色丢失发生在生成页预览还是导入后的准备/切片视图
- [x] 设计持久模型索引、历史扫描和损坏条目降级策略
- [x] 保持当前 3D 预览跨页面切换和导入后可恢复
- [x] 支持模型库展示全部有效历史 OBJ，双击载入 3D 预览
- [x] 保留并恢复每个模型的颜色模式、色板、来源和风格元数据
- [x] 完成 Windows Release、正式 OBJ、跨页面和重启后模型库 GUI 验收
- [x] 当前 Orca 进程关闭后，将最新 Release DLL 同步到日常运行目录
- **状态：** complete

#### 阶段 25 约束
- 模型库历史必须来自项目 `generated_models` 中的真实持久产物，不使用 mock 条目。
- 双击历史模型只加载生成页 3D 预览，不自动导入、摆盘或切片。
- 颜色元数据缺失时优先从 OBJ 顶点色恢复，不把无色模型伪装成彩色模型。

#### 阶段 25 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 直接向原生 notebook 发送 `TCM_GETITEMRECT` 导致 `COMCTL32.dll` 崩溃 | 1 | 认定为 GUI 自动化误用，不重复该消息；改用正常鼠标/窗口消息完成跨页与双击验收 |
| `libslic3r_tests` 首次重建因 `/MP` 并发触发 `C1060/C3859` 和页面文件不足 | 1 | 使用 `/m:1`、`CL_MPCount=1`、`UseMultiToolTask=false` 串行完成构建，新增颜色用例 5/5 断言通过 |
| 沙箱内 Release 构建无法读取本机 Windows SDK 目录 | 1 | 获得构建权限后在沙箱外运行同一 CMake Release 目标，构建成功 |
| 最新 DLL 与日常运行目录哈希不一致，但 PID 58140 正在使用旧 DLL | 1 | 不强制关闭可能有未保存内容的用户窗口；保留最新构建产物，待当前 Orca 正常关闭后同步 |

### 阶段 26：真实彩色 OBJ 保色闭合修复
- [x] 从用户截图确认失败阶段、模型规模、颜色状态和错误文本
- [x] 定位本次真实任务、原始 OBJ、修复诊断和失败拓扑特征
- [x] 设计不重新付费生成、不绕过流体门禁的确定性保色修复
- [x] 实现局部闭合修复与失败降级，保留原始模型和颜色
- [x] 支持已下载/历史 OBJ 在 sidecar 任务失效后由用户确认继续导入切片
- [x] 完成离线拓扑/颜色回归、Windows Release 构建和真实 GUI 切片验收
- [x] 部署正式版本并恢复日常启动状态
- **状态：** complete

#### 阶段 26 约束
- 不创建新的 Tripo 任务，不消耗 3D 生成额度。
- 不允许开放网格或非流形网格绕过门禁进入切片。
- 修复前后必须保持单一主体、尺寸和顶点色；失败时保留原 OBJ 与结构化诊断。

#### 阶段 26 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 独立修复调用导入 `orca_ai_sidecar` 时找不到 `openai_preprocessor` | 1 | 显式将项目 `tools/ai` 加入 `PYTHONPATH`，不修改系统环境 |
| 前后对比脚本对原始开放网格使用严格拓扑校验并按预期被拒绝 | 1 | 修复前使用 `allow_repairable=True` 读取诊断，修复后继续使用严格零异常边校验 |
| sidecar 重启后 GUI 确认按钮请求旧 job ID，返回 `Model job not found` | 1 | 改为有效本地 OBJ 优先导入；模型库载入后允许用户显式确认生成 G-code，远端下载仅作本地缺失兜底 |
| Windows `rg` 不接受 `AIModelGenerationClient.*` shell 通配路径 | 1 | 改用明确文件或目录范围，不重复 Windows 路径通配写法 |
| 构建和首次全量测试超过工具 5 秒等待窗口 | 2 | 先检查残留进程，确认构建继续完成；测试改用 60 秒超时并读取最终 68/68 结果，不把工具超时误判为失败 |
| PowerShell 将 `foreach` 结果直接接管道时报空管道元素 | 2 | 先写入数组再统一 `Format-List`，避免脚本块后的直接管道解析差异 |
| GUI 截图命令使用不存在的程序集简称 | 1 | 使用完整 `System.Windows.Forms` 与 `System.Drawing` 程序集名重跑，Orca 进程和切片不受影响 |
| `planning-with-files-zh` 完成检查脚本因安装文件乱码无法解析 | 1 | 不修改技能安装目录；直接核对阶段 26 全部复选项和 `complete` 状态，并将工具问题记录在计划中 |

### 阶段 27：修复失败手动导入与模型库切片
- [x] 核对自动修复失败路径、模型库预览状态和主窗口切片回调
- [x] 明确失败模型不自动切片、手动导入只进入准备页的安全边界
- [x] 实现自动修复失败后的“手动导入 / 取消”交互
- [x] 实现模型库预览后的“导入并切片”明确操作
- [x] 完成 Windows Release 构建和两条真实 GUI 流程验证
- [x] 部署正式版本并恢复日常运行状态
- **状态：** complete

#### 阶段 27 约束
- 不创建新的 Tripo 任务，使用项目内已有真实 OBJ 验证。
- 自动修复失败的开放网格不得自动触发切片。
- 手动导入与模型库导入都必须复用现有颜色映射，原 OBJ 与诊断保留在项目目录。
- 模型库双击只预览，切片必须由用户点击明确按钮触发。

#### 阶段 27 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `MessageDialog` 不暴露 `SetYesNoLabels`，首次编译失败 | 1 | 改用项目中支持自定义按钮文案的 `RichMessageDialog`，保留“手动导入 / 取消”交互 |

#### 阶段 22 约束
- Windows 演示优先，不扩展 macOS/Linux 工作。
- 不新增真实 Tripo 调用；使用现有真实 OBJ 和 mock 验证。
- 预览不得绕过网格、颜色、温度兼容和切片安全门禁。

#### 阶段 22 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 组合读取状态机源码的命令超时 | 1 | 缩小到具体函数和行段，避免同一命令并发大范围搜索 |
| PowerShell 将 `rg` 的通配符/含双引号正则错误解析 | 2 | 改用明确目录和单引号固定模式，不再使用 Windows 下的 shell 通配符 |
| 预览画布首次为空白 | 3 | 依次排除 DPI、刷新和上下文绑定，最终确认生成页先于主 3D 画布打开时全局着色器未初始化；补齐 `SetCurrent -> init_opengl -> render` 顺序并隔离共享 GL 状态 |
| 新增 framebuffer 隔离后编译缺少 GLAD 声明 | 1 | 按 `GLCanvas3D.cpp` 的既有模式引入 `<glad/gl.h>` |
| 非 DPI-aware 截图只捕获应用左半边，误判模型仍为空 | 1 | 截图进程启用 Per-Monitor DPI awareness，使用 3144x2008 物理像素完成视觉验收 |

### 阶段 28：模型导入颜色策略解耦
- [x] 核对 Orca 原生 OBJ 颜色导入与 AI 自动映射路径
- [x] 明确“生成颜色约束”和“导入颜色策略”的独立语义
- [x] 新增“正常导入（保留颜色）/ 单色导入”选择
- [x] 自动上色失败时保留模型并进入准备页手动上色
- [x] 更新状态文案和模型库导入行为
- [x] 完成 Windows Release 构建与真实 OBJ GUI 验证
- **状态：** complete

#### 阶段 28 约束
- 默认使用正常导入，不再由“使用可打印颜色”开关隐式强制单色。
- 自动上色失败不得阻断模型导入，也不得直接触发切片。
- 网格修复门禁保持不变；开放网格仍需修复或手动处理后才能切片。
- 不创建新的图片或 Tripo 付费任务。

#### 阶段 28 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 单色模型首次 G-code 导出成功后被 `enable_prime_tower` 归一化立即作废 | 2 | 首次误写 `project_config`，但完整配置仍被当前打印预设覆盖；改为在显式单色自动切片前更新 `prints.get_edited_preset().config`，同步预设/项目脏状态和 Plater 配置 |
### 阶段 45：质量优先的多样本四色综合调优（2026-08-15）

- [x] 建立 8 类输入的可恢复质量基准，并完成两轮 Image2 定向调优
- [x] 修正成人身份保持、动物原始花纹、稳定商品底座和动态耗材色板提示规则
- [x] 将“四色”统一为“最多四种实际耗材色”，少色但语义正确的结果允许继续
- [x] 新增 cel_shaded、enamel_inlay 打印风格并完成正式 GUI 验证
- [x] 用人像和机器人各完成一次真实 Tripo 50 万面生成与顶点色 OBJ 后处理
- [x] 消除三色三角面，同时保持共享拓扑、多个有效部件和顶点色
- [x] 对齐 Orca 的有向边判定，自动规范同向相邻面的绕序
- [x] 在正式 Orca 中完成两类模型的四色映射、导入和 500 层切片验证
- [x] 完成 Python AI 全量回归并重启正式 sidecar
- **状态：** complete

#### 阶段 45 验收结论

- 付费调用总计：35 次 Image2、2 次 Tripo；未为拓扑修复重复创建 Tripo 任务。
- 人像最终 OBJ：239,148 顶点、478,292 三角面、1 个连通部件、0 边界边、0 非流形边、0 同向绕序边。
- 机器人最终 OBJ：467,634 三角面、3 个有效部件、0 非法拓扑边；保留多部件是为了保留 Orca 原生多色语义，不再强制合并成单对象。
- 人像正式切片：500 层、604 次工具更换、预计约 4 小时 16 分；修复前的“6 条非流形边”提示已消失。
- 机器人正式切片：500 层、597 次工具更换、预计约 4 小时 19 分；出现的打印床/擦料塔路径提示属于布局问题，不是模型拓扑错误。
- Python AI 全量回归：124/124 通过；`git diff --check` 通过。
- 正式服务：`127.0.0.1:18764` 健康，版本 `orcaslicer-ai-sidecar-v4`，上游 `https://laotie.dev`。

### 阶段 46：模型生成 Beta 1 与 3D 结构质量门禁（2026-08-16）

- [x] 总结当前产品目标、成熟度、已有证据和 Beta 1 缺口
- [x] 冻结第一版质量门禁边界：确定性结构检查优先，视觉语义评分后接
- [x] 设计 `pass / review / reject` 契约和旧任务兼容策略
- [x] 实现独立 OBJ 结构质量分析模块和结构化报告
- [x] 接入正式 Sidecar 制品准备链路，不新增付费任务
- [x] 增加合成网格回归，并用阶段 45 两个真实 OBJ 校准阈值
- [x] 运行 AI 全量回归、语法检查和差异检查
- [x] 回写验证结果和下一阶段视觉评分/盲测计划
- **状态：** complete（第一批确定性结构门禁完成；下一批接 GUI 复核详情与历史模型显式重验）

#### 阶段 46 约束

- 本阶段不创建 Image2 或 Tripo 付费任务，复用现有真实 OBJ。
- 只有确定性错误可以阻断；启发式打印风险先进入 `review`，防止误伤有效多部件模型。
- 多个有效部件合法，不恢复“必须单对象”的旧需求。
- 所有新增产物和临时验证结果继续保存在项目 `generated_models/` 下。
- 第一版不把短边或悬垂指标宣传为精确壁厚/免支撑保证。

#### 阶段 46 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| Windows `rg` 不接受 `AIModelGenerationClient.*` shell 通配路径，同批并行只读脚本返回失败 | 1 | 改用明确文件路径或目录范围；没有修改项目文件 |
| 从仓库根以模块名运行新测试时找不到 `printable_model_quality` | 1 | 按现有 AI 测试模式将测试文件所在的 `tools/ai` 加入 `sys.path`，不修改生产导入路径 |
| 微小部件测试用两个同为 4 面的四面体，正式面数占比阈值下未触发复核 | 1 | 测试显式放宽面数占比，仅验证尺寸与占比联合判定；生产阈值保持不变 |
| 新门禁将既有“少量开放边交给 Orca 修复”的安全降级误判为硬失败 | 1 | 独立分析默认严格拒绝；Sidecar 显式声明下游修复能力后，小于既有上限的异常降为 `review`，超限仍拒绝 |
| 首次兼容修复补丁把 Sidecar 修改行误放在质量模块补丁段，补丁整体校验失败 | 1 | 拆分并明确各文件更新段；失败补丁未修改任何文件 |
| 合并停止、日志重定向、隐藏启动和健康检查的 PowerShell 命令被本机策略拦截 | 1 | 命令未执行；改用安全停止、隐藏启动、健康检查三个短步骤 |

#### 阶段 46 验收结果

- 新模型落盘 `model-quality.json`，API 暴露 `model_quality`；`reject` 阻断 ready，`review` 保留人工/Orca 修复路径。
- 人像为 `pass`；机器人为 `review`，仅有两个微小组件提示；两者均无硬错误。
- 合成质量门禁 9/9、质量模块/OBJ/Sidecar 契约定向回归 77/77、最终 AI Python 全量回归 135/135 通过。
- 全部 `tools/ai/*.py` 通过 `py_compile`，`git diff --check` 通过。
- 没有新增 Image2 或 Tripo 付费任务，没有 C++ 改动。
- 正式 Sidecar 已加载最终代码，PID `198400`，健康检查正常且 latest-job 契约包含 `model_quality`。

### 阶段 47：质量复核 UI 与历史模型重新检查（2026-08-16）

- [x] 核对质量报告契约、C++ 状态解析、3D 预览和模型库加载边界
- [x] 确定常驻质量卡、三态导入规则和窄 recheck 接口
- [x] 实现 Sidecar 历史任务本地重新检查接口与回归
- [x] 扩展 C++ `JobStatus` 质量契约和客户端调用
- [x] 在 3D 预览区增加中文质量卡、详情和重新检查操作
- [x] 让模型库双击加载后恢复对应质量状态
- [x] 完成 Python/C++ 回归、Windows Release 构建与正式 GUI 验收
- [x] 更新 Beta 1 状态文档和阶段记录
- **状态：** complete

#### 阶段 47 约束

- 重新检查只接收 `job_id`，不接受客户端传入文件路径。
- 本阶段不创建 Image2、Tripo、conversion 或其他付费任务。
- `review` 不阻断用户明确导入，`reject` 必须禁用导入；无报告不能冒充通过。
- 历史任务无法重验时保留模型预览和原文件，不自动删除或覆盖。
- Windows 优先；本阶段不扩展 macOS/Linux 验证。

#### 阶段 47 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 辅助符号搜索再次使用 Windows 不支持的 `AIModelGenerationClient.*`/`ModelGenerationPanel.*` 路径通配 | 1 | 搜索项失败但同批源码读取和差异检查正常；后续只使用明确文件名，不再使用路径通配 |
| 首次 60 秒构建超时留下孤儿 MSBuild，造成同一项目重复编译 | 1 | 精确结束旧构建进程树，后续给构建保留长超时并分段读取；串行构建成功 |
| Sidecar “启动并轮询”组合 PowerShell 被本机策略拦截 | 1 | 拆为独立启动和健康检查命令，服务正常恢复 |
| GUI 只显示另一工作目录中的 3 个模型，无法验收项目历史库 | 2 | 确认 Orca 启动会改变当前目录；正式启动必须显式传入与 Sidecar 相同的 `ORCASLICER_AI_OUTPUT_DIR`，随后完整历史列表出现 |

#### 阶段 47 验收结果

- Sidecar 合约支持 `POST /v1/orcaslicer/model-jobs/{job_id}/recheck`；旧模型仅按已校验 UUID 接管 `generated_models/<UUID>/model-vertex-color.obj`，拒绝任意路径和目录越界。
- 正式 GUI 三态均已用现有真实模型验证：`pass` 绿色且允许导入，`review` 橙色且允许人工确认后导入，`reject` 红色并禁用导入。
- 历史模型双击加载、状态恢复、显式重新检查、检查期间暂时禁用导入、完成后保留预览和文件均正常。
- Python AI 全量回归 138/138、`py_compile`、`git diff --check`、`libslic3r_gui` 和完整 Windows Release 链接均通过。
- 部署 DLL SHA-256：`84A8008751194027127805D1D72D56FCA0CD8F6D9B33998C4F86791858DE75F1`；本阶段未创建任何 Image2 或 Tripo 付费任务。

### 阶段 48：多视角渲染与 AI 视觉复核（2026-08-16）

- [x] 审计 Orca 缩略图渲染、现有 OBJ 解析和 OpenAI 兼容接口
- [x] 对比 Sidecar 软件渲染、Orca OpenGL 离屏渲染和供应商截图三种路线
- [x] 冻结“视觉建议不覆盖结构硬门禁”的首版边界
- [x] 实现最终 OBJ 的五视图渲染、拼图和缓存元数据
- [x] 实现显式视觉复核接口、结构化报告与安全失败降级
- [x] 在 3D 生成页增加中文视觉复核状态和操作
- [x] 增加 Python/C++ 定向回归并完成 Windows Release 构建
- [x] 用现有真实模型完成正式 GUI 验证，并确认视觉复核不改变结构硬门禁
- [x] 回写 Beta 1 状态、发现和下一阶段盲测计划
- **状态：** complete

#### 阶段 48 约束

- 只有用户显式操作才能产生视觉模型调用；Sidecar 启动、历史库扫描和结构重验不调用付费 AI。
- 视觉结果第一版只有 `pass / review / unavailable`，不得产生硬拒绝或覆盖 `model-quality.json`。
- 图生 3D 在用户显式复核时可发送原参考图和最终模型拼图；不保存或输出 API 密钥。
- 多视角产物、报告和临时文件只写入当前任务的 `generated_models/<job_id>/` 目录。
- 不创建新的 Image2 或 Tripo 任务；Windows 优先，本阶段不扩展 macOS/Linux 验收。

#### 阶段 48 验收结果

- 最终 OBJ 可确定性生成前、后、左、右、等轴五视图及 `model-view-sheet.png`；29.6 万面真实模型首次渲染约 12.8 秒，重复复核命中 SHA-256/版本/模型缓存。
- 新接口 `POST /v1/orcaslicer/model-jobs/{job_id}/visual-review` 只接受任务 ID 和布尔 `force`，只读取任务目录内已注册 OBJ/参考图；报告写入 `visual-quality.json`。
- 正式人物模型完成 1 次真实视觉模型调用：86 分、置信度 0.91，因“截断式半身且无独立底座”进入 `review`；没有调用 Image2 或 Tripo。
- 正式 GUI 显示中文视觉卡、分数、摘要和告警；视觉 `review` 后“导入并自动切片”仍可用，结构 `reject` 的既有硬阻断逻辑未改变。
- AI Python 全量回归 148/148、全部 `tools/ai/*.py` 语法检查、`git diff --check`、`libslic3r_gui` 和完整 Windows Release 链接通过。
- 部署 DLL SHA-256：`658A5C545BEF582B843BF3CB4743301A6A822C546B5052D2E027A4F9BF80C5F8`；正式 Sidecar 与 Orca 已加载新版本。

#### 阶段 48 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| JSON 清单把 Python tuple 写成 list，首次缓存测试不命中 | 1 | 写清单前把背景色显式规范为 JSON list，缓存回归通过 |
| PowerShell 不替 Python 展开 `tools/ai/*.py` | 1 | 改由 Python `glob` 枚举并逐个 `py_compile`，全部通过 |
| 新终端未继承 CMake PATH | 1 | 使用现有 Visual Studio 2022 自带 CMake 的绝对路径完成构建 |
| 首次部署时旧 Orca 尚未完全释放 DLL | 1 | 核对进程和目标 SHA，待句柄释放后重新复制并确认源/目标哈希一致 |
| 正式 Orca 启动初期只有恢复弹窗，主窗口未立即暴露 | 1 | 通过 Computer Use 选择唯一 Orca 窗口并恢复验证会话，随后完成页面验收 |

### 阶段 49：模型生成小批量盲测与评分校准（2026-08-16）
- [x] 冻结第一批盲测范围、类别、色板、评分维度和付费上限
- [x] 编写可恢复盲测设计文档
- [x] 实现清单驱动的参考图、Tripo、结构/视觉复核编排器
- [x] 增加无付费默认、指纹防串样、恢复与汇总回归测试
- [x] 用 4 个全新样本生成动态四色参考图
- [x] 最多创建 4 个 Tripo 30 万面生成任务并保留转换产物
- [x] 完成结构门禁、五视图与 AI 视觉复核
- [x] 完成人工评分和自动/人工一致性汇总
- [x] 更新 Beta 1 状态、发现与下一批扩容建议
- **状态：** complete

#### 阶段 49 约束

- 盲测期间冻结生产提示词、结构阈值和视觉阈值，不依据中间样本即时调参。
- 每例最多 1 次 Image2、1 次 Tripo 生成、1 次 OBJ 转换、1 次视觉复核；第一批 Tripo 生成不超过 4 次。
- 所有执行产物写入 `generated_models/model-blind-benchmark-phase49/`，不得覆盖历史模型库。
- 付费阶段必须显式确认；已有状态时只恢复原任务，禁止因超时或本地失败重复付费。
- 单例失败不终止整批，错误与成功结果同样进入汇总。

#### 阶段 49 验收结果

- 4/4 参考图通过动态四色图片门禁；实际新增 4 次 Image2、4 次 Tripo 生成、4 次 OBJ 转换和 4 次视觉复核，没有重复付费任务。
- 4 个最终 OBJ 为 266,148–297,406 面，均严格使用各自目标四种顶点色，边界边、非流形边和退化面全部为 0。
- 狐狸、机器人、相机结构 `pass`；塔楼因 5 个四顶点微小脱离组件为 `review`，没有结构 `reject`。
- AI 视觉分数为 82、78、71、89；自动与人工均判定四例需要复核，三态精确一致率 4/4。
- 五视图确认当前最大缺口是单张参考图无法充分约束背面/侧面，不是颜色或 OBJ 拓扑；正式扩容前先验证多视图 3D 输入。
- AI Python 全量回归 156/156、全部 `tools/ai/*.py` 语法检查和 `git diff --check` 通过。

#### 阶段 49 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 首次真实 Image2 命令把 shell 超时设为 1 秒，进程在创建状态前被终止 | 1 | 改用长命令超时配合可持续读取的 cell；确认未产生图片、状态或付费调用后继续 |
| 首次读取图片报告时误用 `printable-image.json` 文件名 | 1 | 按管线实际输出读取 `metadata.json`，没有修改产物 |
| PowerShell 汇总表达式在 `foreach` 后直接接管道导致空管道语法错误 | 1 | 先将循环结果赋给数组再格式化，仅影响只读汇总 |
| 新增参考图质量门禁后，旧单元测试未显式把参考阶段标记为成功 | 1 | 修正测试前置状态；生产门禁保持不放宽 |

### 阶段 50：多视图约束 3D 与架构复核（2026-08-16）

- [x] 核对 Tripo v3 官方多视图能力、输入契约和当前本地供应商客户端
- [x] 比较 Image2 四视图、Tripo 多视图预处理和拼图单图三条路线
- [x] 冻结“Image2 四视图板 + 原生多视图 3D”设计与 Tripo 调用上限
- [x] 创建多视图供应商边界 ADR 与非功能约束
- [x] 实现 Image2 四视图生成、切分、四色处理和一致性门禁
- [x] 实现 Tripo 命名视图请求与可恢复多视图验证编排器
- [x] 增加协议、布局、门禁、防重复付费和恢复测试
- [x] 生成机器人、相机四视图参考并通过人工/AI 预检
- [x] 最多创建 2 次 Tripo 多视图 3D 任务；必要时才启用第 3 次狐狸
- [x] 完成 OBJ 后处理、结构/视觉复核并与单视图基线对比
- [x] 运行全量回归并完成 Orca/Sidecar/Provider 架构复核
- [x] 更新 Beta 1 状态和下一步建议
- **状态：** complete

#### 阶段 50 约束

- Image2 可为视图一致性充分迭代；Tripo 3D 生成默认只使用机器人和相机两次，最多三次。
- 多视图实验不得直接扩展 Orca GUI 或 3MF/打印配置格式。
- GUI、Sidecar 领域状态不得出现 Tripo URL、file token 或 endpoint。
- 未通过本地四色硬门禁的样本不得提交 Tripo；AI 一致性为 `review` 时，必须完成人工目视复核并持久化批准理由、图片哈希和 AI 警告后才可提交。
- 阶段结束必须按 ADR 检查依赖方向、恢复边界、测试隔离和未来多供应商适配。

#### 阶段 50 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| Windows `rg` 再次收到 `tools/ai/test_*.py` shell 通配路径并返回路径语法错误 | 1 | 改为以 `tools/ai` 为目录并使用 `-g "test_*.py"`；同批显式目录搜索已正常返回 |
| 精确图片编辑测试在临时目录上下文退出后才检查输出文件 | 1 | 将文件存在断言移入临时目录生命周期；生产代码未修改 |
| PowerShell 汇总再次在 `foreach` 表达式后直接接管道，触发空管道语法错误 | 1 | 将循环结果先赋给 `$rows` 再格式化；仅影响只读汇总，没有修改产物 |

### 阶段 51：多视图配对校准第二批（2026-08-16）

- [x] 恢复阶段 50 状态并比较一次扩 20 例、分批扩样和立即产品化三条路线
- [x] 冻结狐狸、塔楼第二批样本和最多 2 次 Tripo 上限
- [x] 实现可恢复的最终结构/视觉/基线比较 `review` 阶段
- [x] 增加最终复核确认、缓存、损坏状态和比较报告测试
- [x] 生成并预检狐狸、塔楼四视图；每例 Image2 最多 3 次
- [x] 最多创建 2 次 Tripo 多视图任务并完成 OBJ 后处理（实际仅狐狸 1 次）
- [x] 完成狐狸最终复核、塔楼生成前拒绝和 4 组累计对照汇总
- [x] 运行全量回归并完成架构复核
- [x] 更新 Beta 1 状态和下一批 6 例建议
- **状态：** complete

#### 阶段 51 约束

- Image2 可充分迭代，但每例本阶段最多 3 次；Tripo 生成总数最多 2 次。
- 不修改 Orca C++、3MF 或打印配置；不把 Tripo 字段加入正式客户端契约。
- 最终视觉复核必须显式确认，重复复核必须显式 `force`；结构分析和本地渲染不产生付费调用。
- 单例失败不得触发自动重做付费任务；保留状态和产物供恢复或人工审查。

#### 阶段 51 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 会话恢复脚本返回了早期架构 PPT 的陈旧未同步上下文，且终端编码错误 | 1 | 以当前阶段 50 规划文件、真实产物和 `git diff` 为准；不执行陈旧上下文中的动作 |
| 只读样本汇总再次把 PowerShell `foreach` 直接接管道 | 1 | 改为先赋值 `$rows` 后输出；同时核对塔楼真实 case ID 为 `pagoda_watchtower` 并修正文档 |
| 样本专属契约测试使用整张纯红图，先被真实四色门禁拦截，视觉复核未执行 | 1 | 将测试夹具改为每个象限包含完整四色条带；生产门禁和实现均不放宽 |
| 首次架构复核用 `rg` 扫描整个 `src` 超过 20 秒超时 | 1 | 缩小到实际 AI、Panel 和 adapter 边界文件；确认 C++ 供应商字段命中为 0 |

#### 阶段 51 验收结果

- 正式 `review` 阶段可恢复地写出结构、视觉和同源基线比较；人工拒绝会记录理由与产物哈希，并阻断同一视图继续付费生成。
- 狐狸多视图 OBJ 为 288,836 面、严格四色且零非法拓扑，自动视觉 82→83；人工五视图发现头顶悬浮圆环/横线，判定为拒绝，自动提升不计为成功。
- 塔楼在 3 次 Image2 上限内仍无法稳定保持三层离散结构，生成前拒绝，Tripo 调用为 0。
- 累计 4 组配对仅机器人、相机 2 组人工接受；固定四宫格方案未达到至少 3/4 的原验收线，不进入正式 Sidecar。
- 本阶段新增 4 次 Image2、3 次生成前视觉复核、1 次 Tripo 生成、1 次转换和 1 次最终视觉复核，无隐式重复付费。
- 最终 AI Python 全量回归 175/175、34 个脚本语法检查和 `git diff --check` 通过；C++ AI/GUI 边界无 Tripo 或多视图供应商字段。
- 详细结果见 `Docs/MULTIVIEW_CALIBRATION_WAVE2_REPORT.md`；架构复核见 `Docs/architecture/2026-08-16-phase51-calibration-review.md`。

### 阶段 52：自定义生成风格与正式用户旅程复验（2026-08-17）

- [x] 审计当前风格选择、请求契约、页面状态恢复和即时刷新链路
- [x] 冻结“预设风格 + 自定义展开输入框”的桌面交互设计
- [x] 实现自定义风格输入、校验、请求传递和页面状态保持
- [x] 增加 C++/Sidecar 定向回归，确保预设风格行为不变
- [x] 完成 Windows Release 构建并部署正式 Orca DLL
- [x] 在正式 GUI 复验输入、风格切换、图片预览、3D 生成入口、页面往返和历史恢复
- [x] 修复旅程中发现的问题并完成全量回归与架构复核
- **状态：** complete

#### 阶段 52 交互约束

- “自定义”位于现有风格选项末尾；只有选中时显示文字输入框，不增加模态弹窗或额外确认步骤。
- 切换到预设风格时隐藏但保留本次自定义文字；重新选回自定义时恢复，避免用户反复输入。
- 自定义文字为空时不能发起图片生成，并在输入区附近给出明确中文提示；预设风格流程保持兼容。
- 自定义风格属于模型生成请求，不进入 Orca 3MF、打印配置或核心模型对象。

#### 阶段 52 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 首次批量读取技能时误用当前工具集中不存在的 `shell_command` | 1 | 改用已提供的 `exec_command`；未修改项目文件 |
| 会话恢复脚本仍输出旧会话摘要且终端编码异常 | 1 | 摘要内容与阶段 51 磁盘状态一致；继续以当前规划文件、源代码和 `git diff` 为准 |
| Windows `rg` 再次收到 `AIModelGenerationClient.*` shell 通配路径并报路径语法错误 | 1 | 改为显式列出 `.cpp`、`.hpp` 与 Panel 文件；只读审计未影响代码 |
| 全量回归中旧测试用倒数位置断言 `generate_image` 参数，新增末尾字段后失败 | 1 | 改为分别断言阴影色、色板角色和空自定义风格；生产调用值正确 |
| 当前 PowerShell 未继承 CMake PATH，首个构建命令未启动编译 | 1 | 使用 Visual Studio 2022 自带 CMake 的绝对路径 |
| `/m` 并行构建触发 MSBuild 结构化输出 `OutOfMemoryException` | 1 | 降低 MSBuild 并发；源码尚未报错 |
| `/m:2` 仍因项目全局 `/MP` 并发编译大型 GUI 文件而触发页面文件不足 | 1 | 关闭本轮遗留构建节点，使用进程内 `CL=/MP1`、`/m:1`、`/nodeReuse:false` 串行构建成功 |
| 尝试用不受支持的 `MSBuild -shutdown` 关闭节点 | 1 | 仅停止父进程已不存在且命令行为 `/nodemode:1` 的 20 个本轮遗留节点；未影响其他构建 |
| 首次 GUI 键盘操作误把目标窗口对象当作实例方法调用 | 1 | 按 Computer Use API 使用 `sky.press_key({ window, key: "Return" })`，后续每次操作后重新读取状态 |
| 首次全量回归把不存在的 `tools/ai/tests` 作为发现目录 | 1 | 改为测试实际所在目录 `tools/ai`；最终 179/179 通过 |

#### 阶段 52 验收结果

- “自定义”位于风格列表末尾，选择后就地展开输入框；切到预设会隐藏但保留草稿，切回后原文恢复。
- 主体描述存在但自定义风格为空时，预览按钮保持禁用；输入自定义文字后无需失焦，顶部下一步文案和按钮立即更新。
- 正式 Image2 生成了 AI 原图、严格色板、可打印清理和问题热力图；任务正确持久化 `style=custom`、自定义文字、四色角色和打印约束。
- 页面切到“准备”再返回，以及强制关闭并重启 Orca 后，主体描述、自定义文字、图片预览和 3D 确认入口都能恢复。
- 3D 付费确认显示目标 30 万三角面；验收在确认框选择“否”，未创建新的 Tripo 任务。本阶段只新增 1 次 Image2 调用。
- AI Python 全量回归 179/179、`git diff --check`、Windows Release 完整构建通过。架构复核见 `Docs/architecture/2026-08-17-phase52-custom-style-review.md`。

### 阶段 53：生成页信息精简、自适应输入与显式图片对比（2026-08-17）

- [x] 根据正式界面截图定位左侧信息过载、固定高度文本框和右侧对比退化问题
- [x] 冻结精简信息层级与原图/处理图显式对比方案
- [x] 实现自适应文字框、默认折叠高级参数和简化中文文案
- [x] 修复图片对照模式，使原图与当前处理结果同时可见
- [x] 完成定向回归、Windows Release 构建和正式 GUI 用户旅程复验
- [x] 完成架构复核并更新阶段文档
- **状态：** complete

#### 阶段 53 约束

- 不修改 Sidecar 任务、图片产物、3MF、打印配置或 Tripo/Image2 调用逻辑；本轮复用既有任务验收，不新增付费调用。
- 首屏只展示主体描述、风格、参考图、是否限制为可打印颜色与色板摘要；角色映射和物理打印参数进入默认折叠的高级设置。
- 文字框按内容自动增长，但必须有最小/最大高度，不能挤掉固定主操作区。
- “图片对照”必须同屏显示 AI 原图与用户当前选择的处理阶段；没有参考图的文生图任务也以 AI 原图作为对照基准。

#### 阶段 53 验收结果

- 首屏仅保留“打印颜色”开关、色板来源、四色样本和摘要；颜色用途与打印尺寸归入 `高级设置（通常无需修改）`。
- 主体描述在 2–6 行、自定义风格在 2–5 行范围内随内容即时增高，恢复短文本后会立即缩回，不需要点击其他控件。
- 纯文字任务在“严格色板”“可打印清理”和“问题热力图”阶段同屏显示 `AI 原图` 与当前结果；选择 `AI 原图` 时显示单图，避免重复。
- 进入“准备”再返回后对比图仍保留；高级设置展开/收起只影响左侧滚动区域，不再改变 Orca 主窗口尺寸。
- Windows Release 的 `libslic3r_gui` 和完整 `OrcaSlicer` 构建通过，AI Python 全量回归 179/179 通过，新 DLL SHA-256 为 `FA553148AD63302C7A7E37BEA070FE696B513DE84837A72481009EB0E05F34AF`；`git diff --check` 通过。
- 本阶段复用任务 `9d038b5e-ee51-40ae-91e3-6c31b6707c24`，没有新增 Image2 或 Tripo 付费调用。架构复核见 `Docs/architecture/2026-08-17-phase53-input-comparison-review.md`。

#### 阶段 53 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| PowerShell 5 不支持用 `&&` 连接首轮只读检查命令 | 1 | 改为独立并行执行 `rg` 与 `git diff --check`，未修改文件 |
| 首次部署在强制关闭后立即复制 DLL，文件锁尚未释放，且同一命令末尾又启动旧 DLL | 1 | 再次核对精确进程路径，停止该进程并等待两秒，分步复制并比对源/目标哈希 |
| 正式 GUI 展开 `wxCollapsiblePane` 时触发框架默认顶层窗口缩放 | 1 | 增加 `wxCP_NO_TLW_RESIZE`，重新构建后确认展开/收起不再改变主窗口尺寸 |

### 阶段 54：生成页紧凑排版与明确高级设置入口（2026-08-17）

- [x] 根据用户正式截图确认松散间距和空白折叠条问题
- [x] 审计左侧 sizer 间距、控件最小高度和高级设置控件实现
- [x] 用明确文字按钮替换原生折叠控件，并统一紧凑间距
- [x] 完成 Windows Release 构建和正式 GUI 视觉复验
- [x] 更新架构复核与阶段记录
- **状态：** complete

#### 阶段 54 约束

- 保持既有字段、默认值、即时刷新、任务恢复和图片对比逻辑不变，只优化布局密度和高级设置显隐入口。
- 高级设置入口必须始终显示明确中文动作，不使用只有箭头或可能丢失文字的空白控件。
- 间距采用小/中/大三级，字段内部紧凑、分组之间可辨，不能通过压缩字体牺牲可读性。
- 不新增 Image2 或 Tripo 调用；继续复用既有任务完成正式 GUI 验收。

#### 阶段 54 验收结果

- 页面外边距从 18 DIP 收紧到 12 DIP，标题、旅程、字段、颜色区和高级参数按 4/6/8/12 DIP 层级重新排布。
- 主体描述与自定义风格继续自适应高度，并移除不必要的垂直滚动箭头；正式界面中两行内容无需额外滚动控件。
- 原生 `wxCollapsiblePane` 已替换为可访问的全宽文字按钮，默认显示 `显示高级设置`，展开后显示 `收起高级设置`，不存在空白标题条或无说明图标。
- 正式 GUI 验证默认收起、展开/收起、紧凑高级参数、窗口尺寸稳定及右侧原图/清理图保持均通过。
- Windows Release 的 `libslic3r_gui` 与完整 `OrcaSlicer` 构建通过，AI Python 全量回归 179/179 通过，`git diff --check` 通过。
- 新 DLL SHA-256 为 `2E5C8BF71D8DCEDB848BA23C37A751AD7F0B6ED8203E059FC55FD7FFC81C133D`；本轮未新增 Image2 或 Tripo 调用。架构复核见 `Docs/architecture/2026-08-17-phase54-compact-layout-review.md`。
# 阶段 55：3D 预览局部智能改色（第一版）

- [x] 建立独立的顶点色网格选区模块：射线拾取、色块/法线区域生长、局部补选与擦除
- [x] 在 3D 预览中显示高亮选区，并接入紧凑的中文局部改色交互
- [x] 将确认后的颜色安全写入新的 OBJ 副本，保持原始模型和拓扑不变
- [x] 确保改色副本进入现有预览、模型库与 Orca 导入链路
- [x] 增加算法测试，完成 Windows Release 构建与正式 GUI 用户旅程验证
- [x] 完成架构复核并更新项目状态文档
- **状态：** completed

#### 阶段 55 约束
- 第一版仅处理已有 OBJ 顶点色，不自动拆件、不改变三角面和对象几何。
- 智能选区优先使用连通色块与局部法线连续性；语义 AI 分割保留为后续可插拔增强。
- 原始 OBJ 永不覆盖；改色结果写入 `generated_models/downloads/` 下的新文件。
- 目标颜色只允许来自当前可用的 1 至 4 种耗材色。
- 不调用 Image2 或 Tripo，不修改 Orca 核心切片、3MF 或打印配置。

#### 阶段 55 验收结果
- 独立 `VertexColorRegionEditor` 已实现三角面邻接、射线拾取、颜色/法线区域生长以及局部补选、擦除；UI 只负责把点击模式和范围参数传给该模块。
- 3D 预览内的选区使用青色覆盖层即时高亮；真实 290,438 面 OBJ 上，智能选区得到 728 个三角面，擦除后降为 611 个三角面。
- 应用耗材 1 红色后生成新的 16.1 MB 顶点色 OBJ，元数据记录 290,438 个三角面、目标耗材色及 `source=local_recolor`；原 OBJ 保留且哈希未变化。
- 改色结果自动回到 3D 预览并进入历史模型；自然色源模型继续标为自然颜色，已受四色约束的源模型继续显示“4 种可打印颜色”。
- 最终正式 GUI 复验以自然色历史模型 `0c85a1eb` 选中 36,472 个躯干三角面并改为耗材 1 红色；新历史记录显示“自然颜色”，权威产物为 `generated_models/downloads/orcaslicer-ai-edit-67a3a299-cb87-46d5-914c-88dfea183a1a.obj`。其 JSON 为 `palette=[]`、`use_printable_colors=false`、`recolor_target=#FF0000`、`preserves_unselected_vertex_colors=true`。
- Windows Release 的 `libslic3r_gui` 与完整 `OrcaSlicer` 构建通过，正式 DLL 已部署，SHA-256 为 `0AE8401808352DD22479B11B428DCCA2B6088AA26A45D483D9E1836E0F43EB6F`，`git diff --check` 通过。
- 已增加算法与 OBJ 保真测试源码；当前主构建配置的 `BUILD_TESTING`、`BUILD_TESTS` 均为 `OFF`，因此测试目标未生成，本轮以 Release 构建和真实 GUI 旅程完成运行验证。
- 正式启动仍必须通过启动器显式设置 `ORCASLICER_AI_OUTPUT_DIR`；直接启动 exe 会继承 Orca 改写后的工作目录。验证产物已迁回项目 `generated_models/downloads/`。
- 本轮未调用 Image2 或 Tripo。架构复核见 `Docs/architecture/2026-08-17-phase55-local-recolor-review.md`。

### 阶段 56：局部改色颜色一致性修复（2026-08-18）

- [x] 根据用户截图确认问题发生在应用改色后的重新加载/显示阶段
- [x] 复现绿色目标色并核对 UI、OBJ 文件、Orca 解析和 GL 预览四段颜色值
- [x] 增加覆盖多通道颜色的回归测试并修复根因
- [x] 完成 Windows Release 构建、部署和正式 GUI 复验
- [x] 更新架构复核与状态文档
- **状态：** complete

#### 阶段 56 约束

- 修复必须保持标准 OBJ 顶点色 RGB 顺序，不能为单一预览路径引入非标准 BGR 文件。
- 必须同时验证红、绿、蓝至少三种非对称颜色，避免只修复用户当前绿色案例。
- 不修改选区算法、模型拓扑、Orca 核心切片、3MF 或打印配置。
- 不调用 Image2 或 Tripo；复用现有历史模型完成验证。

#### 阶段 56 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| PowerShell 直接把 `foreach` 语句接到管道产生空管道解析错误 | 1 | 先将结果赋给数组变量，再传给 `Format-Table` |

#### 阶段 56 验收结果

- 绿色改色没有发生 RGB/BGR 通道交换：正式复现产物的 JSON 目标为 `#00FF00`，OBJ 顶点为标准 RGB `0.000000 1.000000 0.000000 1.000000`，Orca 重新加载后仍为绿色。
- 根因是选区层固定使用不透明青色且模型重载后没有刷新零选区摘要；现改为选区高亮即时跟随目标耗材色，并在每次模型加载成功后统一通知选区状态。
- 正式 GUI 以绿色目标选中 41,605 个三角面：高亮立即呈绿色；点击“应用改色”后模型保持绿色、摘要恢复“尚未选择区域”、应用按钮禁用。
- 最终权威产物为 `generated_models/downloads/orcaslicer-ai-edit-f9c868be-b3be-4790-ad95-1e95497e8cbc.obj`，SHA-256 为 `42AA459441B6CC91A23BE308102F9B3248FD0BC7BC0F4574476152E1A9D6D6E9`；同名 JSON 记录目标色 `#00FF00`、290,438 个三角面和 23 种实际顶点色。
- 新增红、绿、蓝三通道 OBJ 写出并经 Orca `load_obj` 重新读取的往返测试源码；当前主构建 `BUILD_TESTING=OFF`、`BUILD_TESTS=OFF`，所以测试目标未生成，正式验收由 Release 构建和真实 GUI 旅程补足。
- Windows Release 的 `libslic3r_gui` 与完整 `OrcaSlicer` 构建通过，正式 DLL 已部署，源/目标 SHA-256 均为 `E2095733DECAE8CA0078E0EB400ADA87B47DC4D81EC2BFDA149A0AEB6C73C4AB`；`git diff --check` 通过。
- 本阶段未调用 Image2 或 Tripo。架构复核见 `Docs/architecture/2026-08-18-phase56-recolor-color-consistency-review.md`。

### 阶段 57：GitHub 提交前仓库整理（2026-08-18）

- [x] 恢复当前规划并确认真实分支、基线与工作树范围
- [x] 区分应提交源码/测试/文档、本地过程状态与生成产物
- [x] 检查密钥、大文件、调试代码、Mock 残留和文件依赖完整性
- [x] 完成安全精简、忽略规则和提交说明整理
- [x] 运行差异检查、AI 回归与 Windows Release 定向构建
- [x] 输出可提交文件清单、建议提交边界和剩余人工决定
- **状态：** complete

#### 阶段 57 约束

- 不执行 `git add`、`git commit`、`git push` 或远端变更，除非用户后续明确要求。
- 不删除 `generated_models/`、构建目录、模型资产或历史基准；仅用忽略规则隔离本地过程文件。
- 不回退或覆盖现有脏工作树；精简必须保持阶段 52–56 已验证功能和 Orca 原版兼容边界。
- 密钥审计只报告文件与风险类型，不在日志或文档中输出秘密值。

#### 阶段 57 初步发现

- 当前分支为 `codex/model-generation`，HEAD `ca5de31dff`；工作树含 21 个已跟踪文件修改和 59 个未跟踪文件。
- 未跟踪文件合计约 0.36 MiB，均为源码、测试、设计/架构文档和 `.planning/` 本地状态，没有未忽略的 OBJ、图片、安装包或构建产物。
- 根目录 `real-tripo-text.3mf` 为既有已跟踪文件且约 27.76 MiB，不属于本轮新改动；是否从 Git 历史后续移除需要单独决定。

#### 阶段 57 错误记录

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 会话恢复脚本再次返回早期架构 PPT 的陈旧上下文且终端编码异常 | 1 | 忽略陈旧指令，以阶段 56 磁盘记录、当前分支与实际 `git diff` 为准 |
| 两个 PowerShell 只读汇总再次把 `foreach` 结果直接接到管道，触发空管道解析错误 | 2 | 将对象先赋给 `$rows` 再格式化；错误只影响统计输出，没有修改文件 |

#### 阶段 57 验收结果

- `.gitignore` 已新增 `/.planning/`；9 个本地规划状态文件不再出现在待提交清单，`generated_models/`、`build*`、`output/`、本机配置和 API Key 仍保持忽略。
- README 已同步真实模型生成目录、正式 Sidecar/测试 Mock 边界、启动入口、配置模板、当前能力文档和提交前校验命令。
- 待提交工作树为 23 个已跟踪修改条目和 48 个未跟踪条目，按目录汇总为 Docs 25、`tools/ai` 28、src 8、tests 2、打包脚本 2、根级/其他 6；没有未忽略的模型、图片、安装包或构建产物。
- 密钥扫描未发现真实 OpenAI/Tripo Token；唯一命中为契约测试中的 `test-openai`/`test-tripo`。正式运行 Mock 不进入发布白名单，仍保留为无付费协议回归夹具。
- AI Python 全量回归 179/179 通过，35 个 Python 文件 `py_compile` 通过，29 份变更 Markdown 本地链接检查通过，1 个未跟踪基准 JSON 可解析，`git diff --check` 通过。
- Windows Release 完整 `OrcaSlicer` 构建通过，新构建 DLL SHA-256 为 `239A0A6DE9036DFB32E3C1BF3DDB9AE79EB85C8781B8E456B60E9D64929D58E5`；仅有 Orca 上游既有 C4172/C4005/C4101/LNK4098 警告。
- 当前 `codex/model-generation` 与 `origin/codex/ai-integration-20260814` 基线提交一致且没有 upstream tracking；用户提交后首次推送应使用 `git push -u origin codex/model-generation`。
- 本阶段未执行暂存、提交或推送，也未调用 Image2 或 Tripo。`real-tripo-text.3mf` 为历史已跟踪 27.76 MiB 文件，是否另行清理不阻塞本次提交。
