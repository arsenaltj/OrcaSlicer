# 阶段 70：采样式局部厚度预检实施计划

日期：2026-08-23

1. 新增失败测试：带 0.4 mm 附着细颈的单一闭合流形组件触发局部薄壁，而组件整体厚度正常；普通方盒保持通过；开放模型指标未知。
2. 实现 Morton 排序 BVH、短距离射线/三角面相交和确定性有上限采样。
3. 输出 `structural-v6` thresholds、metrics 和 review warning，避免与整体薄组件重复提示。
4. 扩展 `AIModelGenerationClient` 可选字段解析，并在质量卡显示最小采样局部厚度和中文建议。
5. 运行质量门、Sidecar 契约、全量离线测试、真实 29 万面模型性能复核和 Windows Release 构建。
6. 使用仓库本地应用、本地 Sidecar和合成样本完成 GUI 验收，记录、提交并保持工作树干净。
