# 任务计划：模型生成 v2 六通道颜色能力

## 目标
在 `codex/model-generation-v2` 上把模型生成升级为兼容 1～6 个物理通道、可表达离散色与叠色意图的独立模块，并彻底移除模型生成触发自动切片的职责。

## 当前阶段
阶段 10：回归、真实构建与交付

## 各阶段

### 阶段 1：分支基线迁移
- [x] 获取远端引用并核对分支关系
- [x] 确认集成线已接收旧功能线最新提交
- [x] 从本地集成线创建 `codex/model-generation-v2`
- **状态：** complete

### 阶段 2：历史资料筛选
- [x] 比较旧功能线与 v2 基线中的文档
- [x] 保留完整发现、进度和路线图快照
- [x] 选择长期有效的产品、质量和架构资料
- [x] 排除临时产物与重复阶段稿
- **状态：** complete

### 阶段 3：冻结归档
- [x] 将历史资料集中到 `Docs/history/model-generation-v1/`
- [x] 写入来源 SHA、集成回执和使用边界
- [x] 链接集成线已经包含的两份质量报告，避免重复
- **状态：** complete

### 阶段 4：验证与提交
- [x] 验证归档内容、链接和暂存范围
- [x] 提交迁移结果
- [x] 确认当前分支与工作区状态
- **状态：** complete

### 阶段 5：架构与实施计划
- [x] 复核 v2 分支、Orca 原生混色能力与模型生成现状
- [x] 写入六通道颜色契约设计与 ADR
- [x] 写入逐任务实施计划和验收矩阵
- **状态：** complete

### 阶段 6：移除模型生成自动切片
- [x] 先修改契约和架构测试，证明模型生成不再携带或触发切片状态
- [x] 删除模型生成面板的“导入后自动切片”控件和状态
- [x] 删除 OrcaWorkspaceAdapter 的切片配置写入与布尔导航
- [x] 验证模型生成导入仅停留在准备工作区
- **状态：** complete

### 阶段 7：1～6 色动态化
- [x] 建立共享的颜色数量策略和类型化能力契约，允许 1～6 色
- [x] 去除 Python sidecar、推荐、图像处理和视觉质检通用链路中的固定四元素结构
- [x] 保持未声明颜色数的旧 Python 请求默认四色，并兼容旧持久化任务
- [x] 将 C++ 面板与客户端改为请求驱动的 1～6 色动态结构
- **状态：** complete

### 阶段 8：类型化颜色能力与意图
- [x] 定义离散耗材模式与过程混色模式
- [x] 定义物理通道、虚拟配方和模型目标色类型
- [x] 为 GeneratedModelArtifact 增加可选的版本化颜色意图清单引用
- [x] 从 Orca 配置填充类型化物理通道与已有虚拟配方能力
- [x] 生成并消费实际颜色意图清单
- **状态：** complete

### 阶段 8A：形体参考与打印配色解耦
- [x] 让付费 3D 使用连续色调、打印友好但不做精确色板约束的形体参考
- [x] 保留精确 1～6 色预览作为独立的材质确认结果
- [x] 将写实人像身份优先路径从固定四色推广到 1～6 色，保持旧任务恢复兼容
- [x] 生成同时包含连续目标色意图与 1～6 色兼容回退的版本化清单
- [x] 更新界面文案，明确区分“造型参考”“配色草图”和 3D 后的“可打印预览”
- **状态：** complete

### 阶段 9：模型颜色意图交接
- [x] 验证 OBJ 的 1～6 色兼容回退与颜色意图清单一致
- [x] 由 Orca 导入边界校验并保留清单引用，不在模型生成分支求解层叠配方
- [x] 为智能切片分支提供稳定的目标色、语义区域和物理通道能力输入
- **状态：** complete

### 阶段 9A：打印原生艺术预设
- [x] 增加身份优先的“肖像速写”，用 2～5 个大体块和少量可建模线条概括人物
- [x] 增加带实体背板的“水墨版画浮雕”，用 2～4 个大明度块和正负形表达画面
- [x] 两种预设继续使用连续色调造型参考，并在造型之后收敛到用户确认的 1～6 色可打印子集
- [x] 接入本地风格推荐、sidecar 能力、原生客户端、下拉映射和质量基准
- [x] 覆盖身份保持、提示词边界、1～6 色、健康契约和原生映射回归
- **状态：** complete

### 阶段 10：回归、真实构建与交付
- [x] 覆盖 1、2、3、4、5、6 通道测试矩阵
- [x] 验证旧四色情况、3MF/profile 和 Orca 默认行为不回归
- [ ] 完成 Windows 构建、GUI 验收与交付记录
- **状态：** in_progress

## 已做决策

| 决策 | 理由 |
|------|------|
| 旧功能分支保持冻结，不反向合并集成线 | 避免无关历史合并和双向依赖 |
| 新开发线直接基于最新 `codex/orca-integration-v2` | 保持共同祖先，便于后续集成 |
| 历史资料放入独立归档目录 | 防止旧计划被误认为当前规范 |
| 当前计划文件保持精简 | 完整历史已经在归档快照中 |
| 模型生成导入完成后只进入准备工作区 | 智能切片由独立分支负责，模型生成不再持有切片策略 |
| 六通道表示 1～6 个物理基色通道池 | 与 Orca 当前成熟的 1～3 分量稀疏配方能力对齐 |
| 采用类型化颜色契约并复用 Orca 混色引擎 | 避免把物理槽、虚拟配方和 RGB 目标色混为一谈，也避免重复颜色算法 |
| 先保留 OBJ，再增加版本化颜色意图清单 | 最小化格式迁移风险，3MF 仍由 Orca 项目层负责 |
| 精确色板约束后移到 3D 表面颜色阶段 | 保留连续明暗与身份几何线索，同时继续提供可打印的 1～6 色结果 |
| 模型生成只发布目标颜色意图和兼容回退 | CMYK/六通道层叠配方与切片决策属于独立智能切片分支 |
| 艺术预设允许只使用所选色板中的有意义子集 | 不为凑满颜色制造微小、虚假或难打印的色区 |
| 肖像速写与水墨版画浮雕是造型策略，不是滤镜贴图 | 抽象必须转化为可建模体块、凹凸线条和实体留白 |

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 两份报告因 `Docs/` 与 `docs/` 大小写差异看似缺失 | 1 | 比较 Git blob，确认内容相同并链接现有小写路径 |
| 首次批量移动脚本变量名拼写错误，三份 ADR 未移动 | 1 | 检查暂存状态后逐项补移并重新校验 |
| 首次追加六通道计划时补丁假设存在“备注”段 | 1 | 按实际文件结构拆分补丁并继续，未产生部分写入 |
| 当前 PowerShell 找不到 `cmake` | 1 | 使用已有构建树/Visual Studio 对应的 CMake 绝对路径，不修改系统环境 |
| 现有构建树经 CMake 和 VS Build 两条路径均因捆绑 Python 3.12.13 不完整而重新配置失败 | 2 | 首个里程碑调用 MSBuild 编译/链接内部目标绕过 CustomBuild；完整验证阶段单独恢复依赖 |
| `build-ai-tests` 是旧工程且缺少当前 wx inspector 头文件 | 1 | 不用它做完整 GUI 编译；改查主构建树/实际依赖并进行改动单元验证 |
| 手工绕过 PCH 编译完整 GUI 单元时本地化宏未按工程顺序注入 | 2 | 不用该结果判断源码失败；契约单元已直接编译，完整 GUI 编译留待依赖/PCH 构建树恢复后执行 |
| 适配器独立编译首次使用相对强制包含路径、再次遗漏 UTF-8 源码选项 | 2 | 改为绝对 PCH 路径并加入 `/utf-8`，同一生产源文件随后编译通过 |
| 独立 Catch2 测试首次触发 Boost 自动链接名称不匹配，随后经 `cl` 包装链接未找到入口 | 2 | 编译时使用工程已有的 `BOOST_ALL_NO_LIB`，再直接调用 MSVC linker 链接既有 Catch2 库；测试可执行文件通过 |
| Python 全量测试首次触发 sidecar 9450 行架构预算 | 1 | 将颜色数量归一化下沉到共享调色板策略；sidecar 回到预算上限，两个守卫复测通过 |
| C++ 面板动态化后超过既有 5200 行架构预算 3 行 | 1 | 压缩本次新增控件构造代码，不提高预算；架构守卫复测通过 |
| 直接调用旧 `build-ai-tests` 的全量 `ClCompile` 再次触发缺失 wx inspector 头文件 | 1 | 立即停止全量目标，改用主构建树 PCH、原编译参数和已有测试桩只编译本次面板源文件；编译通过 |
| PowerShell 中组合搜索肖像激活字段时正则被解析成未闭合分组 | 1 | 不重复该表达式；改用多个固定 `-e` 模式搜索 |
| 记录上述搜索错误时补丁误用了 `progress.md` 中的长文本作为 `task_plan.md` 上下文 | 1 | 读取两个文件实际尾部后分别使用精确短上下文补写，未产生部分写入 |
| 按惯例读取不存在的 `tools/ai/test_model_job_support.py` | 1 | 不创建重复测试文件；现有 `generation_prompt` 覆盖位于 `test_preprocess_fallback.py`，后续在该处扩展 |
| 同时更新设计与 ADR 的补丁假设 ADR 使用单段决策文本 | 1 | 实际 ADR 为编号列表；拆分文件并按真实段落逐项修改，第一次补丁未写入 |
| Task 15 首次定向红测命令引用了两个不存在的 unittest 类名 | 1 | 改为运行实际测试模块，红灯与转绿结果均按模块统计 |
| 复用生产 PCH 编译命令时改写 `/Fd` 导致 C2859 | 1 | 保留生产 PCH 对应 PDB，仅替换独立对象输出 `/Fo` |
| 原生 presentation 测试首次缺 Catch2 配置头，随后链接带入 PCH/TBB 符号 | 2 | 补齐测试 include，并对测试与纯 Core 使用无 PCH 编译；最终 145 个断言通过 |
| 一次性更新三份跟踪文档时引用了错误的表格上下文 | 1 | 确认补丁未部分写入，随后按文件拆分并使用实际短上下文 |
| 艺术预设子色板提示首次拼接遗漏 `+` 并触发 SyntaxError | 1 | 补齐连接符后，定向 55 项与全量 630 项 Python 测试通过 |

## 历史入口

- [模型生成 v1 历史上下文](Docs/history/model-generation-v1/README.md)

## 范围说明

- 当前开发范围仅限模型生成；智能切片、硬件控制和供应商协议明确不在本计划内。
