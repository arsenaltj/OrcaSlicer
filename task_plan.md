# 任务计划：OrcaSlicer AI 能力路线图

## 目标
评估模型生成、智能切片、易用性、软件架构以及账号计费五大领域的当前状态，形成 provider-agnostic、兼容 OrcaSlicer 演进的目标架构、依赖关系和可执行分期计划。

## 当前阶段
阶段 38 已完成：双机协作 Git 基线审核、精简与发布。

### 阶段 38：双机协作 Git 基线审核、精简与发布
- [x] 清点全部已修改和未跟踪文件，划分正式源码、测试、文档、本机配置与过程产物
- [x] 审计待提交内容中的密钥、个人路径、大文件、Mock/旧入口和明显冗余
- [x] 精简正式代码与提交集合，保留用户本地过程文件但不纳入 Git
- [x] 在根 README 增加双人职责、模块边界、分支和同步流程
- [x] 完成 Python/C++ 定向回归、Release 链接和提交前安全检查
- [x] 创建并推送共享集成分支，验证远端 commit 与本地一致
- **状态：** complete

#### 阶段 38 约束
- 不删除本地模型、截图、构建产物或历史过程文件，只控制 Git 提交范围。
- 不提交 API Key、用户配置、Codex/Claude 会话数据、运行日志或大体积生成产物。
- 不直接推送 `master`；共享基线使用独立集成分支，双方功能从该分支派生。
- 保留当前业务行为与 Orca 解耦边界；本轮只做审核、必要精简、协作文档和版本发布。

#### 阶段 38 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 恢复脚本再次返回旧 PPT 会话上下文且控制台乱码 | 1 | 将其视为过期恢复数据，不执行其中动作，以当前工作树和阶段 37 结果为准 |
| 误把工具输出层乱码判断为规划文件磁盘编码损坏 | 1 | 检查原始字节前缀确认文件仍为正确 UTF-8，不执行编码重写 |
| PowerShell 把 `foreach` 代码块直接接管道导致解析失败 | 2 | 改为 `$rows = foreach (...) { ... }` 后再单独接管道；后续所有循环输出固定采用数组形式 |
| 枚举约 30,276 个 untracked 文件并逐个取大小超过 20 秒，且 Git 转义中文路径无法直接传给 `Test-Path` | 1 | 改用 `core.quotepath=false` 的目录级 status 与顶层目录统计；不再逐文件扫描全部输出包 |
| 本机未安装 `ruff`、`pyflakes`、`cppcheck`、`clang-tidy` 或 `gitleaks` | 1 | 不临时安装或改变依赖；使用 AST 重复定义/函数规模审计、定向安全搜索、编译器和现有测试替代 |
| 一次从仓库根递归搜索打包脚本引用超过 20 秒 | 1 | 缩小到 `packaging/`、`scripts/`、`README.md` 和 `Docs/` 明确范围后立即完成 |
| Windows `rg` 再次收到 `tools/ai/test_*.py` 字面通配路径并返回失败 | 1 | 使用 `rg -g 'test_*.py' ... tools/ai` 的工具内 glob，不再依赖 shell 展开 |
| PowerShell 双引号字符串中的 `$file:` 被解析为无效变量名 | 2 | 使用 `${file}:...` 明确变量边界；后续带冒号的插值统一加花括号 |
| 用 `rg` 精确匹配空 Key 批处理行时引号经 PowerShell 传递后未命中 | 1 | 改用 `Get-Content` 与字符串精确相等检查，两个 Key 均确认为空 |
| 首次批量清理文件末尾空行的补丁误用了 `04-stop-ai.bat` 结束行上下文 | 1 | 读取六个文件的精确尾部后重新应用补丁 |
| AI 暂存内容检查发现 6 个新文件末尾多余空白行 | 1 | 清理批处理和契约头文件末尾空行，重新暂存后 `git diff --cached --check` 通过 |
| 文档暂存检查发现两处 Markdown 尾随空格和三个文件末尾空白行 | 1 | 用空引用行替代 Markdown 双空格换行，并删除末尾空白行 |

### 阶段 37：模型生成线与 Orca 第一批等价解耦
- [x] 冻结 `GeneratedModelArtifact`、导入请求/结果和可打印色板契约
- [x] 建立模型制品消费端口与 Orca 工作区适配器
- [x] 将 OBJ 导入、颜色映射、网格修复和自动切片编排迁出 `ModelGenerationPanel`
- [x] 让 `ModelGenerationPanel` 不再直接包含或持有 `Plater`、Preset、CGAL 修复依赖
- [x] 保持 MainFrame 为薄组合根，并登记新增源文件
- [x] 完成契约/适配静态边界检查、Windows Release 链接和现有 AI 回归
- **状态：** complete

#### 阶段 37 约束
- 本批只做等价抽取，不改变当前用户交互、颜色语义、修复策略和自动切片默认值。
- 保护当前大量未提交改动，不创建会丢失现状的独立 worktree，不执行 reset/checkout。
- 不升级 Orca 上游，不拆多个进程或仓库，不调用收费 API。
- Windows Release 为交付构建门禁；领域契约不得依赖 wx、`Plater` 或供应商 SDK。

#### 阶段 37 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `session-catchup.py` 返回另一会话的旧架构图上下文且控制台编码异常 | 1 | 只作为恢复数据记录，以当前 Git diff、阶段 36 文档和用户最新指令为准，不执行其中动作 |
| 一次 `apply_patch` 把 `progress.md` 的上下文误放入 `task_plan.md` 补丁，导致校验失败 | 1 | 拆成两个独立补丁并使用各自文件中的精确上下文 |
| 将成功的 `git diff --check` 与预期无匹配的 `rg` 串在同一命令，整体因 `rg=1` 被标为失败 | 1 | 后续把空白检查与“预期零匹配”检查分开解释，不将零匹配当成代码错误 |
| PowerShell 查找 `job_id` 行返回两处结果，数组参与减法导致范围读取失败 | 1 | 改用唯一函数边界和字符串索引，不依赖非唯一行模式 |
| 首次从完整文件工具输出提取函数块时受包装/截断影响，未找到结束标记 | 1 | 让 PowerShell 只输出目标函数子串，再由 `apply_patch` 完成替换 |
| 当前 PowerShell `PATH` 不包含 `cmake` | 1 | 使用 Visual Studio 2022 自带 CMake/MSBuild 的绝对路径，不修改用户环境 |
| 短超时构建退出后 MSBuild 子进程仍在后台运行，第二个构建与其争用对象文件 | 1 | 核对完整命令行后只停止本轮两组精确 PID；后续不并发启动同一工程构建 |
| 新 adapter 沿用 GUI 根目录本地 include，单文件编译找不到 `GUI_App.hpp` | 1 | 改用从 `src` include root 锚定的 `slic3r/GUI/...` 路径 |
| Panel 删除 `Plater.hpp` 后暴露 `glsafe` 过去依靠间接 include | 1 | 显式包含定义该宏的 `3DScene.hpp`，消除隐式依赖 |
| `SelectedFiles` 使用相对路径时 MSBuild 未编译目标文件 | 1 | 使用绝对源码路径分别编译 adapter、Panel 和 MainFrame |
| `/t:Build` 因跟踪状态准备重编 338 个未改动 GUI 文件并超过短验证窗口 | 1 | 停止精确构建 PID，改用 `_Lib` 基于现有 362 个对象归档，再用 `_Link` 完整链接 DLL |
| 两次 PowerShell/`rg` 组合正则引号解析失败 | 2 | 放弃复杂引号表达式，改用简单固定模式查找 MSBuild `_Lib`/`_Link` 目标 |

### 阶段 36：模型生成与智能切片双主线解耦架构
- [x] 盘点当前 AI 代码、Orca 核心改动和跨模块依赖
- [x] 比较可选解耦方案并确定推荐边界
- [x] 设计模型生成、智能切片、Orca 适配层和 sidecar 契约
- [x] 定义两名开发者的目录所有权、接口先行和合并规则
- [x] 制定上游 Orca 版本同步、兼容测试和迁移路线
- [x] 输出架构设计文档与关键 ADR
- **状态：** completed

#### 阶段 36 约束
- 当前只规划“模型生成”和“智能切片”两条主线；交互重构、账号、计费暂不进入实现范围。
- 保持 `.3mf`、打印机配置和 Orca 原生切片行为向后兼容。
- 优先减少对 Orca 高频变更文件的直接修改，确保后续可持续同步上游版本。
- 本阶段只做代码审计和架构文档，不改业务行为。

#### 阶段 36 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| PowerShell 将 `foreach` 代码块直接接管道时报空管道元素 | 1 | 先把结果收集到数组，再统一 `Format-Table` 输出 |

### 阶段 35：正式模型生成任务持久化与重启恢复
- [x] 审计正式 sidecar、GUI、模型库和 Windows 打包入口的恢复断点
- [x] 原子保存文字、参考图、风格预览、色板、质量档、任务状态和 Tripo task ID
- [x] sidecar 启动时恢复等待确认、生成中与已完成任务
- [x] 恢复生成中任务时复用已保存 generation/conversion ID，禁止自动重复付费
- [x] 新增最近任务接口，生成页自动恢复输入、预览和最终 OBJ
- [x] 删除任务时移除任务清单但保留用户生成产物
- [x] 完成 AI 全量回归、`git diff --check` 和 Windows Release 构建
- [x] 使用隔离正式 sidecar 执行一次收费 3D 生成，并验证两次重启均复用原 generation/conversion ID
- [x] 验收恢复后的真实 OBJ：95,338 面、2 个连通部件、0 个异常边、顶点色有效
- [x] 在当前未保存 Orca 正常关闭后部署正式 sidecar/DLL，并完成正式 GUI 恢复验收
- **3D 付费预算：** 1/10（本阶段新增；无需逐次确认）
- **状态：** complete

#### 阶段 35 约束
- Windows 正式版优先，macOS/Linux 本轮不处理。
- 不使用 mock 作为功能或 GUI 验收结论。
- 模糊状态（远端 task ID 尚未可靠落盘）不得自动创建第二个付费任务，必须由用户重新发起。
- 任务清单和所有过程/最终产物继续保存在项目 `generated_models/`。

#### 阶段 35 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 大范围并行状态扫描超过工具等待时间 | 1 | 缩小到正式链路入口、sidecar 状态机与定向测试分段审计 |
| 当前 PowerShell `PATH` 不含 `python.exe` | 1 | 使用 Codex 工作区依赖中的 Python 绝对路径运行回归，不安装新依赖 |
| `_download_conversion` 新增默认参数导致两个旧 mock 断言失败 | 2 | 非恢复路径保持原四参数调用；只有恢复路径显式传入 `resume=true`，80 项回归全过 |
| 恢复图片时原图和 AI 预览争用单一 HTTP 请求句柄 | 1 | 串行恢复原图，再下载风格预览；原图失败时直接继续预览恢复 |
| Windows Release 构建超过工具短等待窗口 | 3 | 不重复构建，持续跟踪同一 `cmake/MSBuild/cl/link` 进程直到链接完成 |
| 本机代理把 Tripo 官方 CDN 解析为保留地址，安全校验误判产物 URL | 1 | 仅精确允许 HTTPS 官方主机 `openapi.cdn.tripo3d.com`，继续拒绝相似域名、凭据 URL 和 HTTP，并增加安全回归 |
| 10 万面档实际返回 95,338 面，被旧的精确下限误判失败 | 1 | 按每个所选档位独立执行 90%–125% 容差，不设置跨档位统一最低面数 |
| 恢复下载向已有 `package` 目录重复解压，返回无效 OBJ 包 | 1 | 优先复用并重新校验本地完整 OBJ；损坏时改用新的 `recovery-XX` 工作目录，保留原过程文件 |
| 停止旧正式 sidecar 后，另一遗留启动器立即接管 18764 | 1 | 枚举并核对端口子进程与项目批处理父进程，只精确停止确认属于本项目的 PID 对 |
| Orca 在 sidecar 重建索引前缓存了旧 job ID | 1 | 确认新窗口无未保存内容后正常重启 Orca，使生成面板在初始化时读取新的最新任务 |

### 阶段 34：Windows AI 同事测试包

### 阶段 34：Windows AI 同事测试包
- [x] 核对正式 Orca 运行时、AI sidecar、Python/Pillow 与外部 API 环境变量边界
- [x] 生成不含真实密钥的交互式 AI 配置、环境检查、启动和停止脚本
- [x] 排除旧 DLL、Mock、测试脚本、付费验证脚本、历史模型和开发目录
- [x] 组装并压缩 Windows x64 测试包，生成 SHA256 校验文件
- [x] 验证 DLL 哈希、包内容、批处理 CRLF、Python/Pillow 和隔离 sidecar `/health`
- [x] 确认验证期间未创建图片或 Tripo 付费任务
- **状态：** complete

#### 阶段 34 交付
- `output/packages/OrcaSlicer-AI-Windows-x64-20260813-demo3-simple-config.zip`
- SHA256：`3E9DFCDAF875D43524D5DD99F1DEF24093B1EB63063DDEBB49D72B784E655EAB`
- 可复用打包入口：`scripts/package_windows_ai_test.ps1`
- 同事只需编辑 `setup/ai-config.bat` 中的两个 API Key；ZIP 解压后无额外同名内层目录。

#### 阶段 34 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| Windows `Compress-Archive` 读取资源 SVG 时遇到短暂文件占用 | 1 | 不重复同一后端，改用系统 `tar.exe -a` 创建 ZIP，并将稳定路径固化到打包脚本 |
| Windows PowerShell 5 将无 BOM 中文 `.ps1` 按系统代码页解析 | 1 | PowerShell 交互提示改为 ASCII 英文，中文使用说明保留在 UTF-8 文本中 |
| 首版环境检查把启动器已有默认值误判为必填 | 1 | 检查脚本与正式启动器共用相同的 Base URL 和模型默认值，只要求两个 API Key |

阶段 33 代码、构建和部署已完成；真实耗材颜色最终端到端验收仍应在下一轮演示彩排中复核。

### 阶段 33：导入颜色与真实耗材匹配修复
- [x] 根据截图定位“显示已映射但模型/打印机颜色不一致”的根因
- [x] 将导入颜色策略改为手动匹配、自动匹配和单色三个显式选项
- [x] 正常导入恢复 Orca 原生颜色匹配窗口并校验用户确认结果
- [x] 禁止自然色导入覆盖打印机当前耗材色块
- [x] 取消或映射退化时停止自动切片并显示准确状态
- [x] 完成 Windows Release 构建
- [ ] 当前未保存的正式 Orca 关闭后部署，并用真实 OBJ 完成 GUI 验证
- **状态：** in_progress（等待安全部署与 GUI 验证）

#### 阶段 33 约束
- “使用可打印颜色”只决定 AI 图片/模型生成约束，不再隐式决定 OBJ 导入策略。
- 打印机耗材色块始终代表当前真实配置，不得被模型自然色静默覆盖。
- 不创建新的图片或 Tripo 付费任务，复用项目内已有真实彩色 OBJ 验证。

#### 阶段 33 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 首次构建调用超过工具短等待窗口，无法取得最终退出码 | 1 | 确认残留构建进程退出后重新运行同一单并发目标，取得明确成功输出 |
| 当前 Windows 构建树没有 `libslic3r_tests` 项目且 `ctest -N` 为 0 项 | 1 | 不重复错误目标；以完整 Release 编译、链接和静态分支检查作为本轮验证，记录测试配置缺口 |
| 隔离运行副本被 Orca 实例检查拒绝启动；首次还误用了不存在的 `--single-instance-off` | 2 | 核对源码后改用官方 `--no-single-instance` 仍在初始化前退出；不继续改共享配置，不操作未保存的正式窗口，待用户正常关闭后部署 |

### 阶段 32：可选导入后自动切片
- [x] 明确开关位置、默认行为与手动处理门禁
- [x] 在 3D 生成页增加默认开启的“导入后自动切片”开关
- [x] 关闭时保留导入、颜色映射、网格检查/修复与自动摆放，并停在准备页
- [x] 让生成结果与模型库导入共同遵循该开关
- [x] 完成 Windows Release 构建与两条分支验证
- **状态：** complete

### 阶段 31：正常导入颜色保真修复
- [x] 复现并区分单对象结构与单耗材颜色问题
- [x] 定位正常导入错误复用温度兼容耗材槽映射
- [x] 将正常导入改为全部有效项目颜色映射，严格可打印模式继续使用兼容槽位
- [x] 检测多色源退化为单色并进入手动上色，不再误报保色成功
- [x] 同步 OBJ 主色到项目耗材色块并完成 Windows Release、真实 OBJ GUI 复验
- **状态：** complete

### 阶段 29：高精度生成、OBJ 结构分组与颜色保真
- [x] 核对低模参数、20,000 面门禁和现有顶点色 OBJ
- [x] 确认 10 万/30 万/50 万/100 万四档，默认 30 万面
- [x] 撤销单对象/单连通体要求，确认保留对象与材质分组
- [x] 输出高精度 OBJ 颜色与分组设计
- [x] 实现 GUI 质量选择与请求契约
- [x] 实现 Tripo 高质量生成参数和 100 万面处理上限
- [x] 保留对象/组/材质边界并提高自然色烘焙精度
- [x] 补充 Python/C++ 回归并完成 Windows Release 构建
- [x] 将付费 Tripo 端到端验收转入阶段 30 的完整用户流程
- **状态：** complete

### 阶段 30：质量优先的付费黄金路径验收
- [x] 关闭打印耗材颜色约束，使用同一参考图生成 Q 版卡通、低多边形、雕塑三种真实风格预览
- [x] 确保三种预览不新增人物、物件、文字或背景装饰，并通过完整主体与可成模门禁
- [x] 选择最佳预览生成 30 万面彩色 OBJ，必要时升级 50 万或 100 万面，总计不超过 20 次 Tripo 生成
- [x] 验证 OBJ 颜色、对象/材质结构、流体性、非流体边和 Orca 导入结果
- [x] 在准备/预览页面实现长期可见的自动流程状态组件
- [x] 按用户流程完成自动导入、摆盘、切片和 G-code 预览
- [x] 对比风格预览、3D 渲染和最终切片结果，记录差异并针对主要偏差迭代
- [x] 完成 Python/C++ 回归、Windows Release 构建和 GUI 截图验收
- **付费 3D 预算：** 1/20（硬上限 20）
- **状态：** complete

#### 阶段 30 当前验证
- Q 版 30 万面 OBJ 离线检查通过：296,642 面、单连通封闭流体、0 边界边、0 非流体边、0 退化面，并携带自然色顶点 RGB。
- 长期侧栏已接入 AI 导入、网格检查/修复、颜色处理、自动摆放、切片和 G-code 六步状态；普通切片不会覆盖已完成状态。
- Windows Release 单编译器构建与 AI Python 75/75 回归已通过；新 DLL 已部署并由正式 Orca 加载，production sidecar v4 健康。
- 全部 296,642 个三角面的正面顶点色渲染证明实际 OBJ 与 Q 版风格图在人物、姿态和主色块上高度一致。
- 正式 GUI 已从模型库双击加载 `d960d74e...`，显示 296,642 面、22 个颜色组和 40.0 x 43.2 x 100.0 mm；正常导入保留自然颜色且不再弹出 OBJ 颜色确认框。
- 空项目单模型干净复跑六步最终全绿，G-code 导出为 38,127,361 字节；日志确认当前打印仅 1 个对象、`0 -> 1`、`on_process_completed:finished`、完成后 `invalidated=0`，且没有刀路冲突、`toolpath_outside`、新的 `1 -> 0` 或崩溃日志。
- 最终 Windows Release DLL 已部署，构建与运行目录 SHA256 均为 `9D0582BB678161FAB3210DBF803FE8FA2E89264161EB938036C8910D62729BD3`；AI Python 75/75、C++ 顶点色 5 个断言、`py_compile` 和 `git diff --check` 通过。

#### 阶段 30 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 当前 Codex PowerShell 的 `PATH` 未包含 `cmake.exe` | 1 | 使用 Visual Studio 2022 Community 自带 CMake 的绝对路径继续构建 |
| 并行构建曾触发 `C1060` 编译器堆空间不足 | 1 | 使用 `/m:1`、`CL_MPCount=1`、`UseMultiToolTask=false` 和 `BuildInParallel=false` 完成两个 Release 目标 |
| 日常运行 DLL 被响应中的 Orca PID 61376 占用 | 1 | 不强制关闭可能含未保存内容的窗口；保留新构建产物，待进程正常关闭后同步并验收 |
| 组合 `Start-Process` sidecar/Orca 命令被本机策略拦截 | 1 | 未启动任何进程；改用固定 sidecar 批处理 `/b` 隐藏启动、健康检查和单独 Orca 启动 |
| GUI 验收时前台仍为 Windows 锁屏 | 3 | 停止坐标操作；真实 OBJ 离线视觉审计已完成，模型库、长期侧栏和 G-code 用户路径必须待解锁后继续，CLI 不能替代 GUI 验收 |
| 完成确认使用临时 `wxTimer` 并在自身回调中删除，触发 `Plater.cpp:10286` 访问冲突 | 1 | 改为由 `Plater::priv` 生命周期托管的单次定时器，通过统一 `wxEVT_TIMER` 分发确认结果；真实复跑后进程持续响应且无新增 crash 日志 |
| 多色自动流程沿用打印预设擦料塔，当前机型将塔放到热床外 | 1 | AI 自动切片前关闭擦料塔，并用 `is_slice_result_ready_for_export()` 作为完成门禁；保留颜色/换料映射，越界或 G-code 错误不再被标记为成功 |
| 首次短超时构建遗留编译器进程，占用 `Plater.obj` 与输出 DLL | 2 | 终止本轮遗留的 `mspdbsrv`，确认文件锁释放并关闭验收 Orca 后，以单并发完成 Release 构建 |

## 各阶段

### 阶段 1：恢复历史上下文
- [x] 检查已有规划文件
- [x] 运行会话恢复脚本
- [x] 定位源码仓库与版本控制根目录
- **状态：** complete

### 阶段 2：检查当前开发状态
- [x] 检查分支、远端与工作区（根目录无 `.git`，无法恢复当前分支/提交状态）
- [x] 分析本地开发改动与历史会话
- [x] 检查构建与测试痕迹
- **状态：** complete

### 阶段 3：总结与续作建议
- [x] 记录关键发现
- [x] 给出当前状态与下一步建议
- **状态：** complete

### 阶段 4：补充架构与打包审计
- [x] 核对 sidecar 打包与跨平台启动
- [x] 核对 feature gate、本地化目录和自动测试覆盖
- **状态：** complete

### 阶段 5：四项能力现状映射
- [x] 评估模型生成当前纵向流程与缺口
- [x] 识别 OrcaSlicer 现有模型检查/修复能力
- [x] 评估切片参数 AI 化当前能力和数据闭环
- [x] 梳理端到端交互可复用的执行入口
- **状态：** complete

### 阶段 6：目标架构与边界
- [x] 定义 provider-agnostic 服务、工作流与数据契约
- [x] 定义确定性内核与 LLM/Agent 的职责边界
- [x] 定义审批点、失败恢复、任务持久化和安全边界
- **状态：** complete

### 阶段 7：分期路线图
- [x] 给出 MVP 到完整能力的里程碑和依赖关系
- [x] 为每阶段定义验收标准、测试和发布要求
- [x] 输出建议优先级和下一步实施起点
- **状态：** complete

### 阶段 8：M0 AI 功能门控与能力发现
- [x] 读取计划、发现和进度日志
- [x] 重新确认当前 Git 工作区、AI GUI 接入点与 sidecar 契约差异
- [x] 增加默认关闭的 `enable_ai_features` 应用设置及 Preferences 实验开关
- [x] 统一 production/mock 的 `/health` v1 capability schema 并添加 Python 契约测试
- [x] 新增异步、loopback-only 的 `AIServiceManager`，仅在 capability 可用时延迟注册 AI UI
- [x] 恢复标准标签固定索引；3D Generate 改为 discovery 成功后追加且不抢占默认页
- [x] 在已提供 Windows CMake 路径后编译 `libslic3r_gui` 与 `OrcaSlicer` Release 目标
- [x] 使用隔离 `--datadir`、mock sidecar 和安装目录可执行文件完成默认关闭与已启用模型生成页的 GUI E2E
- [x] 在独立 `build-tests` 目录启用测试并运行新增 AppConfig Catch2 覆盖（3 assertions 通过）
- [ ] 在支持 OrcaSlicer 自定义菜单的驱动中验证 “Show AI Assistant” 菜单项与停靠 pane 显示/隐藏；当前 Windows DPI 坐标与 UI Automation 未能可靠驱动该菜单
- **状态：** complete — M0 核心门控、discovery、生成页、Release 编译与 AppConfig 单测均已验证；AI Assistant 的菜单交互为唯一后续补充

> 历史状态说明：当前 `master@a1ef7204fe` 已改为始终创建 3D Generate 页面并立即执行 capability discovery，不再保留本阶段记录的默认 `enable_ai_features` 门控；现状与后续决策以阶段 9 为准。

### 阶段 9：五大产品域状态与实施计划
- [x] 读取 `Docs/` 架构文档和 `Docs/开发进展.xlsx`
- [x] 以当前 `master@a1ef7204fe` 源码核对 27 项子能力
- [x] 输出统一成熟度口径、逐项状态矩阵和关键缺口
- [x] 输出核心 AI 与商业平台双轨路线图、依赖和验收门槛
- [x] 更新 `findings.md`、`progress.md` 并校验最终文档
- **状态：** complete

### 阶段 10：当前软件基线验证
- [x] 核对 `master@a1ef7204fe`、Windows 工具链、构建配置和工作区
- [x] 完成 Windows Release 全量构建、安装产物一致性检查
- [x] 完成 129 项 `libslic3r` Catch2/CTest 回归测试
- [x] 完成 production/mock sidecar health、Python 契约和语法检查
- [x] 使用隔离 `--datadir` 与 mock sidecar 完成 GUI 启动、首页、3D Generate、文生 3D、彩色 OBJ 导入和退出检查
- [x] 记录当前测试缺口、平台边界和下一步开发门槛
- **状态：** complete

### 阶段 11：2026-08-10 核心演示版冲刺
- [x] 将 macOS/Linux、账号计费和扩展架构降为最低优先级
- [x] 以当前 Windows Release 与模型生成纵向链路作为冲刺基线
- [x] 确认演示必须调用真实 AI provider；mock 仅作回归和灾备
- [x] 冻结单条黄金路径、非目标和失败降级策略
- [x] 修复黄金路径上的阻断问题并补最小回归覆盖
- [x] 完成真实/演示 provider 文生与图生 smoke
- [x] 完成生成结果导入、摆放、切片预览的端到端彩排
- [x] 产出可重复启动脚本、演示数据和现场操作清单
- **状态：** complete

### 阶段 12：3D Generate 页面交互优化
- [x] 复核用户标注图和 Windows 演示窗口布局
- [x] 图片选择后立即完整预览，并提供 50%–400% 缩放、Fit 和滚动查看
- [x] 将 provider 状态映射为五阶段用户进度，并显示步骤名和百分比
- [x] 保留失败发生时的最后阶段，修复 Windows 中文代码页下的符号乱码
- [x] 完成 Release 构建、锁屏窗口句柄验收和回归检查
- [x] 输出交互设计文档与验收记录
- **状态：** complete

### 阶段 13：完整参考图与两段式图生 3D 流程
- [x] 核对 Windows 启动入口、原始参考图和当前预处理契约
- [x] 将参考图预览改为稳定的 letterbox 完整绘制，保留缩放与滚动
- [x] 明确区分参考图与 AI 风格化预览图
- [x] 仅在风格化预览下载并显示后允许确认生成 3D
- [x] 图片风格化失败时禁止用原图冒充成功结果
- [x] 完成 Windows Release 构建、GUI 验收和 AI 回归
- [x] 更新设计、发现与进度记录
- **状态：** complete

## 关键问题
1. 四项 AI 能力各自已经达到什么程度，哪些只是 UI/规划？
2. 哪些能力应复用 OrcaSlicer 的确定性算法，哪些适合交给 LLM/Agent？
3. 端到端交互需要哪些可审计工具、状态机和用户确认点？
4. 如何分期交付，避免同时铺开四条线导致无法验收？

## 已做决策
| 决策 | 理由 |
|------|------|
| 以文件系统和 Git 现状为准恢复 | 会话恢复脚本没有返回未同步上下文 |
| 续作时先恢复可靠 Git 工作区 | 当前根目录没有 `.git`，继续修改前应先保护并可追踪现有工作 |
| Git 恢复后先修两个 UI 回归，再做真实 Tripo smoke | UI 问题已明确且改动小；真实服务验证是当前最大功能缺口 |
| 模型库与架构增强后置 | 当前首个纵向流程已可操作，应先完成收尾和真实链路验收 |
| 在真实服务验收后优先补 feature gate、sidecar 打包和本地化目录 | 这些缺口会影响未配置用户、跨平台交付和可发布性 |
| C++ 核心保持 provider-agnostic，供应商适配留在 sidecar | OrcaSlicer 不应被 Tripo、AGNES、OpenAI、Claude 等具体服务绑定 |
| 端到端交互采用受控工作流/工具编排，不让 LLM 直接任意修改模型或配置 | 几何、切片、导入和配置变更需要确定性校验、审计和用户确认 |
| 实施顺序采用 M0 基础治理 → M1 生成产品化 → M2 Preflight → M3 Repair → M4 切片闭环 → M5 Guided Workflow → M6 受控 Agent | 各能力先独立验收，避免编排层掩盖底层不稳定；最大化复用当前纵向生成流程和 OrcaSlicer 内核 |
| 演示版允许通过 `ORCASLICER_AI_ALLOW_PREPROCESS_FALLBACK=1` 显式启用文字预处理降级 | OpenAI-compatible 代理当前返回 HTTP 502；文字可保留原始 prompt 继续调用真实 Tripo，但图片必须先得到真实 AI 风格预览，不能再复制原图冒充成功结果 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 未找到既有规划文件 | 1 | 创建新的恢复状态规划文件 |
| 更新问题记录时文本不匹配 | 1 | 重新读取计划并使用准确文本更新 |
| 补充 GUI 追踪代理上下文超限（502） | 1 | 不重试；采用已完成的代码审计、截图和历史运行记录交叉验证 |
| 组合嵌套 Git 对象库与根目录后出现全量删除/未跟踪 | 1 | 放弃该全树 diff；改用版本、时间戳、代码引用和构建产物交叉恢复 |
| 重复提交无变化的计划编辑 | 1 | 不再重复该编辑，继续记录新的实际发现 |
| 等待构建审计代理最终摘要超时 | 1 | 不轮询；等待后台完成通知自动回传 |
| 新阶段恢复流程中的 `git diff --stat` 再次失败：根目录无 `.git` | 1 | 不再尝试根目录 Git 命令；继续以已恢复的文件与历史证据为准 |
| 模型检查/修复盘点代理因上游连接失败返回 502 | 1 | 不重复原任务；缩小范围，在同一研究上下文中只汇总关键入口与缺口 |
| 真实文生 smoke 在预处理阶段失败：OpenAI-compatible `/models` 返回 HTTP 502 | 1 | 未创建 Tripo 任务且未产生生成费用；实现显式、默认关闭的原始输入降级后再执行真实双 smoke |
| 后台重启后探针仍命中旧 `cmd /K` 控制台派生的 sidecar | 2 | 停止两个已确认命令行的生产 sidecar 子进程；改在独立端口以前台受控实例验证环境开关，避免重复付费调用 |
| OrcaSlicer CLI 重开已保存的 140 万面项目并切片时 `0xc0000005` | 1 | 保留 crash 日志与 WER 证据；先用 PDB/转储定位，再区分 CLI、3MF round-trip 与高面数因素，不重复相同切片命令 |
| 锁屏 GUI 验收中同步发送按钮消息超时并叠加多个确认框 | 1 | 改用异步 `PostMessage`，逐个确认现有弹窗；后续每次点击后先检查顶层窗口状态 |
| Windows Release 并行构建出现 `C3859/C1076`，系统分页文件不足 | 1 | 将本轮构建降为 `/m:1` 单并发；代码编译和链接随后通过 |
| 当前 PowerShell 未将 `cmake.exe` 加入 PATH | 1 | 使用 `CMakeCache.txt` 与既有配置定位 Visual Studio Community 自带 CMake，并以绝对路径构建 |
| 首次窗口截图只显示对照区左半边 | 1 | 确认画布无横向滚动；截图进程受 200% DPI 虚拟化影响，启用 Per-Monitor DPI awareness 后以 3144×2008 完整截图验收 |
| 真实 Tripo 低模任务返回 26,147 个三角面且存在 26 条边界边、14 条非流体边 | 1 | 保留唯一付费任务及完整产物，不自动创建第二个任务；门禁拒绝导入并输出结构化诊断，后续需收紧生成目标或增加保色本地修复/简化 |

### 阶段 14：真实 AI 风格预览通路修复
- [x] 确认机器级 `OPENAI_BASE_URL=https://laotie.dev`，并免费探测根路径与 `/v1/models`
- [x] 修复旧 sidecar 复用旧代理地址的问题，重启当前 production sidecar
- [x] 为域名根 Base URL 自动补全标准 `/v1` 前缀，并保留已有/自定义 API 路径
- [x] 为短图片指令补充风格预览约束，要求真实重绘、保留身份与完整构图、禁止原图冒充
- [x] 完成真实 `gpt-image-2` 图片编辑，校验输出与原图哈希不同并人工复核画面
- [x] 完成 Python 回归、语法与空白检查，确认当前 OrcaSlicer 与 sidecar 运行正常
- **状态：** complete

### 阶段 15：原图与 AI 结果对照预览
- [x] 复核用户截图并定位单一预览位图覆盖原图的根因
- [x] 将参考图与 AI 结果拆分为独立状态并并排绘制
- [x] 补齐生成中、等待结果、失败和成功状态文案
- [x] 完成 Windows Release 构建与回归检查
- [x] 使用 mock/已有预览完成 GUI 对照验收
- [x] 更新设计、发现与进度记录
- **状态：** complete

### 阶段 16：OBJ-only 生成与导入
- [x] 将 production/mock sidecar 的模型产物能力收敛为仅 `obj`
- [x] 移除真实生成中的 3MF/STL 回退，只请求并校验 OBJ
- [x] 将启动探针、smoke 客户端和 GUI capability discovery 同步为 OBJ 契约
- [x] 在 GUI 下载前拒绝非 OBJ 产物，并保留顶点色校验
- [x] 补充 OBJ 成功和转换失败不回退测试
- [x] 完成 Python 回归、Windows Release 构建、安装同步与运行态验证
- **状态：** complete

### 阶段 17：OBJ 资源包、颜色与本地产物闭环
- [x] 复核真实 Tripo OBJ 返回结构与 OrcaSlicer 颜色导入能力
- [x] 将 sidecar 任务、原始资源包和中间文件固定到项目根目录
- [x] 安全解压 OBJ ZIP 并保留 OBJ、MTL 与全部贴图
- [x] 将 UV 底色贴图烘焙为 OrcaSlicer 可识别的顶点色 OBJ
- [x] 将 GUI 下载文件固定到项目根目录并取消自动删除
- [x] 补充 ZIP 安全、纹理烘焙、持久化和回归测试
- [x] 离线恢复本次真实失败产物并验证带颜色导入
- [x] 完成 Windows Release 构建、部署和运行态复验
- **状态：** complete

### 阶段 18：彩色低模与打印色板约束
- [x] 核对 Tripo 官方生成、文件与图像编辑参数
- [x] 确认低模生成参数与失败回退策略
- [x] 定位打印色板来源为当前项目 `filament_colour`
- [x] 确认默认色板交互：自动采用当前耗材槽颜色，并在生成区完整展示
- [x] 设计生成提示约束、预览量化和 OBJ 二次量化闭环
- [x] 实现 Tripo 低模参数、色板传递与确定性量化
- [x] 补充 Python/C++ 回归、Windows Release 构建和离线产物验收
- [x] 补充实现计划与验收标准
- [x] 使用真实付费图片编辑验证 Q 版风格、纯色背景和严格打印色板
- [x] 经用户确认后执行一次付费 Tripo 低模 3D 端到端验收
- [x] 保存生成/转换 task ID、原始 ZIP、解包资源、顶点色 OBJ、渲染预览与结构化诊断
- [x] 使真实产物同时通过不超过 20,000 面、单连通、封闭流体和严格色板门禁
- **状态：** complete（阶段 20 新真实任务以 15,148 面、单连通、封闭流体和严格色板通过全部门禁）

### 阶段 19：3D Generate 导航、统一输入与色板交互
- [x] 复核用户截图、当前标签注册顺序、输入状态机和 `filament_colour` 读取入口
- [x] 确定统一文字+图片输入、按内容自动路由和本页自定义色板方案
- [x] 将 3D Generate 标签移到 Prepare 前并保持标签枚举一致
- [x] 移除文字/图片模式选择，支持文字、图片或两者任意组合继续
- [x] 压缩步骤区，只显示当前阶段主操作和必要的复核内容
- [x] 支持当前项目耗材色动态刷新与不回写项目的手动色板
- [x] 完成回归、Windows Release 构建和 200% DPI GUI 验收
- **状态：** complete

#### 阶段 19 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| Windows Release 构建因项目 `/MP` 并发触发 `C3859/C1076`、系统错误 1455 | 1 | 使用 `CL_MPCount=1`、`UseMultiToolTask=false` 和 `/m:1` 真正串行重试 |

### 阶段 20：三风格预览、色板保真与一键 G-code
- [x] 恢复当前实现、运行态、测试和规划上下文
- [x] 定位严格色板预览退化为少量主色的根因并设计保真映射策略
- [x] 增加 Q 版卡通、赛博朋克、古典风格选择并优化各自生成约束
- [x] 从本机图片目录选取合适输入完成三风格真实图片验证
- [x] 将模型生成后的微小脱离件清理、保色拓扑预修复、CGAL 失败保护、尺寸规范化、摆盘和切片收敛为默认自动流程
- [x] 为自动流程补充修复诊断、可恢复产物和 G-code 结果入口
- [x] 使用自然色板真实失败 OBJ 完成自动修复、切片与 Preview 验收
- [x] 完成 Python/C++ 回归、Windows Release 构建、GUI 与 G-code 验收
- **状态：** complete

#### 阶段 20 约束
- 图片生成可按需调用；3D 生成累计最多 20 次，必须记录每次 task ID、用途、结果和剩余额度。
- 默认黄金路径在风格预览确认后尽可能一键到 G-code，不要求用户手动修复、简化、缩放或摆盘。
- 自动修复或简化不得静默丢失打印颜色；失败时保留原始产物和结构化诊断。

#### 阶段 20 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 新色簇匹配算法定向测试报 `NameError: _rgb_to_lab` | 1 | 核对现有实现后改用实际函数名 `_srgb_to_lab`，再运行同组测试 |
| OBJ 自动重试测试仍断言旧三参数调用，且成功夹具触发同路径 `copyfile` | 1 | 更新断言包含 attempt 编号，并允许恢复场景直接复用最终目标路径 |
| 新增肤色区域测试时误用不存在的 `PaletteTests` 类名 | 1 | 通过源码确认实际类名为 `PrintablePaletteTests`，改用精确测试标识重跑 |
| 全仓搜索量化调用点因生成产物目录过大超时 | 1 | 将搜索范围收敛到 `tools/ai`，确认唯一生产调用点并继续修改 |
| 合并停止、环境设置、日志和后台启动的 PowerShell 命令被本机策略拒绝 | 2 | 已分步停止精确 PID；改用现有 Windows 批处理启动链的后台模式传入非敏感配置，不再重复复杂 `Start-Process` 命令 |
| Win32 `GetDlgCtrlID` 将 wxWidgets 高位控件 ID 返回为负数，文本变更通知被临时驱动跳过 | 1 | 除 `-1` 外统一按低 16 位发送 `EN_CHANGE`，确认文字-only 输入可启用下一步 |
| 同步按钮消息导致自动化超时并叠加两个生成确认框 | 1 | 改用异步点击；只确认最上层 mock 流程并取消残留确认框，未产生真实 3D 调用 |
| mock 四色切片完成后提示 G-code 路径超出热床边界 | 1 | 日志确认 `model_fits=1` 且 G-code 成功导出；该提示来自当前 WonderMaker 多色打印配置的路径检查，不影响 OBJ 导入、切片或 Preview 验收 |
| `planning-with-files-zh` 自带 `check-complete.ps1` 因安装文件乱码产生 PowerShell 解析错误 | 1 | 不修改用户技能目录；改用 UTF-8 读取和状态文本扫描确认阶段 20 已完成 |
| 自然色板真实 OBJ 含 1 个微小脱离件、45 条边界边和 4 条非流形边 | 1 | generation `92c11c8e-e949-4902-9bc6-1566b4536846`、conversion `a873c5f1-95a5-4ecb-bf56-0a4a79c2e3fc` 已持久化；不重复付费生成，改为本地删除微小组件并在 Orca 内自动 CGAL 保色修复 |
| 当前 Codex PowerShell 的 `PATH` 未包含 `cmake` | 1 | 使用 Visual Studio 2022 自带 CMake 的绝对路径完成单并发 Release 构建 |
| 复合 PowerShell 后台启动命令被本机策略拦截 | 1 | 未启动任何进程；拆成 mock 与 Orca 两个短批处理入口，并分步执行健康检查 |
| CGAL 自动修复真实 OBJ 后仍返回 `mesh still open after hole filling` | 2 | GUI 已正确撤销导入且未切片；不再重复同一 CGAL 方案，改为 sidecar 对约 1 mm 非流形局部做保色预修复，再由 Orca 执行最终门禁 |
| `py_compile` 与 `unittest` 并行写同一个 `__pycache__` 时 Windows 返回拒绝访问 | 1 | 验证改为串行；单测设置 `PYTHONDONTWRITEBYTECODE=1`，随后语法检查和 3 项定向测试通过 |

### 阶段 21：最终需求审计与生产链路复验
- [x] 对照用户标注截图核对 3D Generate 标签顺序、统一输入、紧凑布局和耗材色板交互
- [x] 确认页面显示时重新读取当前项目 `filament_colour`，并保留本页自定义色板
- [x] 复核三种最终风格预览及其实际用色统计
- [x] 验证生产 sidecar 与真实 AI 的一次连续风格预览到 G-code 黄金路径
- [x] 重跑 Python 回归、Windows Release 构建和最终 GUI 验收
- **状态：** complete

#### 阶段 21 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 真实 GUI 黄金路径导入成功但切片被混合温度门禁拒绝 | 1 | 当前项目槽 1 为 PLA、槽 2–8 为 ABS；不关闭安全门禁，改为自动选择最大的温度兼容槽组并保留原始槽号映射 |
| 初版温度筛选从合并配置读取单值/短数组，真实项目仍显示 8 色 | 1 | 改为从 `PresetBundle::filament_presets` 逐槽读取实际耗材预设，与切片安全检查使用同一温度数据来源 |
| 兼容色板状态单行文案在 200% DPI 下被截断 | 1 | 将兼容颜色数与排除槽位数拆为两行，最终截图无裁切 |
| 首页嵌入式视图吞掉物理导航点击，旧截图未进入 3D Generate | 2 | 枚举导航栏子窗口并向 `3D Generate` 实际句柄发送鼠标消息，废弃无效截图 |
| 首次增量构建调用因工具短超时返回 124，但编译仍在后台运行 | 1 | 不启动重复构建，跟踪原 `cmake/MSBuild` 进程直到链接完成并校验 DLL 哈希 |

### 阶段 22：3D 生成页模型预览与中文化
- [x] 恢复规划、运行态、工作区和既有 GUI 证据
- [x] 明确 3D 预览后的流程停顿点与确认操作
- [x] 设计并实现生成页内的只读交互式 3D 预览
- [x] 将 3D 生成页、状态、操作按钮和相关确认框统一为中文
- [x] 使用现有真实 OBJ 完成 Windows Release、GUI 和 G-code 回归
- [x] 更新设计、发现和最终运行记录
- **状态：** complete

### 阶段 23：可打印颜色开关
- [x] 在生成页增加“使用可打印颜色”开关，默认保持开启
- [x] 关闭时让图片预览、提示词和 OBJ 后处理保留自然颜色
- [x] 关闭时将模型作为普通单耗材对象导入，避免错误映射为有限耗材色
- [x] 补充 sidecar 契约、自然色 OBJ 与状态一致性回归
- [x] 完成 Windows Release 构建和 GUI 验收，不新增付费 AI 调用
- **状态：** complete

#### 阶段 23 约束
- 开启时完全保留现有严格打印色板与多耗材流程。
- 关闭时自然颜色用于图片和 3D 预览；G-code 使用当前单一耗材，不承诺还原自然色。
- 切换开关后，已生成的预览必须失效并要求重新生成，不能混用两种颜色策略。

#### 阶段 23 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 自然色 OBJ 测试假定烘焙说明位于首行，但尺寸规范化会前置自己的注释 | 1 | 移除脆弱的注释位置断言，继续以顶点色、拓扑和产物验证作为验收标准 |
| Windows 下 `python -m py_compile tools/ai/*.py` 不展开通配符 | 1 | 由 PowerShell 枚举明确的 `.py` 文件列表后传给 `py_compile` |
| `cmake --install` 长时间同步资源后未更新运行目录 DLL | 1 | 确认 Orca 未运行后，仅部署已通过构建的 DLL，并以 SHA256 校验构建/运行副本一致 |
| 部署检查只查询 `OrcaSlicer`，漏掉实际进程名 `orca-slicer`，DLL 仍被 PID 49440 占用 | 1 | 使用 `tasklist /m OrcaSlicer.dll` 精确定位本项目实例，停止该 PID 后再部署 |
| 停止 PID 49440 后立即复查仍短暂返回进程存在 | 1 | 单独复查模块占用确认进程随后正常退出，再执行部署，不强制终止或重复发送停止命令 |
| 关闭颜色约束后的单行说明在 Windows 200% DPI 窄栏中被裁切 | 1 | 将自然色预览与单耗材打印说明拆成固定两行后重新构建验收 |

### 阶段 24：正式 AI 链路稳定性与准备页首开性能
- [x] 定位截图中图片预处理服务连接失败的真实根因并恢复正式链路
- [x] 移除日常开发和验收入口对 mock sidecar 的依赖，统一使用 `18764` 正式服务
- [x] 将图片预处理连接错误改为中文、可诊断且不泄露凭据的提示
- [x] 定位“准备”页首次进入卡顿的同步初始化热点并优化
- [x] 完成 Windows Release 构建、正式服务检查和真实 GUI 验收
- **状态：** complete

#### 阶段 24 约束
- 后续不再使用 `18765` mock 版进行功能或 GUI 验收。
- 允许使用不产生费用的正式服务健康检查；新的图片生成调用可直接验证，3D 生成仍需用户单独确认。
- 保留正式失败产物与错误分类，不用本地假结果冒充成功。

#### 阶段 24 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 父进程保留旧 `OPENAI_BASE_URL`，重启子进程仍访问旧 IP | 2 | 启动前从 Windows User/Machine 环境刷新配置；健康接口报告非敏感 provider URL，启动器检测变化后只重启确认归属的正式 sidecar |
| 旧 `/K` sidecar 宿主在仅停止 Python 后继续占用启动链 | 1 | 停止脚本同时验证并关闭专属 `start_orca_ai_sidecar.bat` 父进程，避免旧宿主重新派生子进程 |
| `.bat` 经补丁后使用 LF，`cmd.exe` 无法稳定识别 `goto` 标签 | 1 | 三个 Windows 批处理入口统一恢复 CRLF，并重跑 `--check` |
| 安装目录 DLL 被正在运行的 Orca 锁定 | 1 | 不强制关闭未保存窗口；保留旧 DLL 备份并并排部署已构建 DLL，随后用隔离实例验证 |

### 阶段 25：彩色预览保持与持久模型库
- [x] 定位颜色、跨页面预览和模型库的当前状态根因
- [x] 明确颜色丢失发生在生成页预览还是导入后的准备/切片视图
- [x] 设计持久模型索引、历史扫描和损坏条目降级策略
- [x] 保持当前 3D 预览跨页面切换和导入后可恢复
- [x] 支持模型库展示全部有效历史 OBJ，双击载入 3D 预览
- [x] 保留并恢复每个模型的颜色模式、色板、来源和风格元数据
- [x] 完成 Windows Release、正式 OBJ、跨页面和重启后模型库 GUI 验收
- [x] 当前 Orca 进程关闭后，将最新 Release DLL 同步到日常运行目录
- **状态：** complete

#### 阶段 25 约束
- 模型库历史必须来自项目 `generated_models` 中的真实持久产物，不使用 mock 条目。
- 双击历史模型只加载生成页 3D 预览，不自动导入、摆盘或切片。
- 颜色元数据缺失时优先从 OBJ 顶点色恢复，不把无色模型伪装成彩色模型。

#### 阶段 25 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 直接向原生 notebook 发送 `TCM_GETITEMRECT` 导致 `COMCTL32.dll` 崩溃 | 1 | 认定为 GUI 自动化误用，不重复该消息；改用正常鼠标/窗口消息完成跨页与双击验收 |
| `libslic3r_tests` 首次重建因 `/MP` 并发触发 `C1060/C3859` 和页面文件不足 | 1 | 使用 `/m:1`、`CL_MPCount=1`、`UseMultiToolTask=false` 串行完成构建，新增颜色用例 5/5 断言通过 |
| 沙箱内 Release 构建无法读取本机 Windows SDK 目录 | 1 | 获得构建权限后在沙箱外运行同一 CMake Release 目标，构建成功 |
| 最新 DLL 与日常运行目录哈希不一致，但 PID 58140 正在使用旧 DLL | 1 | 不强制关闭可能有未保存内容的用户窗口；保留最新构建产物，待当前 Orca 正常关闭后同步 |

### 阶段 26：真实彩色 OBJ 保色闭合修复
- [x] 从用户截图确认失败阶段、模型规模、颜色状态和错误文本
- [x] 定位本次真实任务、原始 OBJ、修复诊断和失败拓扑特征
- [x] 设计不重新付费生成、不绕过流体门禁的确定性保色修复
- [x] 实现局部闭合修复与失败降级，保留原始模型和颜色
- [x] 支持已下载/历史 OBJ 在 sidecar 任务失效后由用户确认继续导入切片
- [x] 完成离线拓扑/颜色回归、Windows Release 构建和真实 GUI 切片验收
- [x] 部署正式版本并恢复日常启动状态
- **状态：** complete

#### 阶段 26 约束
- 不创建新的 Tripo 任务，不消耗 3D 生成额度。
- 不允许开放网格或非流形网格绕过门禁进入切片。
- 修复前后必须保持单一主体、尺寸和顶点色；失败时保留原 OBJ 与结构化诊断。

#### 阶段 26 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 独立修复调用导入 `orca_ai_sidecar` 时找不到 `openai_preprocessor` | 1 | 显式将项目 `tools/ai` 加入 `PYTHONPATH`，不修改系统环境 |
| 前后对比脚本对原始开放网格使用严格拓扑校验并按预期被拒绝 | 1 | 修复前使用 `allow_repairable=True` 读取诊断，修复后继续使用严格零异常边校验 |
| sidecar 重启后 GUI 确认按钮请求旧 job ID，返回 `Model job not found` | 1 | 改为有效本地 OBJ 优先导入；模型库载入后允许用户显式确认生成 G-code，远端下载仅作本地缺失兜底 |
| Windows `rg` 不接受 `AIModelGenerationClient.*` shell 通配路径 | 1 | 改用明确文件或目录范围，不重复 Windows 路径通配写法 |
| 构建和首次全量测试超过工具 5 秒等待窗口 | 2 | 先检查残留进程，确认构建继续完成；测试改用 60 秒超时并读取最终 68/68 结果，不把工具超时误判为失败 |
| PowerShell 将 `foreach` 结果直接接管道时报空管道元素 | 2 | 先写入数组再统一 `Format-List`，避免脚本块后的直接管道解析差异 |
| GUI 截图命令使用不存在的程序集简称 | 1 | 使用完整 `System.Windows.Forms` 与 `System.Drawing` 程序集名重跑，Orca 进程和切片不受影响 |
| `planning-with-files-zh` 完成检查脚本因安装文件乱码无法解析 | 1 | 不修改技能安装目录；直接核对阶段 26 全部复选项和 `complete` 状态，并将工具问题记录在计划中 |

### 阶段 27：修复失败手动导入与模型库切片
- [x] 核对自动修复失败路径、模型库预览状态和主窗口切片回调
- [x] 明确失败模型不自动切片、手动导入只进入准备页的安全边界
- [x] 实现自动修复失败后的“手动导入 / 取消”交互
- [x] 实现模型库预览后的“导入并切片”明确操作
- [x] 完成 Windows Release 构建和两条真实 GUI 流程验证
- [x] 部署正式版本并恢复日常运行状态
- **状态：** complete

#### 阶段 27 约束
- 不创建新的 Tripo 任务，使用项目内已有真实 OBJ 验证。
- 自动修复失败的开放网格不得自动触发切片。
- 手动导入与模型库导入都必须复用现有颜色映射，原 OBJ 与诊断保留在项目目录。
- 模型库双击只预览，切片必须由用户点击明确按钮触发。

#### 阶段 27 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `MessageDialog` 不暴露 `SetYesNoLabels`，首次编译失败 | 1 | 改用项目中支持自定义按钮文案的 `RichMessageDialog`，保留“手动导入 / 取消”交互 |

#### 阶段 22 约束
- Windows 演示优先，不扩展 macOS/Linux 工作。
- 不新增真实 Tripo 调用；使用现有真实 OBJ 和 mock 验证。
- 预览不得绕过网格、颜色、温度兼容和切片安全门禁。

#### 阶段 22 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 组合读取状态机源码的命令超时 | 1 | 缩小到具体函数和行段，避免同一命令并发大范围搜索 |
| PowerShell 将 `rg` 的通配符/含双引号正则错误解析 | 2 | 改用明确目录和单引号固定模式，不再使用 Windows 下的 shell 通配符 |
| 预览画布首次为空白 | 3 | 依次排除 DPI、刷新和上下文绑定，最终确认生成页先于主 3D 画布打开时全局着色器未初始化；补齐 `SetCurrent -> init_opengl -> render` 顺序并隔离共享 GL 状态 |
| 新增 framebuffer 隔离后编译缺少 GLAD 声明 | 1 | 按 `GLCanvas3D.cpp` 的既有模式引入 `<glad/gl.h>` |
| 非 DPI-aware 截图只捕获应用左半边，误判模型仍为空 | 1 | 截图进程启用 Per-Monitor DPI awareness，使用 3144x2008 物理像素完成视觉验收 |

### 阶段 28：模型导入颜色策略解耦
- [x] 核对 Orca 原生 OBJ 颜色导入与 AI 自动映射路径
- [x] 明确“生成颜色约束”和“导入颜色策略”的独立语义
- [x] 新增“正常导入（保留颜色）/ 单色导入”选择
- [x] 自动上色失败时保留模型并进入准备页手动上色
- [x] 更新状态文案和模型库导入行为
- [x] 完成 Windows Release 构建与真实 OBJ GUI 验证
- **状态：** complete

#### 阶段 28 约束
- 默认使用正常导入，不再由“使用可打印颜色”开关隐式强制单色。
- 自动上色失败不得阻断模型导入，也不得直接触发切片。
- 网格修复门禁保持不变；开放网格仍需修复或手动处理后才能切片。
- 不创建新的图片或 Tripo 付费任务。

#### 阶段 28 错误记录
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 单色模型首次 G-code 导出成功后被 `enable_prime_tower` 归一化立即作废 | 2 | 首次误写 `project_config`，但完整配置仍被当前打印预设覆盖；改为在显式单色自动切片前更新 `prints.get_edited_preset().config`，同步预设/项目脏状态和 Plater 配置 |
