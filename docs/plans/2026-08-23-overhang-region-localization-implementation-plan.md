# 阶段 68：局部悬垂风险定位实施计划

日期：2026-08-23

1. 新增失败测试：接地底面被排除、离床向下面被选择、微小区域被过滤、无结果时原选区保持不变。
2. 在纯 C++ 区域编辑器中实现与 Sidecar 一致的候选面、连通区域和显著面积筛选。
3. 在 `ModelPreview3D` 接入选区历史、高亮重建和变更通知。
4. 在模型质量卡增加定位入口，成功时展开局部区域工具，失败时安全提示。
5. 运行定向 C++ 测试、全量模型生成离线测试、`git diff --check` 和 Windows Release 构建。
6. 使用仓库本地应用与本地 Sidecar，在真实模型和合成悬垂样本上完成 GUI 验收，记录结果并提交。
