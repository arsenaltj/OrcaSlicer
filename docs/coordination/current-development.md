# 2026-09-23 远端同步与本地提交候选

- 模型生成分支基线已更新至 `d8bdb2f19`；当天 GitHub 查询模型生成 HEAD 仍为该提交，集成 HEAD 为 `decf64fab`（合入同一模型生成 HEAD 的合并提交）。本地长期未提交的工作在原 stash 和快照中留有备份；生成 G-code 保持本地，不进源码提交。
- 合并后修复美颜工作台到颜色匹配的入口/页面，并去除生成提交回调的重复定义，保留带任务身份校验的新实现。Windows 增量 GUI 目标编译通过；相关 C++ 64＋18＋21 项、开发入口 20 项、语义 Python 176 项通过。日志见 `.tmp/dev/logs/20260923-175852-908`、`20260923-180059-078`、`20260923-180348-300`、`20260923-180632-715`。本次未重装或启动新 GUI，颜色匹配真实窗口仍待复核；不能将离线测试视为视觉/实物打印验收。
- `verify_ai_integration.py` 的四处共享文件差异预算未过：`src/slic3r/CMakeLists.txt`、`MainFrame.cpp`、`Plater.cpp`、`Plater.hpp`。保留门禁供架构审查，不把当前候选标记为可直接合入集成；多平台 CI 和非作者复核仍未执行。

# 2026-09-21 通用分区当前轮：光栅加速落地，识别与配色质量仍待解决

- Windows 本地语义识别的逐面光栅已接入可选原生内核，DLL 随开发程序安装并参与宿主/worker 双侧身份校验；无 DLL 时仍走原 Python 路径。H5/H7/H10 共 24 个真实视角的可见面、深度和纹理 RGB 逐像素相同，H10 完整 worker 的投影分区/五官提示与冻结基线相同。H10 `raster` 累计 186.1→2.8 秒，隔离完整分析 43.1 秒；含诊断写盘的旧 228.8 秒只作参考。
- 编译、安装、捆绑运行检查与相关定向测试通过。真实 Orca 由 H10 原始模型首次进入工作台，31.7 秒时仍识别、60.1 秒时已可编辑，原约四分钟；仍为 6 耗材/63 区。原色与自动匹配比较下，深灰肤色/青绿夹克及围巾误选并未改善，不能计 P3/P4/P5 通过。H10 原件及先前草稿已保存到历史，开发程序停留在新可见工作台供检查。
- 下一轮优先验证头发与围巾的三维材质/几何边界，冻结规则后用 H5/H7 回归和新未见人像验收；同时单独记录六色耗材对肤色和夹克色的覆盖缺口。默认分区 A 未切换，累计真实 3D 仍 15/20。证据 `.tmp/dev/general-regions/round33-raster-probe/review.md`。

# 2026-09-21 通用分区上一轮：局部放大识别未解决围巾误选

- 固定人脸框的两种局部视野在 H5/H7/H10 复跑：H10 正面深绿围巾改善，但 H5 真发丝和黄衣识别退化。H10 从原网格重新渲染 0°/45°/315°后，正面可见三角面 86,916→129,541；三视角额外渲染约 42.5 秒。侧面全图与局部都将部分深色围巾误认成头发。
- 将同一视角的全图与局部仅算一次一致证据、再要求不同方位共同支持，离线追加撤回 85 补丁/1,535 面；原纹理对照仍见围巾蓝色细条，关键边界未过且成本过高，**不接入默认产品**。下一轮停止调裁剪/投票，回查三维材质与几何边界；另把原生 A 的配色偏差与首次等待单列处理。累计真实 3D 15/20，未改产品或用户草稿。证据 `.tmp/dev/general-regions/round32-local-body/review.md`、`h10-dark-waves-scarf-jacket-focus-guard-comparison.png`。

# 2026-09-21 通用分区当前轮：H10 围巾/头发边界仍未过关

- 冻结 v11/v14/v16/v20 后，用新图在实际 Orca 流程生成 H10 深卷发/深绿围巾/米色夹克人像，948,639 面，累计真实 3D 提交 **15/20**。自动化清空的提示在任务记录中仍为上次的“头发黑一点”，因此 H10 不作为纯图片生成证据；不能据此判断手工输入存在残留。H10 此后转诊断/回归，改算法须用新样本验收。
- 冻结候选按 v14 支持度路由 v11，三维头发扩张误吞两侧围巾及少量衣服。新增离线多视角衣服证据否决只撤回部分扩张（206 补丁、34,935 面），正面仍有细条残留，**拒绝接入默认工作台**。工作台原生 A 可打开，6 色下为 63 区，首次约四分钟可编辑；原色对照明显好于目前耗材映射的深灰脸/青色夹克。配色质量另列问题，不作为分区收益。数值上修复投影交叠 IoU 的 1 ULP 越界，12 项定向测试通过；未保存编辑或导入准备页。
- 残余诊断显示 H10 新扩张头发中的未标注面积约 2,964，约 2,406 没有低分辨率人体图的逐面像素；简单逐面投票也不能补齐。下一轮不加 H10 专属阈值，测试保留上下文的局部放大观察与三维边界传播，再回归 H5–H10，用真正未见人像/物品验证。默认 A、已运行程序与用户草稿不变。证据 `.tmp/dev/general-regions/round31-h10/review.md`、`h10-dark-waves-scarf-jacket-guard-focus.png`。

# 2026-09-21 通用分区当前轮：厚度提供部件线索，但新夹克否定直接接入

- G2/G3 的法线候选剩余面不能简单合为一块或按 30° 折角切开：最大连通件仍跨冰淇淋/杯身和瓶身/把手。深度图和最近对向面厚度近似也没补上大面。独立单射线穿透网格的厚度信号能区分药水瓶、夹克的宽窄部件，但这不是官方 SAMesh 的多射线 SDF。
- 固定厚度彩图＋SAM2.1 small 八视角＋双视角三维关联，在 G3 药水瓶覆盖 65.3% 网格面积，G2 冰淇淋仅 18.4%。冻结规则后新 G4 白色夹克由当前 worker 和原生 A 得 28 块，厚度候选只覆盖 12.3%，正面可见 4.4%，漏掉衣身与袖子。直接厚度五级聚类找回大面但产生更多色带和碎边；固定邻接平滑后 G4 正面边线仍由 A 的 2,070 升至 5,836。**拒绝两种厚度候选接入默认工作台**，G4 转回归，下一轮把厚度当辅助证据研究联合表面边界，不再调 G4 专属阈值。
- 本轮只动隔离实验与记录；产品源码、程序、历史草稿、打印色、准备页、3MF 未改或未验，默认 A 不变；累计真实 3D 提交 14/20。证据 `.tmp/dev/general-regions/g-holdouts/review.md`；八方向 `round12-thickness-multiview/g4-jacket/consensus-eight.png`、`round14-thickness-smooth/g4-jacket/smooth-k5-eight.png`。

# 2026-09-21 通用分区当前轮：平滑缝在新物品未泛化，法线多视角是有用但不完整的证据

- 在 G2 冰淇淋，平滑共享缝离线编辑分区 28→21，杯身假竖线减少；初版在旧人像跨少量脸/脖子等标签，故冻结只用于未知区域的保护规则。按文件名提前冻结的 G3 药水瓶是 21,156 面白模，worker＋原生 A 为 28 块；保护规则到 21 块却只去掉正面 11.6% 边线，瓶身仍有明显锯齿，**不接入默认产品**。G3 对此规则是独立检查，此后转回归。
- SAMesh 思想中的平滑法线图是独立实现、非官方 SAMesh 复现：固定 SAM2.1 small 参数在 G2/G3 单视角各给出 4 个有效部件掩码，比原白图/普通灰光照有信息；GPU 八方向额外约 14.9/12.6 秒、峰值约 0.98 GB，投回原网格后分别只覆盖 23.4%/28.7% 的表面积。冰淇淋顶部、药水瓶底座和把手跨视角一致，但杯身/瓶身大面未解决；顺序叠加现有 A 更达不到默认首次交互预算。下一轮研究未覆盖大面的通用表面边界，须重新用新物品与人像验收，不能继续把 G1/G2/G3 当独立样本。未改产品源码、运行程序、用户草稿、配色、准备页或 3MF；默认 A 不变，累计 3D 14/20。全量证据 `.tmp/dev/general-regions/g-holdouts/review.md`，视觉 `unknown-smooth/g3-potion-focus.png`、`round5c-normal-gpu/g3-potion/lift-group-3-eight.png`。

# 2026-09-21 通用分区上一轮：G1/G2 实际分区与八视角检查

- G1 徽章原 3MF 的横版对象已保几何派生为 17 部件/33,682 面的中性 GLB；原文件未含实际材料色，不能据此评配色或原生 3MF 导入。G2 冰淇淋 34,478 面、纯白。两件均通过当前 worker 和已编译原生 A 探针，分别为 27/28 编辑块；徽章底板有多余斜线，冰淇淋杯身有多余竖线。全连通件合并让 G1 回到 17 块，但把 G2 杯口/冰淇淋边界吞掉；有条件的平坦同色同耗材合并只改善 G1（正面边线 4,497→3,419 像素），G2 不变，四个既有人像回归模型均不触发。直接把 G2 几何光照图输入固定 SAM2 自动掩码设置也没有分出部件。**不接入默认工作台**；G1/G2 已用于构造规则，后续不再当独立泛化样本。下一轮检查相邻块共享表面边界能否去掉 G2 杯身假线，同时保留杯口真边；再做新物品/人像验证。工作台 GUI、配色、准备页和 3MF 此轮未验，默认 A 不变，累计 3D 14/20。证据 `.tmp/dev/general-regions/g-holdouts/review.md` 及 `g1-orca-badge-planar-focus.png`、`g2-monochrome-ice-cream-planar-focus.png`。

# 2026-09-21 通用分区上一轮：普通物品输入核对，G2 可用于几何压力测试

- 冻结的 G1 OrcaBadge.3mf 是三个对象、39 个引用组件的 Orca 工程，不能直接作为当前单 GLB 分区链路的同条件样本；需保留来源地构造/找到可导入的多部件 GLB，或把原生 3MF 导入另作独立测试。G2 冰淇淋 GLB 可由现有解析和八视角渲染处理，34,478 面、一种纯白材质；原纹理视图只剩白轮廓，几何光照可辨冰淇淋、杯、底座，是验证几何证据价值的合适压力样本。这里只完成输入适用性和八方向预览，**尚未跑自动分区/工作台**，不计 G 或 P3 通过。两资产哈希与冻结登记一致。下一轮补 G1 兼容输入并对 G1/G2 同条件运行 A/B/C，按原纹理与几何视图分别评估；默认 A 不变，累计 3D 14/20。证据 `.tmp/dev/general-regions/g-holdouts/asset-review.md`。

# 2026-09-21 通用分区上一轮：局部三维距离带可防碎点，但形状收益不足

- H9 衣领的正面可见交界与三维网格接触边并不重合。单视角整段条带使正面更顺，但侧面碎点；八视角合并仍斑驳，因此拒绝这两种屏幕掩码。H7 白领复核同类距离问题：可见条带 820 面只有 12 面直接接触衣身，H9 是 1,985 面中 0 面。三维距离带解决连续性，但整圈改变超出局部手势；和手势的三维邻域相交后，H9 候选无条带式斑驳，却只带来较小形状改善，放大后左端出现新台阶。**拒绝接入工作台**，不再围绕 H9 图邻接步数调参。下一轮按计划验证 G 普通物品和新未见人像的失败类型，寻找更通用的表面边界方法。当前只是离线冻结面 ID 试验，未改源码/用户模型/草稿或默认 A；3D 累计 14/20。证据 `.tmp/dev/general-regions/round28-arc-probe/review.md`、`round29-geodesic-band/review.md`、`round30-local-band/review.md`。

# 2026-09-21 通用分区上一轮：切向拖动无可见收益，已撤销

- 在 H9 干净衣领上重复同一 13 像素拖动，切向衰减相对各向同性 72 DIP 只改变 107/974,626 面，八方向最多 20 个可见像素差异，尖端仍在。定向测试、真实 GUI 和颜色守恒通过，但视觉价值不足；切向代码与其测试已移除，独立试验草稿恢复为保存基线，逐面逻辑区/槽位/颜色差异 0。上一轮的近指针预览响应和 opt-in 72 DIP 保留；默认产品 A 不变。下一轮检查三维面掩码轮廓，试表面约束后再用 H7/G；累计 3D 14/20。证据 `.tmp/dev/general-regions/round26-tangent/review.md`。

# 2026-09-21 通用分区上一轮：扩大衰减让衣领弧线更长，但尖端尚未解决

- 独立 H9 试验副本先恢复到保存的干净衣领，再把大区域拖动衰减范围 44→72 DIP；小五官的局部范围不变。真实工作台 13 像素上拉命中/预览 5 毫秒、完整编辑 2,130 毫秒，4,008 面衣领→衣身且槽位 8→1，其他面逻辑区/耗材槽/颜色零变化。冻结八方向对照见 `.tmp/dev/general-regions/round25-shape/wide-drag-front.png` 和 `wide-drag-eight.png`：正侧面形成较长弧线，背面没有可见溢出；起点仍尖，因此只留在 opt-in H9 副本，不推广默认 A。下一轮沿边线切向控制局部变形，再检查 H7 领口/卷发和 G。试验草稿留在开发程序供查看，保存版本保持干净；无新增 3D，累计 14/20。完整证据 `.tmp/dev/general-regions/round25-shape/review.md`。

# 2026-09-21 通用分区上一轮：H9 拖动起点已流畅，边界形状仍需收敛

- 纠正上一轮耗时归因：旧日志中拖动放开后计算约 2 秒，主要卡在按下鼠标时同步收集整圈可见边线。将预览采集限制在指针附近 128 DIP 后，真实 H9 工作台 974,626 面/111 编辑区的衣领拖动起点对 6,816 条边采 3,132 次射线，耗时 11 毫秒；放开到完成 2,305 毫秒。实际转移 2,018 面，衣领→衣身对应槽 8→1，其他面颜色/槽位变化 0。尖楔形仍在，本次测试笔已撤销，当前试验副本回到原草稿。只保留这一轮的响应优化，默认 A 不变；下一轮约束边界曲线和邻近材质，再评估 H7/G。无新增 3D，累计 14/20。证据 `.tmp/dev/general-regions/round24-boundary-hit/review.md`。

# 2026-09-21 通用分区上一轮：逻辑编辑层在 H9 实际工作台可用，拖边界仍需收敛

- H9 独立历史副本 974,626 面，原生 A 的 156 块对应 111 个稳定逻辑编辑区；点选只突出当前轮廓。真实 GUI 中头发 415,075 面改成现有蓝色耗材后，选区外逐面槽位/颜色变化 0，逻辑区保持；撤销、重做、保存新版本、历史重开通过。此功能仅对显式带 `edit_regions` 的试验模型生效，默认 A 未切换。
- 衣领逻辑边界的第一笔实际转移 4,413 面但耗时接近数分钟。屏幕采样加精确射线优化后的下一笔约 12 秒完成，转移 1,839 面，非目标面颜色/槽位变化 0；视觉出现尖楔形，第二笔已撤销。大历史记录缩进后达 140,973,889 字节而无法重开；紧凑保存后 28,377,229 字节，重启进入工作台成功。下一轮聚焦曲线/语义约束、边缘平滑和拖动响应，再检查 H7/H9 与 G；不能宣称 P3 泛化或 P4 验收。无新增 3D 生成，累计 14/20。证据 `.tmp/dev/general-regions/round23-edit-ui/review.md`。

# 2026-09-21 通用分区上一轮：断开发卷能找回，但打印杂块反而增加

- H7 多视角诊断发现旧合并步骤只保留最大的头发连通块，丢掉的 974.9 面积单位在八方向主要落于右侧/背面卷发；但 H6 同类丢弃候选又落于衣服和眼镜，不能全部恢复。固定规则 v21 仅恢复同原纹理、无衣服冲突且多视角贴近的 24 个断开块，H7 覆盖增加 54,404 面，却使焦点黄线 14,302→20,548、原生双区改色块 222→445。v22 额外的多视角邻近补洞只新增 890 面，块数进一步到 458。两方案选区外逐面颜色/耗材槽变化 0，但分区与打印效果均退化，**拒绝接入默认工作台**。H5/H6/H8/H9 在相同规则下无新增候选。H7 首次证据处理 264.5 秒，其中光栅化 213.1 秒，性能未改善；配色亦未改。下一步停止局部阈值累加，转向保留更完整的语义证据及独立编辑区域/打印色层验证；新候选必须再用未见模型证明泛化。证据 `.tmp/dev/general-regions/round22-projected-hair/review.md`。3D 累计 14/20，本轮未增加。

# 2026-09-21 通用分区上一轮：封闭头发小洞有收益，卷发仍是主要失败项

- 以真实 Orca 工作台 H9 为起点重新核对：GUI 初始 **156 块**（旧离线 A 为 134，不能混用）。首次可编辑处于点击后 202–264 秒；保存试验副本后同进程重开约 422 毫秒。实际匹配颜色仍有肤色偏粉、砖红领子偏灰紫的问题，和分区分开评估。见 `.tmp/dev/general-regions/round18-gui-baseline/review.md`。
- 新的实验性三维封闭小洞规则 `tools/ai/local_surface_region_holes.py` 只填同色、无开放边、无受保护语义和竞争候选的小洞。H9 头发黄线像素 9708→3130，真实 GUI 拼图原生双区改色块数由 v16 的 156→367 改为 156→256，选区外逐面颜色/耗材槽变化 0；H7 卷发仍 222→357 块、头发黄线几乎不变，H5/H6/H8 没有安全填补。因此**不接默认工作台**，下一步处理非封闭卷发/发丝缺口以及编辑层点选；首开等待与配色另记。表面分区相关 10 项离线防网络测试用美颜运行环境通过；普通 `dev.ps1 Test` 因其轻量 Python 缺 NumPy 未运行成功。见 `.tmp/dev/general-regions/round20-safe-hair-holes/review.md`。3D 累计 14/20，本轮未增加。

# 2026-09-21 通用分区上一轮：编辑层表示可减少可点块，但黄线尚未变简洁

- 已在活动源码新增内部 `BeautyEditRegions`：按源模型几何与 SHA-256 固定逐面用户编辑 ID，跨多个原生打印块一次改色后保持编辑 ID 不变；保存记录严格版本/面数/来源校验，不迁移旧草稿。两项 C++ 定向测试通过（`.tmp/dev/logs/20260921-024723-986/result.json`），涵盖真实 `BeautyPuzzle::paint_faces_filament` 切分与损坏/错源拒绝。**尚未接入工作台画线、点选、拖边界或保存 UI**，因此不能算 P4 验收。下一步以受控主窗口实验接入显示与点选，同一批面改色后核对 UI 与打印拼图；默认 A 仍保持。
- 离线 v17 在 H5/H6/H7/H9 现成原生编辑上分离逻辑编辑 ID 与原生打印块。H9 打印块 348 对应 103 个逻辑编辑 ID，但八方向全显示黄线 22,647→24,035（+6.1%），卷发内部仍有碎洞；H5/H6/H7 逻辑 ID 54/214/169。只换表示层不能直接给用户看。下一轮做受控工作台交互：悬停/选中只突出当前大区，核对点选、拖边界、撤销保存和逐面打印色同步。未接入默认路径，当前 A 保留；P3/P4/P5 仍未完成。证据 `.tmp/dev/general-regions/round17-edit-layer/review.md`。

# 2026-09-21 通用分区第三批 P3：v16 保住材质边界，但编辑块暴涨

- H9 在实际 Orca 主窗口由已冻结的砖红高领/深蓝衣身照片生成 974,626 面模型，第 14/20 次 3D 提交。冻结 v14 质量门放行，v16 原纹理选区在正侧背均将衣身与高领分开；真实原生双区改色选区外逐面颜色/耗材槽位变化 0，八方向显示边线 22,647→18,592。然而打印块 134→348；H6/H7 虽保住衬衫/白领，块数仍分别 259→373、222→364。v16 未通过 P3，默认工作台继续原生 A。下一轮只用已有模型验证独立用户编辑区域层与原生打印分块分离，重点看黄线/点选/拖边界、撤销保存和逐面槽位；不要继续靠微调识别阈值宣称泛化。G、人工修正、GUI 时延、P4/P5 仍待测。证据 `.tmp/dev/general-regions/round16-coherent-material/review.md`。

# 2026-09-21 通用分区第二批 P3：v14 未通过，转向衣物材质边界

- 四个新未见人像 H5–H8 均完成真实 Orca 生成、八视角、固定路由与原生改色检查。H5 干净改善；H8 的深蓝背心被质量门正确回退；H6 外套选区触及浅衬衫且块数 259→303；H7 的蓝绿上衣选区吞入整圈白领、块数 222→312，尽管总显示边线下降 29.3%。v14 未过 P3，不接入默认工作台，当前产品保留 A。下一轮做同一衣物语义组内的材质/颜色边界约束，再以 H5–H8 作回归并准备新未见验证。3D 累计提交 13/20；G、人工修正、GUI 时延、P4/P5 仍待测。证据 `.tmp/dev/general-regions/round14-quality-gate/review.md`。

# 2026-09-21 通用分区第二批 P3：H6 混合结果，H7 验证中

- v14 固定质量门第三个未见样本 H6（短卷发、胡须、眼镜、红外套/浅衬衫）的真实 973,450 面模型，支持度比 0.361 路由 v11。头发选区基本正确，但外套选区触及浅衬衫领口；八方向总显示边线 22,657→17,775（−21.5%），原生块 259→303（+17.0%），两次改色的选区外逐面颜色/槽位变化 0。H6 不算干净通过，当前产品继续 A；已冻结 H7 白领/卷发图片的 Orca 设计图正在生成，评估后再考虑衣物边界/块数质量门。3D 累计提交 12/20。证据 `.tmp/dev/general-regions/round14-quality-gate/review.md`。

# 2026-09-21 通用分区第二批 P3：首样本通过，辫子压力样本生成中

- v14 离线支持度回退规则与 v11 候选冻结后，首个新未见 H5（灰黑长发跨绿围巾）实际原生双区改色没有明显脸/围巾/底座误选，八方向总显示边线 20,401→12,858（−37.0%），编辑块 158→171，选区外逐面颜色/耗材槽位变化 0。第二个新未见 H8（黑银辫子跨深蓝背心）在看编辑效果前因支持度比 5.012>2.0 回退原生 A；被拒绝候选确实将大片背心识别成头发，反事实改色使块数 140→315。两模型原识别证据采集分别 245.9/248.6 秒，主要为渲染；不是 GUI 首开耗时。累计 3D 提交 11/20。v14 尚未接入工作台；下一轮 H6 短卷发/胡须/眼镜用于过度回退检查，H7、G1/G2、人工修正、GUI 时延和 P4/P5 仍待测。证据 `.tmp/dev/general-regions/round14-quality-gate/review.md`。

# 2026-09-21 通用分区 P3：冻结候选在未见辫子样本失败

- **最新决定**：冻结 v11 在四个新 3D 人像上做了八方向原纹理与真实原生双区改色。前三例大区边线减少，但第四例辫子跨衣服时大量发丝漏选、可见边线反增且块数 164→256；没有达到“全部无明显退化”，默认工作台及用户保存版本保持 A。后续小组接桥 v12 在历史长发上误选黑色底座；v13 加底部保护后底座正确、辫子改善，但第四例仍有深色漏选条纹且块数 164→339，暂不接入。八个 v11 原生编辑与 v13 受影响样本的非选区逐面颜色/槽位变化均为 0。H1–H4 已用于调试，后续泛化验证需新未见模型；G1/G2、人工修正负担、GUI 时延与 P4/P5 仍未完成。证据 [计划](../3DBeautiful/2026-09-20-generalizable-3d-regions-plan.md)、`.tmp/dev/general-regions/round11-holdout/review.md`、`round13-guarded-bridge/review.md`。3D 生成累计提交 9/20，本轮未增加。

# 2026-09-20 通用分区 P2：三维图割双区仍未通过

- **最新真实改色效果**：SAM2 大区再经三维纹理/法线邻接图割，头发与衣服分别选区，以现有粉、蓝两种耗材连续改色。四个 D 模型八方向的可见边线相对 A 降 46.2%/1.5%/36.2%/43.5%，四次原生改色选区外逐面颜色和槽位变化为 0；49 项定向测试通过。但长发背面另一侧衣服漏选、眼镜衣服底边残留暗纹，三模型原生块数上升。效果未达接入门槛，默认 A/当前运行软件保持；下一轮处理同语义跨多个连通块和高对比内部小洞，复测不同耗材的双区改色。H/G 未参与调参，P3–P5 未执行。证据 [计划](../3DBeautiful/2026-09-20-generalizable-3d-regions-plan.md)、`.tmp/dev/general-regions/round7-graphcut-pair/review.md`、`*-hair-then-blue-cloth-all-effect.png`。

# 2026-09-20 通用分区第一轮：候选不接入默认产品

- **P2 最新实验结论**：四个 D 模型的 SAM2 编辑组使八方向显示边线减少 30.6%–44.4%，但用现有单层拼图真正改色，长发模型从 121 增至 415 块且显示边线反增。四个真实模型探针均保持选区外原生颜色与耗材槽不变；49 项定向测试通过。此候选未进入工作台，下一轮改为独立编辑选区层与打印颜色层。四个 H 人像模型已冻结且未用于调参；P3–P5 未验收。证据 [计划](../3DBeautiful/2026-09-20-generalizable-3d-regions-plan.md) 与 `.tmp/dev/general-regions/round4-edit-group-v4/review.md`。

- **持续执行更新**：P2 首轮 SAM2.1 small 通用候选完成四模型32视角原生 B/C 对照；三模型分区无变化，眼镜165→162且逐面打印色不变，质量收益不足，拒绝默认接入。16→32自动点阵的代表背面视角掩码数量未增加，CPU耗时约7→24秒；GPU性能尚未实测。证据 `.tmp/dev/general-regions/round2/{index.html,progress.md}`。四张未见人像2D参考图已冻结，3D提交仍为4/20；下一步生成独立3D保留集和试验其他区域构建方式。P3–P5未完成，当前产品不变。

- 按 [通用三维分区与人脸约束计划](../3DBeautiful/2026-09-20-generalizable-3d-regions-plan.md) 完成四开发模型完整识别证据采集、原生分区/匹配 A/B 和各八方向视觉检查。新增实验模块 `tools/ai/local_surface_region_consensus.py` 未安装到运行包、未启用默认路径。区域数眼镜172→165、长发121→123、卷发301→272、白西装177→164；眼镜头发与白西装后颈出现退化，因此拒绝默认接入。五官保护区打印色变化为0；不代表原识别正确。8项Python、48项BeautyPuzzle和8次原生探针通过。证据 `.tmp/dev/general-regions/round1/{index.html,result.json,review.md,manifest.json}`。
- P0保留集/物品与界面时延仍未完成；下一轮P2验证完整区域候选＋五官约束，分别评估边界和取色，不继续针对四模型调阈值。当前运行程序保持；本轮无GUI导航、准备页/3MF/切片或新生成。完整识别含证据采集约166–238秒，CPU渲染占主要时间，候选整合额外约0.9–1.3秒；不冒充真实首次进入界面耗时。

# 2026-09-20 同色衣服与头发的多余分区简化

- 四个真实模型各检查8个环绕方向，发现背部同色衣服和后脑有多余编辑边界。新自动分区匹配后，仅合并相连、同耗材同颜色、原始色差≤8且接缝平缓的衣服/头发及邻接未知块；组内完整色差约束防止连续吞并到另一种颜色。五官/皮肤、不同已知类别、断开表面和明显折角保留；衣服碎点不再误用五官保护规则。只在首次自动分区执行，不重写已保存编辑。
- 48项BeautyPuzzle测试通过。四个固定识别证据经实际C++分区/匹配，全模型编辑块为195→182、159→136、140→117、442→188；逐面打印色及耗材编号变化均为0。32视角对照和输入身份见 `.tmp/dev/body-regions-v2/{index.html,result.json,views.json}`。这是边界简化，不是识别准确率或打印效果提升；长发遮挡手臂、后颈误色及后脑残留分区仍待修正。使用上一轮固定四视角识别证据，未为四个模型重新跑完整八视角识别。
- 已接入现有工作台首次自动分区链路。当前旧内部版保留运行，本轮没有GUI导航、准备页/3MF/切片复验或打包。后续优先检查遮挡处皮肤/头发/衣服的识别和配色，不以增加分区数量替代质量改进。

# 2026-09-20 现有版本人体大区域补充

- 已接入可选、固定哈希的 Selfie Multiclass 人体识别，复用八个全身视角，仅对未知面补 hair/neck/cloth；保留原有五官、已有标注和形状锚定路径，不采用 PR13 耳部扩张。至少两个独立视角且所有可见证据一致，底部10%保守排除。多人和冲突处保持原状；覆盖仍稀疏，不能宣称完整区域或边界平滑。
- 四个历史模型的同面ID回放保留全部既有标注；眼镜模型完整原生客户端识别、几何证明及证据消费通过（29断言）。90项Python相关回归、41项开发运行/请求检查、86项原生相关测试通过，后续受影响回归复核通过。证据/对照 `.tmp/dev/body-supplement-v1/{index.html,review.md,validation.json,native-acceptance-final/host-acceptance.json}`。
- 开发程序 `.tmp/dev/run` 已编译、刷新并核验；开发配置已指向本轮可选人体权重，旧配置保留在上述证据目录。策略v5与权重身份隔离旧分析缓存，已保存美颜版本不自动重写。当前打开的旧内部版未替换；本轮未GUI导航、准备页/3MF/切片验收或重新打包。下一步在不吞边界的前提下改善亚像素三角面覆盖。

# 2026-09-20 Windows 内部候选版整理

- 用户要求：本次移除 ImageMap，不纳入 PR #13；同步其余更新，制作安装包并验证安装后的程序。仅少量内部测试，不发布、不推送。
- 来源：本地 `301ff41e88` 加已有未提交工作；纳入远端 `301ff41e88..bf63b3b605` 与 `5c193ab321..decf64fab12` 的变更，排除 PR #13 区间。以完整文件哈希快照标识，不能称为干净合并提交。
- ImageMap 入口、模型负载、连续色切片及其专用依赖已移除；旧 ImageMap 3MF 明确拒绝加载，需用原始 GLB/OBJ 重新配色。普通原生涂色、耗材叠色、3MF 保留。
- 保留当前 FaRL/MediaPipe 美颜识别，提供可随安装移动的离线运行环境；这不是 PR #13。`ORCA_BEAUTY_RUNTIME_ROOT` 指向 `scripts/stage_beauty_runtime.py` 准备的运行环境。显式用户配置优先，可关闭识别。
- 原模型被已保存美颜版本或草稿引用时禁止删除，避免编辑记录失去基础纹理。
- 本轮安装验证的进度和结果记录在 `.tmp/dev/internal-candidate-20260920/plan.md` 与后续验证报告；不能把下方历史运行证据当成本次安装通过。原用户编辑另存为 `finish-bb0b7972-…`，当前工程备份为该目录 `before-update.3mf`。

以下为之前阶段的历史交接（其中 ImageMap 当前状态已被本节取代）：

# 当前开发状态

更新：2026-09-20。此文件是当前交接入口；源码与本轮证据优先于历史报告，不累计逐轮日志。

- **当前待看效果（肤色协调离线试验，2026-09-20）**：新增 `tools/ai/portrait_skin_study.py`，复用已有单人语义，将明确皮肤块统一取色；面积加权面部亮度35%–75%区间估计中间色，经真实原生叠色推荐器选配比。眼镜及两历史人像53/52/49块协调，深肤色无更优建议保持；几何/边界/区域身份不变，眼唇眉标注面未改，有4个头发边界面随整块改变。5项离线测试及原生探针通过。逐张检查：脸鼻颈割裂减轻，但仍偏粉，眼唇/发色和未识别颈部灰斑未解决；用户尚未验收。对照与证据 `.tmp/dev/skin-harmony/{glasses-comparison.png,female-comparison.png,blazer-comparison.png,dark-comparison.png,validation.json,review.md}`。这是独立预览，未接入工作台、未创建耗材槽/GLB/3MF、未更新运行软件；试验JSON故意去掉槽位，不能直接当保存文档导入。原始照片/模型/记录保持，未新增生成，累计4/20。后续根据观感决定算法，不能将已有运行版本误称为本次肤色协调版。

- **最新用户验收纠正（2026-09-20）**：用户反馈本轮肤色比上一版更差；原色驱动叠色的视觉效果未通过，不能将下述“13处数值改善”或流程测试通过当成观感改善。眼镜样本只改脸部主块/上下唇，脸部由粉色变为粉60%+灰40%，鼻子/脖子/耳部仍保留旧色，造成灰褐脸与偏红相邻肤色割裂。目标采用原纹理面积均色、没有区分光照与底色，推荐仅比较单块色差，缺少全脸肤色协调与唇肤对比约束。旧版本已保留；本次诊断未修改模型或运行代码。后续应以上一版观感为基线，先验证整个人脸肤色一致性，再决定是否采用新叠色。

- **当前完成（原色驱动原生叠色推荐，2026-09-20）**：工作台“添加叠色耗材”按选中块原始纹理面积均色预填 Orca 配比；同材质且比当前颜色/物理单色更接近才推荐，否则保留手动入口。六人像18处目标13处推荐、5处无更优建议，包含两历史人像及新男性；不改识别和自动整体配色。65项相关C++通过。真实眼镜模型确认肤色/上下唇三块改色，取消、撤销重做、复用槽位、保存恢复、准备页及3MF保存重开完成；956327面槽位一致，155区域/几何/指导保持，7旧模型及10旧槽位保持。证据 `.tmp/dev/region-mix/{validation.json,review.md,glasses-comparison.png,native-region-prepared.3mf}`，新资产 `finish-5e5d2f80-96df-4750-a4c7-00c36ee5a569`。源身份 `a0d8d8d21a6f3e75c1f8e435660095053ecb1d4566217eb01045e710241ce217`，247构建输入匹配；日志 `20260920-100559-359`，PID36420/窗口925184，最终仅修正标题分隔符。未新增生成，累计4/20。仍有鼻颈协调、眼周和发色问题，六样本不代表广泛泛化；旧网格/排版/擦料塔越界警告保留，未切片/实物验证。下一步优先相邻语义区域协调与眼内深浅，避免无差别增加叠色。

- **此前完成（漏识别五官的表面上下文补齐，2026-09-19）**：整块眼唇没有 FaRL 内部种子时，允许通过短共享网格边路径连接同人物已确认面部。至少两个独立终点、最多八条边、两个视角均可见；路径不进入着色、不放宽阈值、不增加相机。六样本局部对照：眼镜补回449面眼区/232面虹膜，胡须补回7面口腔；深肤色、两历史女性、新增男性的已有提示保持。网格边数限制仍受密度影响，不宣称广泛泛化。肤色偏粉、唇偏灰和小碎区仍待优化。
- **本轮验证与运行**：88项Python、79项相关C++及集成检查通过；CTest中两项环境跳过已在配置解释器后通过WorkerClient组。244项构建输入哈希一致。源身份 `fc4a27f8fdda20c7f85976f194453f173b1d6dbf7a0b132dc50693d93bb17581`，日志 `.tmp/dev/logs/20260919-225308-441/result.json`，PID33168、窗口2952082。真实眼镜新导入→识别→155区域→保存恢复→准备页→另存3MF完成，资产 `finish-a3998e84-1edc-45e0-b5df-a195a2377b72`；956327面槽位逐面一致，六旧模型与8项配色/叠色设置保持。完整重识别对照143→155区域/2532面变化，与冻结三视角449面补齐分开记录。证据 `.tmp/dev/feature-consensus/{validation.json,review.md,context-results.json,context-prepared.3mf}`；对照 `.tmp/dev/portrait-batch/surface-context-glasses-comparison.png`。新增男性 `1a42397d-299b-4638-879c-ea058653dcb6` 经真实GUI生成并保存264区域基线，最终规则仅跑局部对照，未再跑最终完整GUI。累计3D生成4/20。两原照/原始GLB/旧工程保持；新3MF未重开、未切片，已有网格警告和排版靠近提示保留。下一步优先五官内部深浅、肤色唇色，避免只有提示计数提升而观感不变。

- **此前完成（历史列表性能，2026-09-18）**：历史列表仅保留卡片所需字段，同一进程按文件高精度修改时间/大小复用摘要，避免每份美颜编辑记录完整解析最多三次。摘要仅读；完整模型加载和编辑保存仍使用原始JSON，不迁移或覆盖现有记录。首次仍需扫描JSON语法，图片解码/卡片布局与工作台准备未异步化，不宣称整个页面毫秒打开。
- **本轮验证**：17项相关C++测试、集成检查通过；31份真实历史元数据与完整读取对照384项断言通过。摘要冷读取5.94秒、缓存读取0.001秒；原完整读取一遍9.31秒（不是三遍，也不是完整页面耗时）。主窗口首次/重复列表、加载指标更新、重启后历史加载及工作台恢复通过；205区域、6物理耗材保持，GLB/五官提示/区域配色哈希与上轮一致。证据 `.tmp/dev/history-performance/validation.json`。保留模型 `finish-07f1d907-dc73-45e7-b294-d6545788d7f6`（20:14:52）。本轮没有修改识别、配色、切片或打印逻辑。
- **当前识别基线**：MediaPipe眼唇轮廓通过可见原生三角面落回3D，独立视角一致后作为编辑提示；FaRL提供人物归属，鼻眉仍沿用FaRL。局部碎块清理后205区域，但少于10面的碎块仍有138个（此前FaRL版118个），尚未解决全部碎区或广泛泛化。上轮94项C++/41项Python及原生29项断言证据保留于 `.tmp/dev/face-structure/validation.json`；见[工作台五官轮廓](../3DBeautiful/local-eye-landmarks.md)。
- **此前工作台流程**：历史本地GLB/带纹理OBJ导入、缓存恢复、空格暂隐边线、新区域保留在更多操作、原生MixedFilamentDialog叠色和固定槽位直接保存导入已落地。此前83项回归及3MF重开逐面槽位/叠色配方一致，证据 `.tmp/dev/workbench-flow/validation.json`；本轮不重复声明为新的切片验收。

- **此前眼部验证**：可选本机 MediaPipe 1.0.1 眼睑/虹膜提示限制在已验证 FaRL 眼区；眼部控制点、放大编辑、主动细分及撤销/重做已在此前主窗口验证。最后正面视角优选/旧眼珠控制点资格修正现已并入本轮运行版本，未单独复测其识别效果。此前用户版本 `finish-57dadfc7-f144-4928-860d-233a55286fc4` 保留；本轮又先保存了用户新女性模型为 `finish-f9abc6ea-1bb2-4060-ac57-ae72cf6fb853` 再正常退出旧实例。旧证据 `.tmp/dev/eye-landmarks/validation.json`；边界见 [本地眼部关键点](../3DBeautiful/local-eye-landmarks.md)。

- **此前完成（2026-09-18）**：眼珠等小区域增加五个直接拖动的控制点：中心移动、左右调宽、上下调高；原边界拖动按小块尺寸缩小影响范围。原始全彩的已识别眼区内进行保守深浅细分，新分区使用细分提示；已保存版本仅通过「更多操作→细分眼部深浅区域」主动应用、可撤销。优先修整重叠的已有眼珠，保留区域身份和配色，并清理本次新生碎片。默认六耗材匹配、v2 槽位与原生导入契约保持。
- **此前眼部验证**：Windows 运行源身份 `44001765c294bf85452b11637a6f48099b74395a29aed3d33e522ac10c360fa0`，`.tmp/dev/logs/20260918-131043-495/result.json`；35项区域/拖动回归全部通过，CTest 5.60秒（130747-736），集成检查通过，230项运行输入哈希匹配。真实949,058面模型眼部细分改变286面，146区域及6耗材槽位保持；宽度、局部边界、高度、移动分别验证61/18/53/46面变化，撤销/重做、另存历史版本与重新加载通过。新13:16:46资产 `finish-cc9a3914-c924-4e1f-bf5a-b2b2a4432eb2` 保存细分和轻微宽度调整；原12:37:41资产 `finish-825a91d1-…` 未覆盖，测试草稿已清理。证据 `.tmp/dev/puzzle-workbench/eye-controls-validation.json` 与 `eye-controls-final.png`。本轮未重跑切片或3MF往返。
- **此前配色/导入验证**：源身份 `ab0374727d3c015e3ec86cb2ddbd8d0daff29637348755608774eb0c563a0615`；36项区域/配色逻辑（110619-706）、9项导入/纹理回归（114737-211）通过。真实主窗口验证默认6耗材/146区域、原始对照、选色/取消、拖动/撤销重做、保存/重载/重启恢复、准备页无二次匹配导入及导入撤销重做。3MF保存→原生重开→另存后，949,058面槽位逐面一致，6耗材颜色一致；对应导入/切片源码本轮未改。
- **保留与证据**：用户已允许保存0.12mm测试预设；原旧窗口正常退出，原GLB及工程保留。后续放弃未保存工程被自动审批拒绝，改为先完整另存诊断工程和测试预设后正常退出，未绕过拒绝。GUI发现并修复UV缝重复顶点对应、物理耗材类型来源、固定涂色误启用ImageMap三处交接问题。证据集中于 `.tmp/dev/puzzle-workbench/matched-palette-status.json`、`matched-artifact-evidence.json`、`matched-3mf-evidence.json`；测试工程 `matched-palette.3mf` 与 `matched-palette-reopened.3mf` 含移开的旧对照模型。新工作台资产 `finish-8a9faf73-bf3e-4dbf-8315-b0a34cf0fec0`，几何/法线/UV/索引与保存前逐属性一致。

- **当前最高优先级**：拼图工作台保持点选、双击改色、拖边界调整大小。最新实现将逐三角边线整理为有界贴面曲线，增强固定屏幕宽度的深青/白衬普通边线与橙色选中边线；吸附和拖动使用同一可见曲线。实际分区使用物理尺度窄带扩散，按候选邻接片段独立接受/回退。选色仍优先准备页物理耗材。
- **可见入口**：图片生成/历史模型 → 3D美颜工作台（可跳过）→ 返回作品/导入 → 准备页。保存过v2/v3区域配色时直接沿用确切物理/原生叠色槽位；其他入口进入既有颜色匹配弹窗。ImageMap均默认关闭，用户需要时手动启用「ImageMap 连续色（实验）」；固定区域涂色与连续色切片不同时启用。工作台GLB/JSON保留原始UV/纹理和区域编辑记录；固定配色3MF保留原生涂色与耗材，不能还原完整工作台编辑记录。ImageMap方式导入的3MF另行嵌入纹理资产。
- **核心实现**：固定官方 v1.0.44 提交 `235e94dbbde65b2d0750df9a7574c050f90739df` 的 Generic Solver / PigmentPainter，保留来源许可证。UV / 顶点色 / 贴图随 ModelVolume 保存；完整层轮换物理耗材，按可见度调整侧面外壁宽度、中心线和真实挤出量，兼容普通混色路径。3MF 内嵌纹理资产，模型更改使切片失效。
- **美颜实现**：初始/语义分区、菜单平整和拖动使用同一实际分区平滑，保留面积预算、颜色差约束、连通性与原有小块。修复一处坏候选导致全模型扩散结果回退的问题。显示曲线固定交汇点、有界移动并投影回表面；三角带描边避免显卡粗线限制，颜色不受光照压暗，命中范围14 DIP。显示线不进入打印模型。耗材色板、草稿、保存、基底纹理重烘焙沿用原契约。
- **此前边界平滑验证**：101 项相关 C++ 测试全部通过（15.12 秒），集成检查通过，Windows 增量构建与运行身份匹配。真实949,058面模型平整10,586面，146区域及颜色保留，边界边数10932→9985、实际长度减少约4.8%；操作及截图往返2.51秒。最终20px吸附拖动更新29面，无新增区域，撤销回已保存状态。前一显示迭代重做和6耗材色板/取消通过，对应业务逻辑未再改动。
- **此前边界平滑证据**：`.tmp/dev/puzzle-workbench/curved-boundaries-validation.json`、`curved-boundaries-artifact.json`、`curved-boundaries-final.png`、`curved-boundaries-drag-final.png`。用户最新22:43:43版本 `finish-07a0f665-…` 保留；平整后另存23:29:26版本 `finish-24f31591-…`，GLB SHA256 `8428e6c7655ede02f7f70aa7c9cc4603253acb3ab94ae0c3472b94dcd67b2712`，位置/法线/UV/索引逐属性一致。临时拖动已撤销。旧证据与 `before-direct-edit.3mf` 保留。
- **此前切片证据**：`.tmp/dev/imagemap-workbench/validation.json` 与 `project-evidence.json`。`finished-from-workbench.3mf` 的 15% 表面平滑保持完整纹理；0.12 mm 切片 833 层、832 次换色，约 5 小时 3 分、69.06 g；`finished-software-only.gcode` 为此前软件输出，不冒充本轮美颜 v2 切片结果。相关切片源码本轮未变。
- **效果证据**：`.tmp/dev/imagemap-port/validation.json` 记录源身份、运行版本、文件哈希、输出与实际验证范围。主样本 0.12 mm 为 833 层 / 832 次换色；卡通 0.20 mm 为 500 层 / 499 次换色，实际均为 T0–T5、每层一种颜色、外壁 0.32–0.95 mm。官方与移植同视角分布接近；正面仍有层纹、顶部仍露出周期色。工艺/版本不一致，不能把预估耗时和重量差异当成纯算法差异。
- **运行版本**：`./dev.ps1 -Jobs 8`，复用 `.tmp/dev/{build,run,data,logs}`。最新运行日志 `20260918-223340-206`，PID20812、窗口1513612，源身份 `5d139c2c0c261922d360bbcc4f4325208a780e677b75ef2b55ca38b3709bc418`，243项运行输入哈希匹配。保存 `0.12mm History Performance 20260918` 预设后正常退出旧实例；恢复 `.tmp/dev/face-structure/before-structure.3mf`，两个女性模型、6物理耗材及叠色7/8保留。开发脚本首次读取Windows进程列表失败，经正式提权重跑成功。基线 `301ff41e88f98478e22b2882e9162be2f80e5d87` 加未提交工作，不是干净集成提交；未推送、未发布。
- **已知范围**：目前侧面成像；上游顶部成像、标定及编辑 UI 未移植。2–8 物理耗材、单模型部件；拒绝不兼容路径模式。两个样本不等于广泛泛化验证。图像透明度/PBR 等复杂材质尚未完整覆盖。两原模型开放边/悬空提示仍在；原样本擦料塔在板边，验证工程中移回板内。
- **硬件与实物**：WonderMaker 四喷嘴 / 六材料实际路由仍未确认；现有软件自动映射和 T0–T5 不等于硬件通过。未发送打印、未做实物或 TD 标定。不要反复询问已授权的软件验证，也不要把 G-code 导出写成可直接实机打印。
- **后续重点与边界**：继续检查多样本/遮挡下的眼唇稳定性、鼻眉轮廓及局部小碎块；目前MediaPipe形状与FaRL语义分开，不能宣称解剖级瞳孔或通用人脸几何识别。最多为两张脸增加各三个768px视图，依赖原生Z向上约定，倒置/大幅倾斜/多人尚未验证。首次CPU识别约3分钟，已保存指导可复用。历史元数据重复完整解析已修复，首次摘要扫描、图片/卡片布局和工作台准备仍有开销；缓存仅当前进程有效。衣服仍按颜色/几何分区；缺失依赖保持回退，不下载或付费调用。实际颜色按三角面存储，放大仍有网格台阶，形状拖动偶有新增小块。工作台GLB＋JSON恢复编辑记录，3MF此前已验证原生打印颜色往返；本轮未重切片、实物打印或骨相美颜。
- **开发方式**：本轮单Agent，复用暖构建和真实历史资产，串行修改/构建/GUI验证，保留已有工程。早期边界平滑工作曾使用限定文件所有权的Agent；不作为日常默认。规范见 [AGENTS](../../AGENTS.md) 与[快速开发](quick-development.md)。
- **其他既有边界**：未退休旧 CLI、未合并 PR #12、未改远程 CI。原工程存在大量未提交改动，禁止清理/重置/一并提交。历史独立验证任务额度不可用，保留 NOT_RUN；日常自检自行完成。多人像验证授权3D生成已用4/20，新增前核对 `.tmp/dev/portrait-batch/manifest.json`，不因重启或新一轮验证重置预算。
