# 阶段 72：局部薄壁风险区聚类与排序实施计划

日期：2026-08-23

1. 新增失败测试：附着细颈的四个对射面通过拓扑邻接和跨壁距离归为一个区域；区域指标、排序和证据上限稳定。
2. 扩展采样返回值，保留每个命中面的测量厚度；实现网格邻域并查集和风险排序。
3. 输出 `structural-v8` 区域 metrics/evidence，并保持 v7 平面证据兼容。
4. 扩展 C++ 可选区域解析；GUI 优先定位最高风险区域，并在详情和状态中显示区域数量。
5. 运行定向/全量 Python、C++ 回归、Windows Release 构建和真实高面数性能复核。
6. 使用仓库本地应用与 Sidecar 验收最高风险区定位、旧证据降级和撤销，记录并提交清洁工作树。
