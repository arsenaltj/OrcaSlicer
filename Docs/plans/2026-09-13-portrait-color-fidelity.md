# 人像与颜色保真优化：执行和验证记录

2026-09-13，来源任务“3D模型人像优化” `01a08b37-f26c-7f41-93b3-1fc7d4c7b59b`。基于本任务 GLB/OBJ 分析，以及 9 月 12 日颜色匹配研究与 9 月 13 日婚照实际工作流。当前源码是 `216d36d51638332dead62aea1452fffbfbe70c23` 加完整工作树修改；保留混元接入和婚照配色/接缝修复，不把它们计作本次新增。

## 本版范围

1. 原色导入不再隐式固定为六色。完整颜色匹配默认 12 个可调整的目标色、边界清理 0；实体耗材数量按实际工程配置，现有六通道上限不是必须用六卷料。可显式沿用已开启的试色或从模型原色重新配色，两种方式都保留局部面改色。
2. GLB 几何柔化保存保留原 UV、内嵌图片、材质和节点。先覆盖现有 Tripo 常见静态单网格结构；不支持的结构明确报错并保留原件，不静默导出成仅顶点色模型。法线与几何一致性、纹理字节完整性需自动检查。
3. 写实人像设计图增加克制的表面精修、自然肤色、软中性光和嘴唇/眼睛边界约束，保留身份特征及多人各自面貌。不提前按打印耗材量化颜色，不把提示词测试称为实际生成效果提升。

预览及当前打印导入仍存在纹理转顶点/面颜色的采样限制，本版不将 GLB 母版保真等同于全分辨率原生 UV 预览，也不宣称完成语义识别或实物色彩校准。

## 本轮付费预算

用户原文：“可以无限使用生图模型以及 20 次 3D 覆盖 tripo 和混元，提升效果”。图像调用按本项目改进需要使用；**Tripo 与混元新 3D 任务合计最多 20 次，当前 0/20**。失败或结果不明的提交按已消耗尝试记录，不自动重试或切换供应商。只读查询、已有模型加载不算新 3D。

优先沿用婚照颜色分析的输入与已存历史作为对照。计划先做少量成对实验，对同一确认设计图比较两家提供商，再对有效方向复测；不为用满额度重复生成。每次真实 3D 必须从实际 Orca 主窗口发起，保存原输入、确认设计图、提供商任务 ID、参数、原模型和新版本，检查历史/切页/重启恢复。

固定验证任务已回执：014-r5 的 `launch_live.ps1 -RunLabel live-initial` 小猫图混元付费/外传启动两次被自动审批审查拒绝，未有适用正式批准；015 没有新尝试，不能称再次拒绝。旧 Tripo、red-hat、方总设计等具体拒绝保留原记录。新预算不解除旧具体拒绝，不改入口重试；独立本地实现和离线验证继续。不得据此扩大成所有不同的新操作都被禁止。精确记录见固定验证 `.tmp/validation/artifacts/20260912-014-hunyuan3d-r5/platform-denials.json` 与 `20260913-015-hunyuan-photo-r1/approval-review.json`。

## 验证与结果

- 必要自检：Python 图像提示词/预处理离线测试，C++ 模型配色导入、GLB/柔化相关测试，架构检查，受影响应用构建。
- 固定“验证与交付”任务 `01a08c10-b505-73d0-9f94-05ab0b4c8b33` 使用冻结快照、独立源码与运行/配置目录，实际检查原色/试色重新匹配、局部改色、取消、历史新版本和重启；不继承旧版本的验收结论作为新版本通过。
- 开发自检：Python 预处理 63 项通过；C++ 最终回归 72 项、7326 条断言通过；架构检查通过。完整 `slic3rutils_tests` Release 目标构建成功，应用主窗口运行仍由固定验证任务执行。
- 既有婚照 GLB 副本柔化实测：33 条断言通过，464083 个源顶点发生位移，979020 个面及原始 28503804 字节 BIN 保留，材质与节点保持一致，原文件 SHA 不变；约 17.0 秒。该探针验证保存保真，不证明柔化强度适合人脸，也没有创建软件历史版本。
- 017-r1 完整应用构建、安装和运行目录哈希核验通过，实际主窗口已启动。原色可编辑12色/清理0、6/12/16切换、沿用试色固定色板、从原色重新配色开放控件，以及取消后保持空盘/四耗材/打印预设均取得实际界面证据。
- 婚照目标12/16比6保留更清楚的唇色与眼眉层次，但三者四实体自动映射均有发梢误映唇红的问题，正做局部修正。纯GLB的15%柔化预览、查看处理前和放弃已实际执行；原BIN/UV/贴图/材质/节点及面数保留。轻柔处理视觉差异很小，不夸大美颜改善。
- 验收发现纯GLB旧提示与保真行为不符，且参数变更提示仍引用英文Apply。017-r2只修正两份cpp的说明/提示，计算及协议不变；实际新版文案仍需检查。其他本地编辑/历史串联、切片及新生成对照继续。真实调用仍0/20，尚无新生成质量结论。
