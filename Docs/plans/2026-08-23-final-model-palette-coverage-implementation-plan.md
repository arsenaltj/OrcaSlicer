# 阶段 76：最终模型目标色覆盖度实施计划

日期：2026-08-23

1. 扩展 `printable_model_quality.py` 的可选目标调色板分析、v9 报告字段和警告。
2. 增加离线测试：四色中三色显著通过、仅两色显著警告、无调色板兼容、调色板外颜色覆盖率下降。
3. 让 Sidecar 初次制品分析和重检传入任务调色板。
4. 防御性解析新 metrics/evidence，并在模型生成质量卡显示汇总和逐色占比。
5. 跑质量模块、Sidecar、全量模型生成 Python 测试、C++ 定向测试和 Release 构建。
6. 用本目录应用复核带调色板的本地样本；如没有现成四色 OBJ，则使用离线样本验证 GUI，不调用付费 Provider。
7. 写阶段 76 验收记录并提交，确认工作树干净和边界未变化。
