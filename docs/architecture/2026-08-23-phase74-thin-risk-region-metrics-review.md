# 阶段 74：薄壁风险区导航指标验收

日期：2026-08-23

## 结论

阶段 74 已在薄壁风险区逐处导航中显示当前区域的最小实测厚度、采样数和采样面积。用户现在可以在保持模型和切片设置不变的前提下，直接理解区域的排序依据并决定复核优先级。

## 行为与降级

- 首次定位、下一处导航和撤销回已报告区域共用同一指标格式化逻辑。
- 厚度和面积保留三位小数，样本数使用整数；预览主区域同时保留当前报告序号和完整识别区域数。
- 缺失、非有限或不大于零的数值会逐项省略，不影响证据面定位。
- 撤销到空选区或手动改变选区后不显示旧风险指标。
- v7 平面证据定位保持旧文案和旧行为；本阶段没有新增报告字段。

## 自动化验证

- 模型生成 C++ 定向回归：17 个测试用例、143 个断言通过。
- Windows Release `OrcaSlicer` 构建与链接通过；只有仓库既有 `LNK4098` 默认库警告。
- `git diff --check` 通过。
- 本阶段未修改 Python 与报告契约；阶段 72 的全量模型生成 Python 离线回归 232/232 结果继续适用。

## 仓库本地 GUI 验收

验收应用为 `D:\Workspace\06_3DDY_claude\build\src\Release\orca-slicer.exe`，Sidecar 为本工作区 `tools/ai/orca_ai_sidecar.py`，临时回环端口为 `18764`。

对真实模型 `0c85a1eb-4bd8-4db5-bb0d-f0dfc793bb08` 的现有 v8 报告复核：

1. 第 1/16 个已报告风险区选择 17 个证据面，显示“最薄 0.010 mm · 17 个采样 · 0.264 mm²”，并保留“共识别 64 个”。
2. 第 2/16 个风险区选择 6 个证据面，显示“最薄 0.021 mm · 6 个采样 · 0.162 mm²”。
3. 撤销回第 1 区后选区、序号和三项指标同步恢复；再次撤销到空选区后旧指标清除。
4. GUI 只读取本地历史任务和制品；未触发重检、Image2、Tripo、AI 视觉复核或其他付费端点。

## 架构与变更范围

- 代码修改仅限 `ModelGenerationPanel.cpp`；新增文档仅在模型生成计划与架构目录。
- 未修改 Provider、Sidecar、报告 schema、MainFrame、Plater、CMake、依赖、打包清单、3MF、profile、永久端口配置、输出目录约定或原版默认行为。
- 未引入智能切片业务，也未合并、反向移植或复制 `codex/orca-integration-v2` 代码。
- 本阶段未调用真实付费 API。

## 下一步

下一阶段建议把当前区域的指标与质量门阈值并列显示为“实测 / 建议下限”，使用户能区分极薄误差、显著薄壁和接近阈值的区域；继续只做解释和复核，不自动修改模型。
