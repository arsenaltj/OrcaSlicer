# 阶段 71：局部薄壁证据定位实施计划

日期：2026-08-23

1. 新增失败测试：附着型 0.4 mm 细颈报告返回稳定、有效且有上限的面序号；普通盒子、开放模型和旧报告安全无证据。
2. 扩展采样器和 `structural-v7` 报告，输出最多 256 个确定性局部薄壁采样面。
3. 为纯 C++ `VertexColorRegionEditor` 增加通用面序号选区，覆盖重复、越界、空证据及保留原选区行为。
4. 扩展 `AIModelGenerationClient` 可选 evidence 解析，在质量卡增加“定位薄壁”入口并复用现有高亮/撤销链路。
5. 运行定向及全量 Python 测试、C++ 模型生成测试、Windows Release 构建，并使用仓库本地应用和本地 Sidecar 完成 GUI 验收。
6. 记录结果、提交全部改动并保持工作树干净。
